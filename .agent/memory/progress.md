# Progress Tracker

Update after every work session: status, branch/PR, notes. Statuses: `not-started` · `in-progress` · `in-review` · `blocked` · `done`.

**PLAN-17 done and merged: 17a (`core/notify`) as **PR #44**, 17b (Send/Receive staging) as **PR #45**, both on `develop` (2026-07-30). The hard gate was cleared by the owner ("implement 17b as well"); LAN trust chosen over capability tokens, and 17b deliberately reverses two PLAN-02/05 decisions (see decisions.md, 2026-07-30). Plan archived to `completed/`.** Every plan through PLAN-17 is now done. **v1.3.0 released.** **PLAN-18 (Toolbox) implemented in full on 2026-08-06** — 18a (shell + 4 tools) **merged as PR #48** on 2026-08-06, 18b (the remaining 9) open as **PR #49**, rebased onto develop after 18a was squash-merged. Plan archived to `completed/` in the 18b PR — **both merged, so PLAN-18 is done.** **PLAN-21 (Pensieve) implemented 2026-08-08**, run *before* PLAN-19 per its own recommended order (18 → 21 → 19 → 20). **PLAN-19 (Groot — YAML) implemented 2026-08-10 on `feat/plan-19-groot`**, and its library task collapsed to a single registry entry exactly as PLAN-21 predicted. **Release prep for `develop → main` is in progress (2026-07-31).** Side work merged along the way: `fix/npm-audit-advisories` (17 → 6, **PR #37**), `fix/npm-logs-path` (**PR #39**), `feat/loki-code-folding` (**PR #40**), tree-value copy + floating toast (**PR #41**).

| Plan    | Title                                              | Status         | Branch / PR                                   | Notes                                                                                                                         |
| ------- | -------------------------------------------------- | -------------- | --------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| PLAN-00 | Foundation (tech setup, zero feature code)         | done           | main (direct, per plan)                       | Pushed 2026-07-12; develop created                                                                                            |
| PLAN-01 | UI/UX foundation ⛔ approval gate                  | done           | PR #2 → develop (merged 2026-07-13)           | **UI approved by owner 2026-07-13**; gate cleared — PLAN-02 unlocked                                                          |
| PLAN-02 | File transfer (upload + download + live watch)     | done           | PR #3 → develop (merged 2026-07-14)           | Owner tested locally; CI green — PLAN-03 unlocked                                                                             |
| PLAN-03 | Previews + QR tool                                 | done           | PR #4 → develop (merged)                      | Merge confirmed in git history 2026-07-16                                                                                     |
| PLAN-04 | Theming engine                                     | done           | PR #5 → develop (merged 2026-07-15)           | Gate cleared — PLAN-05 unlocked                                                                                               |
| PLAN-05 | Heimdall admin panel                               | done           | PR #6 → develop (merged 2026-07-16)           | Gate cleared — PLAN-06 unlocked                                                                                               |
| PLAN-06 | Clipboard sync, presence, audit log                | done           | PR #7 → develop (merged 2026-07-16)           | Gate cleared — PLAN-07 unlocked                                                                                               |
| PLAN-07 | Runestone (JSON editor Part A + library Part B)    | done           | PR #14 + PR #15 → develop (merged 2026-07-19) | Both parts owner-tested; CI green. Plan archived to `completed/`                                                              |
| PLAN-08 | Variant (JSON & text diff checker)                 | done           | PR #17 → develop (merged)                     | Owner-tested; merged 2026-07-20. Plan archived to `completed/`. Gate cleared — PLAN-09 unlocked                               |
| PLAN-09 | Ops (PM2, backup, Docker, observability)           | done           | PR #19 → develop (merged)                     | All 8 items merged; plan archived to `completed/`. Owner-manual left: PM2 reboot, Linux docker run, live Grafana, release-PAT |
| PLAN-10 | Heimdall Modal (overlay conversion + new sections) | done           | feat/plan-10-heimdall-modal → PR #20 (merged) | Merged to develop; **archived to `completed/`** per owner 2026-07-21. Gate cleared — PLAN-11 unlocked                         |
| PLAN-11 | Edda (markdown editor + preview + library)         | done           | feat/plan-11-edda → PR #29 (merged 2026-07-23) | Merged to develop; **archived to `completed/`**. Gate cleared — PLAN-12 unlocked                                              |
| PLAN-12 | Loki (JS transforms + regex + execution)           | done           | feat/plan-12b → PR #30 (merged 2026-07-24)     | Part A + Part B "Calcifer" combined in one PR; owner-tested through several rounds. Plan archived to `completed/`. Gate cleared — PLAN-13 unlocked |
| PLAN-13 | Accio (read-later / bookmark shelf)                 | done           | feat/plan-13-accio → PR #35 (merged 2026-07-25)  | Owner-tested through 4 feedback rounds (schemes, error clearing, tag suggestions, grid revamp). Plan archived to `completed/`. Gate cleared — PLAN-14 unlocked |
| PLAN-14 | Nimbus (LAN speed test)                            | done           | feat/plan-14-nimbus → PR #36 (merged 2026-07-25) | Owner-tested; CI green. Plan archived to `completed/`. Gate cleared — PLAN-15 unlocked |
| PLAN-15 | Portkey (LAN go-links)                             | done           | claude/plan-15-tp9uel → PR #38 (merged 2026-07-27) | Owner-tested; merged to develop. Plan archived to `completed/` (late — in the 16a branch). Gate cleared — PLAN-16 unlocked |
| PLAN-16 | Observability (logging Part A · metrics Part B)    | done           | **PR #42** (16a) + **PR #43** (16b, stacked) — both merged | 16a: Heimdall Logs deleted, `logLevel` text key, client logging. 16b: metrics snapshot + Prometheus + Tempo/OTel. Plan archived to `completed/`. Gate cleared — PLAN-17 unlocked |
| PLAN-17 | Send/Receive + notifications (17a notify · 17b flow) | done           | **PR #44** (17a) + **PR #45** (17b, stacked) — both merged 2026-07-30 | 17a: `core/notify` + upload/download failure adoption. 17b: staging actions, move-to-downloads, clean names, `upload_audit` dropped (migration 0010), `.svg` served as text. Gate cleared by the owner; **LAN trust**. Plan archived to `completed/` |
| PLAN-18 | Toolbox (Diagon Alley cards expand in place)        | done           | **PR #48** (18a) + **PR #49** (18b) — both merged to develop | 18b was stacked on unmerged 18a at the owner's direction, then **rebased onto develop** once 18a squash-merged. Plan archived to `completed/` in the 18b PR |
| PLAN-19 | Groot (YAML editor, viewer & library)              | done           | **PR #52** → develop (merged as `de73892`)    | Implemented 2026-08-10. Library task collapsed to **one `core/library/` entry** as PLAN-21 predicted; Groot is Ollivanders' 6th card. Owner validated the flow 2026-08-10. Plan archived to `completed/`. Gate cleared — PLAN-20 unlocked |
| PLAN-20 | Edda: Mermaid diagrams + PDF export                | done           | **PR #53** → develop (merged as `fd382e3`)     | Implemented 2026-08-15. Client-only, single PR, no parts. Spike came back the **opposite** way to the plan's premise (DOMPurify keeps `<style>` in SVG) and exposed a CSS-injection hole the plan's config left open. Plan archived to `completed/` in this PR. **Row corrected 2026-08-27** — this had been left at `in-review` after the merge; caught while adding PLAN-23's row |
| PLAN-21 | Pensieve (unified library shell)                   | done           | **PR #50** → develop (merged as `a9b1f58`)    | Implemented 2026-08-08, **before PLAN-19** per the recommended order 18 → 21 → 19 → 20. Two library pages deleted for one shell over `core/library/`. Owner validated the flow 2026-08-08. Plan archived to `completed/` |
| PLAN-22 | Offline mode (warm-load for pure-client tools)     | not-started    | —                                              | Plan written 2026-08-26 on `develop` directly (docs-only session, no code). Heimdall-configurable registry mirrors theme enable/disable; scope is Diagon Alley's toolbox + Ollivanders' five editors |
| PLAN-23 | Atlas (XML editor, editable plist-aware table)     | not-started    | —                                              | Plan written 2026-08-27 on `develop` directly (docs-only session, no code). Gated on PLAN-22 merging; joins the Pensieve as a 4th kind and Ollivanders as its 7th card |
| PLAN-99 | Future backlog                                     | reference-only | —                                             | Never "implemented" wholesale                                                                                                 |

## Recent activity (newest first)

One line per session; **full detail — test evidence, live-verify runs, deviations — lives in [`history.md`](history.md)**.

- 2026-08-27 — **PLAN-23 (Atlas) planned**, in a discussion session the owner opened by asking for an XML editor with plist support, previewed Xcode-style. **The design reversed once, mid-session, and that reversal is the plan's central fact**: it started as a read-only structured preview — matching every existing structured view in Bifrost (Runestone/Groot's tree) — and was explicitly reopened by the owner to a fully editable table (expand/collapse, a type dropdown per row, value editing, add/delete/reorder), true Xcode parity, after the owner supplied a real `GoogleService-Info.plist` screenshot as the visual reference. Logged as a supersession, not a silent rewrite, the same way `decisions.md` already handles a changed mind for Variant. **The architecture settled on for the editable table**: the parsed `Document` (native `DOMParser`, not a plist npm library — every general-purpose plist library collapses `<integer>`/`<real>` and `<string>`/`<data>` to the same JS type, which is exactly the distinction Xcode's table needs to show correctly) is the single mutable source of truth both panes read from and write to; value edits are surgical text-span replacements, type/structural edits necessarily rewrite at least the affected element's own tags (a DOM property, not a shortcut); table edits dispatch as real CodeMirror transactions via one new `replaceRange` method, so Undo/Redo needs no second stack. Row reordering (owner: include it) uses pointer-based dragging matching Edda's own divider precedent, not native HTML5 DnD. **Layout is Edda's split-panel mechanism** (draggable divider, 30–70% clamp, `split`/`stacked`/`single` breakpoints), not Groot's toggle — the owner wanted code and table visible simultaneously — proposed extracting `core/ui/useSplitPanel` at this second consumer, matching how `JsonEditor`/`TreeView` were extracted at their own second consumers. **Two real findings while checking what Groot actually ships, not just what was discussed**: drag-and-drop file import was missing from the task list entirely until caught against `GrootPage.tsx`'s actual feature set, and `client/src/features/edda/EddaPage.tsx` on `develop` HEAD was found to contain 737 null bytes (`file` reports it as binary data), traced to the PLAN-20 merge — flagged, not fixed, unrelated to this plan. Also added: `.plist`-extension recognition, a `bplist00` binary-plist rejection message (most real Apple plists are binary, not XML, and this plan deliberately scopes binary out), click-to-jump from a table row to its source offset, and a valid-skeleton default for new documents. Confirmed (not built) that find/replace is inherited for free — `JsonEditor`'s search wiring is mode-agnostic by construction. **Ollivanders needs more than the padding trim originally asked for**: `.shell-main`'s `max-width: 62rem` cap, not its padding, is what limits the portal grid to ~3 columns; recommended extending Diagon Alley's already-proven `.shell-main--wide` opt-out (PLAN-18b) to `/ollivanders` instead, a one-line `App.tsx` change. Atlas also registers itself in PLAN-22's offline-mode registry as one of its own tasks — the first real exercise of the standing-knowledge checkpoint that plan built. Considered a two-part PR split given how much the scope grew mid-design; owner chose to keep it single. **Nothing implemented — plan only.** Local `develop` was confirmed current against origin before writing (learned from PLAN-22's session, where a stale clone briefly caused confusion); PLAN-20's progress.md row was found stuck at `in-review` after already having merged and corrected in the same pass. Decisions logged (24 rows), `plans/README.md` gate row added.

- 2026-08-26 — **PLAN-22 (Offline mode) planned**, in a discussion session the owner opened by asking whether Diagon Alley's toolbox could work without the server reachable. **The platform finding that shaped everything downstream**: a Service Worker — the standard way to survive a reload or a brand-new tab offline — only runs in a browser secure context (HTTPS or literal `localhost`), and Bifrost is deliberately plain LAN HTTP, so a Service Worker would only ever work from the host Mac's own `localhost`, never from `bifrost.local` or a LAN IP on another device — the same restriction PLAN-18b already hit with `crypto.subtle`. The owner chose to build only the piece that works on every device: an explicit toggle that eagerly `import()`s the JS for Diagon Alley's toolbox and Ollivanders' five editors while still on the LAN, so an **already-open tab** keeps working after the LAN drops, with no persistence beyond the tab's own lifetime (module-registry-only; dies for free on tab close, no Service Worker/Cache API/IndexedDB involved). Direct inspection confirmed all five Ollivanders editors already degrade gracefully offline with **no code fix needed** — Loki's `fetchLokiConfig()` failure already hides execution and leaves transforms working, Runestone/Groot already silently restore via PLAN-19's `core/draftReturn`, Edda already banners a restore prompt. **Variant has no draft persistence at all** (confirmed empty grep on `localStorage`/draft in `VariantPage.tsx`) — flagged to the owner, who declined to add it ("this is not needed"), left as a known gap. **Found and separately flagged, not fixed here**: `client/src/features/edda/EddaPage.tsx` on `develop` HEAD contains 737 null bytes and `file` reports it as binary data, not text — apparently from the PLAN-20 merge (`fd382e3`); unrelated to this plan. **The registry ended up Heimdall-configurable** (owner's requirement) rather than a fixed client array, modeled directly on the existing theme enable/disable pattern (`themes.disabled` in DB `settings`, `DbThemeVisibilityStore`, admin PATCH, live SSE) rather than reinventing one — page-level granularity only, since Diagon Alley's 13 tools already ship in one PLAN-18 chunk and a per-card toggle wouldn't change what's fetched. A `docs/offline-mode.md` technical reference is specced with a fixed section order so the writing pass can't skip a decision already made here, and two standing-knowledge updates are scoped so future plans don't forget this registry exists: a line in `.claude/skills/new-module/SKILL.md`'s client checklist, and a module-registry entry in `architecture.md`. **Nothing implemented — plan only.** Local git clone had drifted behind `origin/develop` by two commits (PLAN-20's merge + an npm-audit fix) mid-session; caught by the owner asking about a specific commit hash, `git pull` resolved it, and PLAN-20/21 were confirmed already merged, so this needed no declared execution-order exception — it's cleanly next as PLAN-22. Decisions logged (10 rows), `plans/README.md` gate row added.

- 2026-08-23 — **npm audit 12 → 0** on `fix/npm-audit-sweep-aug` (uncommitted, pending owner test). Ten of the twelve rows were **six real advisories plus their dependents**, all fixed forward inside the declared carets so only the lockfile moved: `undici` → 7.29.0, `js-yaml` → 4.3.1, `nanoid` → 3.3.18, `fast-uri` → 3.1.6 + 4.1.3, `dompurify` → 3.4.14, `react-router`/`-dom` → 7.18.2 (the one declared-range bump, `^7.18.1` → `^7.18.2`). Only **`fast-uri`** (ajv + fast-json-stringify, i.e. request validation and JSON serialisation) and **`dompurify`** (the markdown renderer, where the advisory is an actual XSS via `IN_PLACE` hook removal) sit on a reachable runtime path; the rest are dev/test-only. The **`brace-expansion` override needed bumping `^5.0.8` → `^5.0.9`** because the 2026-07-25 pin had landed on what the new advisory declares vulnerable — the CVE-2026-14257 mitigation is bypassable through 5.0.8, so the old override was pinning *to* the defect. The remaining four moderate rows were the esbuild-inside-drizzle-kit cluster that 2026-07-12 **accepted as residue**; that decision is now **superseded** — a scoped override `overrides["@esbuild-kit/core-utils"].esbuild = ">=0.25.0"` clears them. The range is deliberately open rather than `^0.25.x` so npm **dedupes core-utils onto the esbuild vite and tsx already install** (one esbuild in the tree, no added weight, no `npm ls` ELSPROBLEMS — a pinned `^0.25.12` was tried first and produced exactly that inconsistency, the nested copy failing its own override's range). drizzle-kit 0.31.10 is still latest and still pulls the deprecated `@esbuild-kit/esm-loader`; npm's only offered "fix" is still the downgrade to 0.18.1. The override's real risk — 0.18 → 0.28 is ten minors of a pre-1.0 API — is **directly testable, and tested**: `drizzle-kit generate` loads `drizzle.config.ts` *through* the overridden loader, and it read all 10 tables and reported "No schema changes". Gate green (lint/typecheck/**526 server + 790 client**/build). Restart smoke run because `fast-uri` is a server dependency: health 200 → SIGINT clean → restart → health 200, a malformed JSON body still rejected 400 (the ajv path), `integrity_check ok`.
- 2026-08-15 — **PLAN-20 (Edda: Mermaid diagrams + PDF export) implemented** on `claude/plan-20-implementation-odv2my`, the last plan in the 18 → 21 → 19 → 20 order. Client-only, exactly as planned: no module, route, table, migration or capability change. **The spike the plan demanded came back the opposite way to the plan's own premise, and that mattered twice.** The plan expected DOMPurify to strip the `<style>` mermaid puts *inside* its SVG, leaving diagrams with no fill or stroke, and specced a `themeVariables`-only fallback. Rendering a flowchart, a sequence diagram and a Gantt chart through the SVG profile in a dark and a light palette showed the sanitized output **identical in length to the raw SVG**, `<style>` intact, computed `fill`/`stroke` unchanged with and without `ADD_TAGS: ['style']` — `style` is on DOMPurify's SVG allow-list even though the HTML profile drops it. So the fallback was never built. **But keeping that `<style>` opened a hole the plan's config left open**: the plan says to leave mermaid's `secure` key list at its default "so an in-document `%%{init: …}%%` directive cannot raise its own privileges", and mermaid 11.16's default list is exactly `secure, securityLevel, startOnLoad, maxTextSize, suppressErrorRendering, maxEdges` — **`themeCSS` is not on it**, and `themeCSS` is raw CSS. Reproduced, not theorised: a diagram carrying `%%{init: {"themeCSS": "& .node rect { filter: url(http://host/BEACON.png#f) }"}}%%` **made the browser fetch that URL during render**, from a document any LAN device can save and `/edda/preview/:slug` serves publicly — breaking criterion 7 ("nothing is fetched from the network") and criterion 6 ("the directive does not take effect"). The `secure` list is extended to cover `themeCSS`/`theme`/`themeVariables`/`fontFamily`/`altFontFamily`/`htmlLabels`, a test pins it, and the live check confirms zero beacon requests. **Two more defects were found by running it, both fixed and both worth knowing beyond this plan.** (1) The pass's diagrams silently never appeared: it collected placeholders, awaited mermaid, then replaced them — but the container belongs to React, which had re-set it from `dangerouslySetInnerHTML` meanwhile, so every `replaceWith` landed on a detached node, which the spec makes a **silent no-op**. Instrumenting the built app showed `isConnected: false` on all three. The pass now **renders into the cache first and swaps the DOM synchronously last**, and takes a *getter* rather than a node. (2) Even then the diagrams vanished a moment after appearing: **React 19 compares `dangerouslySetInnerHTML` by object identity**, so an inline `{ __html }` literal re-sets innerHTML on every re-render — for Edda once per keystroke via the word-count state. Memoizing it is the fix and also stops the preview re-parsing the whole document on every keystroke. Both surfaces were doing this; both now memoize. Also: `hasMermaid` walks marked's lexer (a regex cannot tell a fence from the word "mermaid"), and the corpus test caught it **missing a fence inside a list item** because list children hang off `items`, not `tokens` — a miss that would have shipped a visible placeholder with nothing loaded to fill it. Beyond the plan: the export inlines each diagram **twice** (theme for screen, ink-on-paper for print, toggled by `@media print`), since mermaid bakes colour into the SVG and one copy cannot satisfy both criterion 13 and the export's promise to open in the theme it was written in. **And the plan's PLAN-99 correction was itself wrong** — the Edda coming-soon footer *does* exist (`.edda-soon`), listing exactly these two features plus "Image paste"; this PR removes the two it ships. Gate green (lint/typecheck/**526 server + 790 client**/build). **Live-verified** on the built server (scratch `STORAGE_ROOT`, port 4747) over CDP: **41 checks**, all passing — the three diagram kinds from one source on all four surfaces; a fence-less edda fetching **zero** mermaid chunks (31 JS requests, none of them mermaid) while the diagram document fetches them; a theme switch **recolouring in place** without a reload; typing appending prose with the three SVG ids **unchanged** and no new chunk; a broken diagram as a bordered block naming the parse error with its source below and the prose after it intact, mermaid's own error graphic absent; the hostile diagram inert — no script, no handler attribute, no `img`, no `foreignObject`, the `click "javascript:…"` directive producing an `<a>` with **no href at all**, the label drawn as literal text, and **zero beacon requests**; the file-preview modal rendering a diagram from a real `.md` dropped into `downloads/` **and** gaining hljs + heading ids, while a fence-less file loads no mermaid; a wide diagram at **533px inside a 573px pane** instead of clipping; and 375/768/1280 with no horizontal overflow. **The PDF was generated headlessly and read**: 3 pages, ink on paper whatever the theme, vector diagrams, **no app shell**, the title on the document (so the saved filename is the edda's name), the flowchart whole on page 1 with the sequence diagram starting page 2 (`break-inside: avoid`), and `<!-- pagebreak -->` putting "After the break" on its own page. **Criterion 12 proven the hard way**: `afterprint` suppressed *in the iframe's own realm* via `Page.addScriptToEvaluateOnNewDocument`, two exports, **zero iframes left** after the fallback. Restart smoke: SIGINT → `shutdown complete` → restart → health 200, five documents intact, `integrity_check ok`, WAL. **Chunk accounting, measured against a build of `develop`**: mermaid contributes **0 bytes** to the eager path (it is a dynamic import, and the `markdown` chunk's only `import()` is `./mermaid.core-*.js`); `/edda`'s static closure grows 1004.5 kB → 1015.4 kB (+1.1%), of which the page's own chunk is +3.4 kB for the export/print code the plan adds. Raised as **PR #53** with **ci + docker green**. **Owner validated the flow 2026-08-23** — they rendered a flowchart of their own through the built app — and the PR was taken out of draft. One question came out of that pass and is worth keeping: a `fa:fa-car` FontAwesome token in a node label draws as **literal text**, not an icon, because `htmlLabels: false` means a label is painted as SVG text rather than built as HTML, and because Bifrost fetches no external font CSS. That is the security posture working as designed, not a defect — the same property that makes `<img src=x onerror=…>` in a label inert — so the setting stands and an emoji (`[🚗 Car]`) is the substitute. **Still owner-manual**: a real phone on the LAN (diagram legibility at 375 on a real screen, and the OS print/share sheet), and a real print dialog — headless `print()` returns immediately, so the "timer starts after the dialog closes" behaviour is argued and unit-tested rather than watched.

- 2026-08-10 — **PLAN-19 (Groot — YAML editor, viewer & library) implemented** on `feat/plan-19-groot`, in the recommended order 18 → 21 → 19 → 20. **PLAN-21's bet paid off exactly as written**: the library task collapsed to **one `LIBRARY_REGISTRY` element** — no page, no chip, no SSE, no filter change — which is what the fake-kind test had been pinning all along, and a new `registry.test.ts` now pins the three real entries too (a typo in `apiRoute` is invisible until someone opens a data URL). Shipped in six commits: the **`mode` refactor alone and green first** (five booleans → one union, four call sites, no behaviour change), then deps, `core/yaml/`, the editor mode, the server module, the client feature. **Three findings changed the design.** (1) **The plan's Tab mechanism was wrong**: it said `indentUnit.of('  ')` makes a literal tab untypeable because "CM's `insertTab` inserts the `indentUnit` facet" — it does not; `defaultKeymap` binds Tab to `insertTab`, which inserts a literal `\t` on an empty selection whatever the unit says, and the trailing `indentWithTab` never runs. Since a tab in YAML indentation is a **hard syntax error**, this was the easiest way for the editor to write a file it could not parse. A `Prec.high` Tab binding fixes it, and the test drives the **real keymap** through `runScopeHandlers` rather than the command (which is where the original reasoning went wrong) — it fails without the binding, verified by removing it. (2) **Format was silently editing values**: found by formatting a real manifest and reading the output, `buildId: 9007199254740993` came back as `…992`, because re-emitting goes through the parsed value. The restyle path now parses with `intAsBigInt`; the analysis path deliberately does not, since `toJS` would hand BigInts to `JSON.stringify`. (3) **The Pensieve's kind chips overflowed at 375** — `.lib-chips` is a `nowrap` scroller with a hidden scrollbar, which always fitted with two kinds; the third pushed YAML off the right edge with nothing pointing at it. They wrap now. Also decided: duplicate keys parse with `uniqueKeys: false` and become an **advisory** (an error would refuse the save, contradicting "advisories block nothing"), `<<` merge keys **resolve** so the tree shows what Compose and CI runners see, and two advisories beyond the plan's table (`merge-key`, `parser-warning` — the library exposes `doc.warnings` nowhere else). `TreeView` gained one optional `annotationAt` prop so an alias is badged `*base` in a tree that otherwise shows resolved values; handoffs travel as one-shot session seeds (`core/runestoneSeed` joins `core/variantSeed`). Gate green (lint/typecheck/**526 server + 750 client**/build). **DB verified both ways per the `db-migration` skill**: a pre-0011 database built by running the *previous commit* in a worktree upgraded cleanly — 11 → 12 migrations, the pre-existing edda intact, `groot_docs` created and writable, `integrity_check ok` — and a fresh `STORAGE_ROOT` bootstrapped 12 migrations, also `ok`, WAL. **Live-verified** on the built server (scratch `STORAGE_ROOT`, port 4747) over CDP: **43/43 checks**, including a 31-line two-document manifest that folds (10 arrows, 4 placeholders), formats with **every comment in place** and is **idempotent**, keeps `9007199254740993` intact through Format, refuses a **billion-laughs document in 1.2 s** while the page stays responsive, badges `*defaults` in the tree, switches documents, saves to a slug URL, and serves `/groot/api/<slug>` as `application/yaml; charset=utf-8` with `access-control-allow-origin: *` **byte-identical to the editor buffer**; the Pensieve lists it beside JSON and Markdown, the YAML chip writes `?type=groot`, both legacy URLs land in one hop, and `POST /api/portkey {slug:"groot"}` is **422**. Responsive matrix **13/13**: 375/768/1280 in Aurora and Daybreak with no horizontal overflow, plus the Pensieve at 375. Restart smoke: SIGINT → `shutdown complete` → restart → health 200, documents intact, `integrity_check ok`. **One pre-existing flake, not this plan's**: `accio-kill` failed once in the first full server run and passed in five subsequent runs (one isolated, four full) — it spawns a real server and races on the port, and this plan adds a fifth spawning suite. **Then, on owner feedback about the conversion round trip** ("switching between runestone and groot… upon coming back the content should be present"): the buffer **was** in localStorage all along — both editors have cached drafts since PLAN-07 — it was simply never applied, sitting behind the "Restore the draft from your last visit?" prompt. New `core/draftReturn` splits the two cases the editor could not tell apart on mount: **returning within one page lifetime restores silently** (jumping to the other tool to see the same document in the other format is one task, not two visits), while a **fresh page load still only offers** the draft, because silently refilling an editor opened to start something new would be the opposite mistake. The marker is deliberately **module scope, not storage** — a reload wipes it, a client-side navigation does not, which is exactly the line; `sessionStorage` would survive a reload and make every refresh a silent restore. **A second bug fell out of testing the first**: the draft is debounced 500 ms, so clicking the convert button within half a second of typing lost the last keystrokes outright — leaving now flushes the buffer directly, guarded on `isScratch` read through a ref so a just-saved document (whose draft is cleared on purpose) cannot resurrect it. **Live-verified 19/19** driving real router navigations rather than page loads: typed YAML → clicked Runestone at **380 ms** (past the 300 ms validation debounce, inside the 500 ms draft one) → JSON opened → Back → **the YAML returned byte for byte, comment included, with no prompt**; both directions through the Ollivanders hub restore their own buffers; a **reload** still shows the prompt and the prompt still restores; a **saved** document leaves nothing behind to reappear. Gate re-run green with no regressions (**526 server + 755 client**), and the earlier Groot suites re-ran at 43/43 and 13/13. Applied to the two editors the owner named; Edda and Loki share the prompt pattern and are one line each if wanted. **Owner validated the flow on 2026-08-10**; **PR #52** taken out of draft with ci + docker green. **Still owner-manual**: a real phone on the LAN (the editor, the fold gutter and the tree at phone width), and a genuine two-device SSE round trip.

- 2026-08-08 — **PLAN-21 (Pensieve — the one document library) implemented** on `claude/next-plan-development-cdjjag`, run **before PLAN-19** per the plan's own recommended order (18 → 21 → 19 → 20), so Groot's library page never gets built. This is a **de-duplication, not a new page**: `PensievePage.tsx` and `EddaLibraryPage.tsx` are **deleted** — near-identical twins holding the same six state fields, the same SSE effect and the same four sorts, with Edda's page already reusing Runestone's `rune-lib-*` CSS — and replaced by one `app/pages/PensievePage.tsx` over a new **`client/src/core/library/`**: `LibraryKind`/`LibraryItem`, an **array** registry (deliberately not a `Record<LibraryKind, …>`, so `groot` can be *named* by the type before PLAN-19 ships an entry), and pure `mergeItems`/`sortItems`/`filterItems`. **The return on the plan is that a fourth type is one array element and no page change, so that is the one thing pinned by its own test** — a fake `scroll` kind lists, filters, sorts, deletes and capability-gates through the same code paths. Listing is a client-side **`Promise.allSettled`** fan-out (`all` would turn one flaky module into an empty library, which reads as "my documents are gone"); a server aggregate is illegal under rule 2 and would couple storage PLAN-99 said stays uncoupled. `sortItems` tie-breaks on **id then kind** — ids are unique only *within* a table, so two kinds can hold the same 6-char handle — and every row is keyed `${kind}:${id}`. The badge colour follows the **kind**, a scoped exception to the positional-palette rule on the Accio hostname-tile reasoning (a type must stay recognisable while the list re-sorts under it); hub cards stay positional. `pensieve` joined `RESERVED_ROOTS` + the guard test; all five legacy URLs (`/runestone/pensieve`, `/runestone/library`, `/runestone/mimir`, `/edda/pensieve`, `/edda/library`) point **straight** at the filtered page so none double-redirects. **One deviation, additive and logged**: a Pensieve card on Ollivanders + `/pensieve` in that tab's nav `match` — without them the unified library's only entrances are the buttons inside the two editors it unifies. **One real bug found by testing**: `kinds` was memoised on the `capabilities` *object*, so a fresh-but-equal object re-fetched every kind and re-subscribed to every SSE event on each render; it is now keyed on the module list's contents. Gate green (lint/typecheck/**494 server + 649 client**/build). **Live-verified** on the built server (scratch `STORAGE_ROOT`, port 4747) over CDP: both kinds interleaved under one sort with per-kind badges; the Markdown chip filtered to 2 rows, wrote `?type=edda`, and **Back restored all 4**; search + device + sort composed across types; **all five legacy URLs landed on the right filter in one redirect each**; with `/api/edda` **blocked**, the JSON rows still rendered beside a "Markdown documents couldn't be loaded · Retry" strip and a disabled Markdown chip (**criterion 6 in a screenshot**), and Retry recovered **only** that kind without a reload; a save in another device's session appeared over SSE and a delete removed it from the list *and* from `/api/edda`; **criterion 7 proven on the wire** by rewriting `/api/capabilities` to drop `edda` — **zero `/api/edda` requests**, no chips, JSON rows only; `POST /api/portkey {slug:"pensieve"}` → **422**; 375/768/1280/1600 with **no horizontal overflow** in both Aurora and Daybreak, the badge dropping its word to an icon at 375 (32px vs 96px) with the actions wrapping to their own line; zero console errors. Restart smoke: SIGINT → `shutdown complete` → restart → health 200, documents intact, `integrity_check ok`; a fresh DB bootstrapped 11 migrations, also `ok`. **One investigation worth recording because it was a false alarm**: repeated navigations appeared to wedge the page in "Surfacing memories…", but it reproduced only with a 1400–1600 ms settle — at 2500 ms seven consecutive loads completed fully, and `lsof` showed the cause as **six lingering browser→server connections**, i.e. the per-origin pool exhaustion already documented in PLAN-99's "Shared SSE across browser tabs" row, not a Pensieve defect. Pre-existing and unchanged by this PR. **Owner validated the flow on 2026-08-08** and marked **PR #50** ready for review (ci + docker both green). **Still owner-manual**: a real phone on the LAN (row legibility and the chips at 375 on a real device), and a genuine two-device SSE round trip.

- 2026-08-06 — **PLAN-18b (the remaining nine tools) implemented** on `feat/plan-18b-toolbox-tools`, **stacked on unmerged 18a at the owner's direction** ("implement 18b as well" — the 12b/16b/17b precedent). URL, ASCII/Hex/Binary, JWT, Iris, CIDR, Case, Password, Cron and SHA-256. **No new mechanism**: Part A's shell took them as nine registry entries and nine bodies, and all thirteen still ship in **one** lazy chunk (49 kB, 16 kB gzipped). Four of them needed a decision rather than a transcription. **JWT decodes and never verifies**, and says so in a standing note *above* the input rather than a footnote — a footnote is read after the reader has already formed a judgement from a badge — and the verdict reads **"Unexpired", never "Valid"**, because an unexpired token is still forged if the signature is wrong and this tool has no key to know. **The password generator draws by rejection sampling**, not `byte % alphabet.length`: 24 symbols do not divide 256, so modulo would make the first 16 characters ~1.5× more likely while the panel prints an entropy figure that is only true if the draw is uniform (pinned by a head-vs-tail distribution test). **Base conversion runs on `BigInt`**, because the values people paste into a converter are 64-bit ids and silently rounding their last digits still *looks* like a number. **Iris implements WCAG contrast client-side**, correcting PLAN-99's note that it could reuse `themes/contrast.ts` — that file is in the server workspace *and* inside a feature module, unreachable from the browser twice over — and is tested against that file's own vectors (`#000`/`#fff` → 21:1, `#fff`/`#fff` → 1:1). Cron explains the **dom/dow OR rule out loud** (`0 0 13 * 5` fires on the 13th *and* every Friday) and steps by field rather than by minute, so `0 0 29 2 *` answers in milliseconds instead of walking two million minutes. Gate green (lint/typecheck/**494 server + 614 client**/build). **Live-verified** on the built server (scratch `STORAGE_ROOT`, port 4747; PM2's 4646 untouched). **The two-origin `crypto.subtle` gate — the one thing 18a could not test — is confirmed on two real origins**: `http://localhost:4747` is a secure context and shows **15 cards including SHA-256**, while `http://192.168.1.33:4747` is not, has no `crypto.subtle`, and shows **14 with SHA-256 absent**; deep-linking `/diagon-alley/hash` over LAN http falls back to the hub with the URL replaced, and on localhost the same URL hashes `abc` to the correct `ba7816bf…15ad`. Every other tool driven for real: `héllo 🌉` → `68 c3 a9 6c 6c 6f 20 f0 9f 8c 89` with 11 bytes / 7 characters / 8 UTF-16 units counted separately; `18446744073709551615` converted to `FFFFFFFFFFFFFFFF` with every digit intact; the jwt.io sample token decoded to alg HS256, "John Doe", **Expired**, with `iat`/`exp` in local time; `192.168.1.0/24` → the full row set and `10.0.0.4/31` → 2 usable with the RFC 3021 note; a 32-character password at **206 bits** that differs on regenerate and touches neither `randomUUID` nor `subtle`; `0 0 13 * 5` explained with the OR rule and its next five runs correct (Fri 7th, Thu 13th, then three Fridays); and **Iris extracted `#ff0000 #00ff00 #0000ff` from a synthesised 30/20/10-pixel stripe image in dominance order**. Zero console errors. **Three bugs found by running it, all fixed**: (1) `slugify` worked on the raw string, so `HTTPResponseCode` slugged to `httpresponsecode` while every other form split it correctly — it now runs through the shared word split; (2) that split used an ASCII character class, so `Crème` split on its own accent into `Cr` + `me` — now `\p{L}`/`\p{N}`; (3) the theme export mapped palette colours onto `--ok`/`--danger`/`--warn` by position, which turned a five-blue photo into a navy warning and a sandy danger — the palette now drives only the accents and the ten positional card hues. **The Iris stretch goal was verified against the real server, not just its own tests**: a theme generated in the browser and POSTed to `/api/themes` returned **201 with `warnings: []`** (so it satisfies the schema *and* the WCAG contrast lint), appeared in the theme list, and applied — `data-theme="toolbox-test"`, `--accent` and `--card-1` reading back the extracted colour. Docs synced (architecture module registry + a `toolbox` section, project-structure, 5 decision rows, `plans/README.md` gate ticked, PLAN-99 flipped `PROMOTED` → `DONE`); **plan archived to `completed/`**. One checklist row left unticked on purpose: a real phone on the LAN. **Then, on owner feedback on the finished 15-card grid** ("you have reduced card size and fit in current area, lets utilize full screen here"): the cause was app-wide — **`.shell-main` caps every page at 62rem**, a *reading* measure that is right for prose, an editor or a form and wrong for a grid of small cards, which just gets fewer columns and more wrapped titles. **Diagon Alley opts out** via `.shell-main--wide` and takes the header's gutters so the two line up; every other page keeps the measure (verified: Midgard and Ollivanders still 992px at 1920). Cards got a larger track and more padding, which stopped "ASCII / Hex / Binary" wrapping its own title — **3 columns at 1024 up to 8 at 2560, zero wrapped titles at any width**. That **exposed a bug already present**: `.tool-rows` stretched with its container, so CIDR's Copy buttons sat **~1100px from the values they copy** at 1920. Tables and lists now flow into as many **bounded** columns as fit (`repeat(auto-fit, minmax(21rem, 26rem))`) — bounded because it is *stretching* that separates a value from its button, so extra width has to buy another column instead — keyed on **container queries** rather than the viewport, since the same table appears in a split panel's right column, in a full-width panel and in half the URL parser, three widths at one viewport size. Measured: CIDR goes 1 column at 1280, 2 at 1440, 3 at 1920, 4 at 2560 with the value-to-copy gap a **constant 12px** throughout, and 24 UUIDs land in 3 columns of 8 instead of a 24-row scroll; mobile and ≤1280 unchanged. **Both parts pushed 2026-08-06** as **PR #48** (18a) and **PR #49** (18b), both green (ci + docker). 18a was **squash-merged**, which left 18b's copy of its six commits duplicating develop — resolved by rebasing 18b's four commits onto develop (`--onto`), which applied with **no conflicts**; the reported conflict was entirely the duplication. **Still owner-manual**: that phone — panel readability, the bottom-nav inset, and seeing the SHA-256 card genuinely absent on a device that is not this Mac.
- 2026-08-06 — **PLAN-18a (Toolbox shell + 4 tools) implemented** on `feat/plan-18a-toolbox-shell` (pending owner test; 18b not started). Diagon Alley stops being a row of doors to pages that mostly did not exist — three of its six cards read "coming soon" — and becomes the toolbox: a tool opens as a **full-width panel at the end of its own card's row**, with Nimbus and Portkey still navigating to their pages. New **capability-only `toolbox` module** (the `variant` precedent: no-op `register()`, both profiles), and the **cloud profile got its first capabilities test** — a cloud build that lost the entry would render an empty hub rather than fail, which nothing would have caught. **`.tone-surface`/`.tone-chip` extracted out of `.portal` in its own commit** before any feature code, with hover moving from `a.portal:hover` to an opt-in `.tone-surface--interactive` because a tool card is a `<button>`. **`core/ui/ExpandingGrid`** owns the mechanism (column measurement off the *used* `grid-template-columns` track list, since `auto-fit` makes the authored value meaningless; `panelInsertIndex`; caret; tone inheritance; a11y) and `features/toolbox/` owns the registry and bodies — in `core/` because a hub page may legally import a feature *today*, but the day anything under `features/` wants the pattern it becomes a build failure with no fix but the move. Open state lives in the URL (`/diagon-alley/:toolId`); `/sigil` is now `<Navigate replace>` to `/diagon-alley/qr` and `SigilPage` is deleted, while the **`qr-tool` module is untouched** — Midgard's Join card still reads `GET /api/qr/server-url`. Four tools: **QR** (migrated, same `QrCard`), **Base64** (UTF-8 via `TextEncoder`, standard + URL-safe, padding/whitespace repaired on decode), **UUID** (v4 + v7 from `getRandomValues`, **never `crypto.randomUUID`** — secure-context-only, so undefined on every device but the host Mac at localhost) and **Epoch** (s/ms guessed by magnitude and the guess shown, ISO/UTC/local/relative, live now row). Gate green (lint/typecheck/**494 server + 473 client**/build); restart smoke on the built server: SIGINT → `shutdown complete` → restart → health 200 → `integrity_check ok`, WAL. **Live-verified** on the built server (scratch `STORAGE_ROOT`, port 4747; PM2's 4646 untouched) over CDP. Measured, not eyeballed: the panel **spans the grid at 1, 3, 4 and 5 columns** and lands at the row-end DOM index the pure function predicts; **no card changes column when a panel opens** (identical column map before/after); resizing with a panel open moved it from index 4 (4 cols) to index 6 (3 cols) and back **with `scrollY` unchanged throughout**; the caret matched its card's centre to within 1px at every width; all **three scroll branches** confirmed (already-visible → no scroll; below the fold → bottom lands on the inset; taller than the viewport → the *card's* top lands at header + 12px); a tool switch scrolled **once** and settled; `/diagon-alley/base64` cold-loads open with **one** lazy chunk and the second tool loads **none**; Back/Forward retrace; `/diagon-alley/nonsense` → hub, URL replaced; `/sigil` → the QR tool; keyboard path complete (focus → open → focus in panel → Escape → focus restored, `aria-expanded` tracking); reduced motion computes `animation-name: none`; typing into Base64, closing and reopening restored the input while a reload cleared it; `héllo 🌉` round-tripped; both themes shot; **zero console errors**. **Three problems found by running it, all fixed**: (1) the scroll was planned against the **loading skeleton**, so a tall tool landed 60px too high with its own heading above the fold — a `ResizeObserver` now re-plans once when the panel's real height arrives, then disconnects; (2) the caret read as a **floating diamond** on the dark themes (a rotated square shows its lower edges against a gradient) — now two border-triangles; (3) uneven card heights left short cards beside a two-line hint. **Criterion 10 proven by comparing computed styles and box geometry of all 8 portal cards on Midgard and Ollivanders, before and after the extraction: zero differences** — a screenshot diff would have been the wrong instrument, since `SkyRelics` places its art randomly and two runs of the same build never match. **Still owner-manual**: a real phone on the LAN (panel readability, the bottom-nav inset), and the two-origin `crypto.subtle` gate, which has no user until 18b's SHA-256 tool (the mechanism itself is unit-tested).
- 2026-08-04 — **PLAN-21 (Pensieve — unified library shell) planned**, promoting PLAN-99's Tier B "Unified library shell" row. Asked to suggest a name, the finding was that **inventing one would be the mistake**: the codebase already calls this the Pensieve **twice** (`PensievePage` at `/runestone/pensieve` and `EddaLibraryPage` at `/edda/pensieve`, both titled `<h2>Pensieve</h2>`) and teaches the word on every save button — so the plan promotes `pensieve` from a nested segment to top-level `/pensieve`, which means adding it to `RESERVED_ROOTS` + the guard test. **This is a de-duplication, not a new page**: the two library pages are near-identical twins (same state shape, same SSE refresh, same sorts) and `EddaLibraryPage` already reuses Runestone's `rune-lib-*` CSS, while `RunestoneSummary`/`EddaSummary` and their sort/query types are **field-for-field identical** — Groot would be a third copy of all of it. So the content is a **`core/library/` registry keyed by kind**, making a fourth document type one entry rather than a fourth page (pinned by a fake-kind test). Listing is a **client-side `Promise.allSettled` fan-out** across the existing per-module APIs, never a server aggregate — rule 2 forbids one module reading three modules' tables, and PLAN-99's own note says storage stays uncoupled; `allSettled` because one endpoint being down must degrade to "the other kinds plus a Retry strip", not a blank page. Also decided: no server module (unlike PLAN-18's `toolbox` — this shell derives from three capabilities that already exist), the type badge's colour follows **kind not position** (the Accio hostname-hash exception), rows not cards, and Accio stays out (a link has no slug, size, content or editor route). **The most useful finding is an ordering one: PLAN-21 should run BEFORE PLAN-19** — PLAN-19's task list contains a `GrootLibraryPage`, a third copy of the page this plan deletes, so the recommended execution order is **18 → 21 → 19 → 20** and PLAN-19 was amended with a gate warning plus a conditional library task. PLAN-99 stripped the same day. Nothing implemented. Decisions logged (5 rows), `plans/README.md` gate row added.
- 2026-08-04 — **PLAN-20 (Edda: Mermaid diagrams + PDF export) planned**, promoting two PLAN-99 Tier B rows into one plan at the owner's instruction (single PR, no parts) — they share a dependency chain, since a PDF that omitted the diagrams would be a bug. **Entirely client-side**: no module, route, table, migration or capability change. The central constraint is that **`renderMarkdown` must stay pure and synchronous** — `architecture.md` names it as the zero-drift property behind the live preview, the public page and the HTML export, and mermaid is both async and DOM-bound, so it cannot run inside it; the fence emits a `<pre class="mermaid-src">` placeholder and a **separate async pass** swaps the SVG in. `exportHtmlDocument` does become async, because an export whose diagram is the literal text `graph TD; A-->B` is a broken artifact. **PDF prints a generated standalone document in a hidden `srcdoc` iframe rather than the app page**: the export template already *is* the clean document, so `.html` and `.pdf` become the same artifact; printing the SPA would instead mean a dozen `@media print` overrides hiding header/nav/relics/Nótt/editor/toolbar/rail that rot every time the shell changes — and the iframe removes the "wait for async diagrams before printing" race **structurally**. Print forces an ink-on-paper palette regardless of theme (browsers drop backgrounds, so an Aurora-dark export prints near-white on white). Mermaid is treated as **untrusted input** — any LAN device can save an edda and `/edda/preview/:slug` is public — so `securityLevel: 'strict'`, `htmlLabels: false`, `suppressErrorRendering: true`, and the `secure` key list left at default so an in-document `%%{init: …}%%` cannot raise its own privileges. One item is flagged as a **spike to settle before anything is built on it**: mermaid emits a `<style>` element inside its SVG and DOMPurify's defaults strip `<style>`, which would leave diagrams invisible. Also specced: source-keyed diagram caching (the preview re-renders on a 200 ms rAF debounce and dagre layout is expensive) invalidated on theme change. **Owner then pulled the file-preview viewer into scope**, which reversed a first-draft decision and moved the mermaid pass from `features/edda/` to **`core/markdown/mermaid.ts`** — in the feature, `previews` could never reach it (features may not import features), so adding it later would be a move plus a re-test; in core it is ~10 lines, and `MarkdownViewer` switches off bare `marked.parse` onto `renderMarkdown`, closing a real drift (it gains hljs highlighting, heading ids, GFM). Two PLAN-99 notes corrected — both rows claim the features are "listed in Edda's coming-soon footer" and **no such footer exists**. **PLAN-99 stripped the same day** at the owner's instruction: the rows for Utility toolbox page, Iris (→ PLAN-18) and both Edda rows (→ PLAN-20) are gone, replaced by `PROMOTED … to PLAN-XX` notes — a new standing rule that a backlog row goes when its idea is *scheduled*, not when the plan completes, which supersedes the "strip it in the plan's own PR" instruction previously written into PLAN-18. Nothing implemented. Decisions logged (10 rows incl. one supersession), `plans/README.md` gate row added.
- 2026-08-02 — **PLAN-19 (Groot — YAML editor, viewer & library) planned.** The third structured-text workspace on Ollivanders beside Runestone and Edda: editor + folding + comment-preserving format + lint + tree view + YAML⇄JSON + a Pensieve library + a public raw endpoint at `/groot/api/:slug`. **Naming took three rounds**: `Yggdrasil` proposed first (the world tree) and rejected by the owner as hard to pronounce; a second round (Cairn, Trellis, Dryad, Saga, Norn) and a third spanning the app's other worlds (Nestor, Atlas, Kodama, Mandrake, Sprout, Groot, Mallorn) followed, and the owner picked **Groot** — a literal walking tree in one unmissable syllable, whose "I am Groot" joke (three fixed words meaning something different every time depending on context) happens to describe YAML's defining hazard exactly, which is why this plan has an advisory rail. The file was renamed `PLAN-19-groot.md` and every identifier re-derived (`groot` module, `groot_docs`, `/groot/api/:slug`, `GROOT_MAX_DOC_KB`); the pure utilities stay format-named at `core/yaml/`. Four findings shaped the spec: (1) **`@codemirror/lang-yaml` was unpacked and read rather than assumed** — its `foldNodeProp` already covers `Item`/`Pair`/`BlockLiteral` plus `FlowMapping`/`FlowSequence`, so block YAML folds out of the box and **no custom `foldService` is needed** (unlike Loki, where lang-javascript had a real `ObjectPattern` gap); the work is enabling `foldGutter`/`foldKeymap` for the mode. (2) **The server never parses YAML** — alias expansion is a billion-laughs amplifier, so the byte cap does not bound the parse; it stores text and caps bytes, the Edda precedent rather than Runestone's. (3) **`indentUnit.of('  ')` is load-bearing** — `indentWithTab` is bound globally and a tab in YAML indentation is a hard syntax error, so without the pin the editor's own Tab key is the easiest way to produce an unparseable file. (4) **`js-yaml` rejected for `yaml` v2** because a naive `parse`+`stringify` format destroys every comment, which for a config file is silent data loss. YAML's ambiguities (the Norway problem, duplicate keys, version-like numbers, unsafe integers) ship as **non-blocking advisories that change no bytes**. Also specced: `JsonEditor`'s three mode booleans collapse into one `mode` prop **in its own commit before any YAML code** (git rules forbid mixing refactor and feature). Table is `yggdrasil_docs` on the `accio_links` precedent. Nothing implemented. Decisions logged (6 rows), `plans/README.md` gate row added.
- 2026-08-02 — **PLAN-18 (Toolbox) planned**, promoted from PLAN-99's "Utility toolbox page" + "Iris" rows at the owner's request, from an owner design sketch. Diagon Alley becomes the toolbox: pure-client tools open **in place** as a full-width panel below the source card's row; Nimbus and Portkey keep their pages. Design discussion resolved four things before writing: **sideways expansion rejected** for one direction plus a per-tool inner layout (`split`/`full`) — in the last column there is no room to the right, so grid would wrap the card and it would jump on click; **its own grid + `.tool-card`** rather than `.portals`, with the tone-lit surface extracted from `.portal` into a shared `.tone-surface` so the card palette can't drift; **deep-linkable `/diagon-alley/:toolId`** (nested under an already-reserved root, so no `RESERVED_ROOTS` change) with `/sigil` redirecting and `SigilPage` deleted — the `qr-tool` module stays, Midgard's Join card still needs `/api/qr/server-url`; and a **`supported()` hook on the tool model** because `crypto.subtle`/`crypto.randomUUID` are secure-context-only and Bifrost is plain http on the LAN, so they are undefined on every device except the host Mac at `localhost`. UUID is built from `getRandomValues` and works everywhere; only SHA-256 is gated. Two other PLAN-99 notes corrected: Iris **cannot** reuse `themes/contrast.ts` (wrong workspace, and inside a server module), and the tools ship as **one** lazy chunk rather than one per tool. Thirteen tools registered; no lore names invented (owner's call). Nothing implemented yet. Decisions logged (6 rows), `plans/README.md` gate row added.
- 2026-07-30 — **PLAN-17b (Send/Receive flow) implemented** on `feat/plan-17b-send-receive`, stacked on 17a after the owner lifted the hard gate ("implement 17b as well"). **Security model: LAN trust** — the plan's per-upload capability token was deliberately not built (the owner didn't pick, and LAN trust is the plan's own recommendation and what the rest of the app assumes); this is stated in the PR body because it means anyone on the LAN can publish any staged file. **`uploads/` stops being write-only** — a deliberate reversal of two logged decisions (`decisions.md:14` "no read route", `:17` "metadata only"), logged as such, with the Send page's "write-only vault — nothing here can be read back" copy removed because it had become untrue. New routes: `POST /api/files/:name/publish` (200 `{finalName,renamed}` · **404** gone · **409** in flight), `PATCH /api/files/:name`, `DELETE`, `GET …/content` (ranges), plus `GET …/preview` served by **`previews`** — metadata belongs to the module that owns "what a file is", bytes to the one that owns the storage, mirroring the downloads split. **Names lost their timestamp prefix**: `upload-files.ts` passes the sanitized name straight through, so `report.pdf` lands as `report.pdf` and only a real collision produces `report-1.pdf`. The collision loop was **unbounded** (its own comment excused that with the prefix this plan deletes) — now one shared **`placeFile`** helper capped at 50 probes then a random `-a7f3` suffix. It also **replaced `open(wx)` + `rename()` with `link()` + `unlink()`**: `link` is an atomic no-overwrite reservation whose target already holds the complete bytes, so the old sequence's crash window can no longer strand a **zero-byte file** under the final name (criterion 24); `EXDEV` falls back to a fsynced copy through a dot-hidden staging file **inside the destination** (or the final link would be cross-device too). The one window left — crash between link and unlink, file in both folders — is closed by a **boot sweep** keyed on `dev:ino`, deliberately not on the name. `file.published` → SSE owns the **banner only** (`download.added` remains the sole source of the listing, or every file announces itself twice); it is **not audited**, because `download.added` already records the same file. **`.svg` now serves as `text/plain` from `core/http/mime.ts`** — an inline SVG runs same-origin scripts exactly like the HTML that file already refused, and uploads/ is writable by anyone on the LAN (closes the downloads hole at the same time). **`upload_audit` dropped** (migration `0010`) along with its repository, port, recorder and usecase: it duplicated `audit_events`, its one unique column converged with `original_name` when the prefix went, and it had drifted — the owner's Heimdall listed screenshots deleted months earlier. Heimdall → Uploads now **reads the directory** (cannot drift) and the dashboard's `uploads.total`/`today`/sparkline read `audit_events`; **dot-file filtering moved into `core/disk-usage`** so the listing and the storage totals apply the same rule (`.DS_Store` was 6 KB of "uploads"). Client: card actions **Move · Preview · Rename · Delete**, all hidden while a move is in flight; a rename that the sanitizer would change comes back **422 with `details.suggestion`** and one click uses it (no second copy of the sanitizer in the browser); delete confirms inline naming the file; the exit is **one CSS animation** (hold, then swipe) removed on `animationend` — no `setTimeout` in the path — with reduced motion swapping in a *still* animation of the same length that deliberately overrides base.css's global `animation-duration: 0.01ms !important` (the reset exists to stop motion, and this animation moves nothing). Preview opens as a **route** (`/upload/:name/preview`) so no feature imports another. Gate green (lint/typecheck/**493 server + 382 client**/build). **Live-verified** end to end on the built server with **two separate Chrome profiles** (two deviceIds) against a scratch `STORAGE_ROOT` on port 4747; PM2's 4646 untouched (21m uptime, still online). Confirmed: four actions on a staged card; preview renders the file's own bytes at `/upload/report.pdf/preview`; `../../etc/passwd` refused with "would be saved as etc_passwd" + a one-click offer; move → confirmation → card gone; the **sender saw no banner while the other device did**; a 3-file bulk move collapsed to **one** banner reading "3 files are ready in Receive ×3"; delete asked first, "Keep" cancelled, "Delete" removed it; publishing onto a taken name produced `birthday-plan-1.txt` and the card said "saved as …"; zero runtime exceptions on either device. **DB verified both ways per the `db-migration` skill**: a **copy of the owner's real `app.db`** (22 `upload_audit` rows) upgraded cleanly — table gone, health 200, `integrity_check ok`, restart clean — and the dashboard then read **17 uploads, exactly the `audit_events` `file.uploaded` count**, while the listing showed the **12 files actually on disk** (the 10 ghost rows gone); a fresh DB bootstrapped 11 migrations and never created the table. **Three bugs found by running it, all fixed**: (1) the card kept showing the *browser's* filename after a rename, so it named a file the host no longer had; (2) at 390px the four actions squeezed the filename to nothing and the Move button overlapped the size — the row now wraps with the actions on their own line (re-measured: name 228px, actions below, no overlap, no horizontal overflow); (3) a **frozen** tab (Chrome memory saver) never resumes its paused CSS animation, so `animationend` never arrives and the confirmation card sat there indefinitely — returning to the page now sweeps any confirmation that has outlived its animation, verified by freezing the page for 6s and restoring it. **Still owner-manual**: a real phone on the LAN (the banner was proven between two desktop profiles), and a genuinely backgrounded-then-restored real tab.

- 2026-07-30 — **PLAN-17a (`core/notify`) implemented** on `feat/plan-17a-notifications` (pending owner test — this is the plan's ⛔ hard gate: the UI and flow must be approved, and LAN-trust vs capability-token picked, before any of 17b). Bifrost had **five hand-rolled `notice` states and no notification system**: `core/ui/Toast` is a presentational `<div role="status">` with no store, queue, timer, or life beyond the page that renders it. New **`client/src/core/notify/`** — a store (`store.ts`), a `<NotificationHost/>` mounted **once in `App.tsx` outside `<Routes>`** and portalled to `<body>` (the floating-Toast lesson: no ancestor stacking/overflow context can trap it), and an **imperative `notify.info|ok|error(message, opts)` handle usable from non-React code** — because the callers are promise rejections, event handlers and (in 17b) the SSE layer, none of which can reach a context. `Toast` stays: it is the *in-flow* thing, and forcing the five call sites to migrate would be churn. **The cap is per kind, by arithmetic rather than a special case**: `MAX_VISIBLE = 4` and `MAX_ERRORS = 3`, so any four newest entries hold at most three errors and a transient one always has a slot — errors don't auto-dismiss, so four of them would otherwise wedge the stack permanently and swallow every later success (criterion 21). A 4th error evicts the oldest error; "Dismiss all errors (N)" appears once more than one is stacked. **Hover/focus pauses the countdown** (the store keeps the remaining ms and re-arms on resume; the bar is a CSS animation keyed on an `epoch` so a collapsed repeat *restarts* it), `dedupeKey` collapses repeats into one entry with a `×N` counter, `prefers-reduced-motion` drops both the slide and the drain (verified: both computed `animation-name: none`), errors are `role="alert"`/`aria-live="assertive"` and everything else `status`/`polite`, and the cross is a real labelled button. Added **`dismissKey(key)`** after live-verify exposed a real bug: the recovery path captured the notification id in a closure, so navigating away and back left a stale "could not refresh" error nobody could clear — the recovering page is rarely the one that failed, so clearing is by key and its boolean answer is what decides whether to say "back in sync". Also shipped **`shouldShowForOrigin`** (pure, unit-tested) for 17b's self-filter: suppress **only when both device ids are non-null and equal**, since `null === null` would otherwise silently swallow the banner on every client without a deviceId (criterion 22). **Real adoption, not a demo**: upload failures (both the pre-flight size/extension rejections and the network path) now raise `notify.error` deduped per file — the in-flow `Toast` that only ever showed the *first* error is gone, the row keeps its Retry — and a failing downloads listing raises one standing error that clears itself, with a transient "Downloads list is back in sync", when the listing recovers. Gate green (lint/typecheck/**460 server + 366 client** (+23 notify)/build). **Live-verified** on the built server (scratch `STORAGE_ROOT`, port 4747; PM2's 4646 untouched — 32m uptime, ↺20 throughout) driving headless Chrome over CDP: four blocked-extension drops left **exactly three error cards** + "Dismiss all errors (3)"; a client-side route change to Midgard kept all three (criterion 7); `elementFromPoint` under the host returned the **portal card beneath** (host is `pointer-events: none`, z-index 80 — above the modal scrim 50, Loki's sheet 60, the floating toast 70); the cross dropped one, "dismiss all" cleared the rest; **blocking `/api/downloads`** produced the listing error, unblocking + returning cleared it and showed the green success **with a draining bar**, hovering it froze the bar at ~75% and it survived **9 s** paused, three upload errors then stacked **behind** it — 4 cards, success still visible (criterion 21 in a screenshot) — and unhovering let it drain away leaving the three errors. Mobile 390×844: full-width cards under the header, **no horizontal overflow**, bottom nav clear. Both themes shot (aurora dark + daybreak light); zero console errors; clean SIGINT (`shutdown complete`). One styling fix came out of reading the shots: the drain bar at full accent strength read as a loading indicator in dark, so it is `color-mix(…60%)`, and a message with no title above it now takes full text contrast. **Still manual**: a real phone on the LAN, and VoiceOver/screen-reader announcement of the live regions.

- 2026-07-29 — **PLAN-16b (Observability — metrics, Prometheus, Tempo) implemented** on `feat/plan-16b-metrics`, **stacked on the unmerged 16a** (owner's call; the plan's own gate says 16b starts after 16a merges — the PLAN-12b precedent). **The snapshot is the record**: a new both-profiles `metrics` module writes one `module:"metrics"` line per `METRICS_SNAPSHOT_INTERVAL_SEC` (cpuPct · rss/heap · loop-lag p50/p99 reset each interval · uploadsDelta · sseClients · diskMb · uptime) into the same pino stream, so the runtime history inherits the log files' backfill and exists with **zero Docker dependency** — Prometheus *scrapes* (down = the data never existed) and Tempo is *pushed to* (down = buffer, retry, drop), so neither can answer "why did it hang at 3am last Tuesday?". Counts are **deltas, never cumulative** (a property test pins that summing them equals the event count); `diskMb` rides its **own slow timer** because the walk is synchronous — sampling it per snapshot would block the loop per snapshot and record the spike it caused; the module logs through a **`trace`-pinned child** so raising `LOG_LEVEL` can't kill the record (`METRICS_ENABLED` is the one off-switch). The recursive walk moved to **`core/disk-usage.ts`** with Heimdall repointed at it (modules can't import each other) and a Heimdall-side test pinning its numbers didn't move. **Prometheus**: `prom-client` + unauthenticated `GET /metrics` (Prometheus has no session; `metrics` added to `reserved-roots`) + a `bifrost_http_request_duration_seconds{route,method,status}` histogram. That histogram needed a real design fix — a Fastify hook only sees the encapsulation context it was added in and every module has its own, so `core/http` now emits **`http.requestCompleted` on the bus** and metrics subscribes (`fastify-plugin` was installed for the hook approach, proved insufficient, and removed). **Tempo/OTel**: `server/src/otel.ts` loaded via **`node --import`** (npm start · PM2 `node_args` · Dockerfile) because ESM hoisting means an SDK started from app code instruments nothing *silently*; `OTEL_ENABLED=false` by default; a startup line naming endpoint + instrumentation count (its **absence** is the `--import` trap's only signal); **rate-limited** exporter-failure reporting (pure, unit-tested); `trace_id`/`span_id` stamped on every line via a pino mixin, with Loki→Tempo and Tempo→Loki correlation provisioned. Stack gained **Prometheus + Tempo** services, datasources, two **Loki-based alert rules** (a rule on a scraped series would evaluate over holes), a **Runtime row** reading the archive and a **Prometheus row** labelled "live only". Gate green (lint/typecheck/**460 server + 343 client**/build). **Live-verified with the stack actually running**: datasources + alert rules + dashboard provisioned; Alloy's labels confirmed on the owner's **real pre-16a archive** — `logLevel` filled from the numeric key (criterion 11: one `logLevel="warn"` query returned lines from an old file *and* a new-format one), `source` defaulted to `server`, and **only `app.N.log` ingested** (the `current.log` symlink no longer double-counts); the Runtime row's LogQL returned `7.8` from a real snapshot line; `/metrics` validated by **Prometheus' own `promtool`**; `--import` trap confirmed (line present only with the flag); with **Tempo down** a 21-second run produced **exactly one** exporter-failure line and the app served normally; SIGTERM mid-interval exited 0 with `integrity_check ok`. **Three real bugs found by running it**: (1) the histogram saw only its own module's routes → the bus event; (2) `promtool` flagged duplicated `process_resident_memory_bytes`/`heap` gauges and abbreviated units → dropped the duplicates, renamed to base units (`_seconds`, `_bytes`); (3) the alert folder named "Bifrost" **crash-looped Grafana** (a folder may not share a dashboard's name) → "Bifrost alerts". **Still owner-manual**: opening the panels in a browser, and the Tempo span round-trip. Plan archived to `completed/`; 3 checklist rows left unticked on purpose with the reason inline.

- 2026-07-29 — **PLAN-15 archived and PLAN-16a (Observability — logging) implemented** on `feat/plan-16a-logging` (**PR #42**, pending owner test). PLAN-15 flipped to `done` (PR #38 merged 2026-07-27) and moved to `completed/`; plans/README gate row ticked. **Step 0 — the Heimdall Logs section is gone entirely**: `LogsSection` + its registry entry + `.log-*` CSS, the client `fetchLogs`/`setLogLevel`/`LOGS_STREAM_URL`/`LogEntry` API surface, all three `/api/heimdall/logs*` routes, `sessionStreamLive`, and **`core/logtap.ts`** deleted; `routes/observability.ts` → **`routes/about.ts`** (deps seven fields → `config`). **`logTap` and `setLogLevel` both leave `ModuleDeps`**, taking the `moduleLoggers[]` array and the persisted-level ordering constraint with them; the settings `log.level` key is dropped from the overlay and **deleted by migration `0009`** (data-only, custom drizzle-kit migration), with a test pinning that a legacy row is *ignored* rather than obeyed. `LOG_LEVEL` default **`info` → `trace`** + new **`LOG_RETENTION_FILES`** (30) → pino-roll's file limit with **`removeOtherLogFiles: true`** (without it pino-roll counts only the current process's files, and Bifrost restarts constantly, so nothing would ever be swept). Level now written as the text key **`logLevel`**; Alloy fills it from the old numeric `level` for pre-change files, promotes `logLevel`/`module`/`source`, and its glob narrows to **`app*.log`** — a **pre-existing bug**: `current.log` is a symlink onto the active file, so `*.log` ingested every live line twice and doubled every count on the dashboard. Dashboard migrated to `logLevel=~"warn|error|fatal"`, gained **Source/Module/Level** variables and an **Errors by source** panel; the logs panel is now a filterable explorer. **Step 1 — the failure paths**: `uncaughtException`/`unhandledRejection` → `fatal` + flush + exit(1); a broken `.env` writes a `fatal` line to the archive (not just stderr) via a new `logsDirFromEnv`; shutdown records its **reason**; `AppError`s are logged **once, centrally** at `debug` in `setErrorHandler` (Fastify's request line carries the 4xx but never the why); real `warn`s added to `upload-audit-recorder`, `fs-stats-reader`, `get-download-stream`, `db-accio-repository`; the deliberate swallows (`build-info`, `core/copy`, `core/theme`, `core/deviceId`) keep silence **with a comment saying why**. **The client has a voice for the first time**: new `client-logs` module (both profiles) with a batched, rate-limited, size-capped `POST /api/client-logs` + public `GET /api/client-logs/config`; client `core/log.ts` (warn+, debounced batches, drops on failure, carries route/deviceId/UA), an `ErrorBoundary` around `<App />`, `window.onerror`, `unhandledrejection`, and `build.sourcemap: true`. `source` is a **child** binding on both sides via sibling `moduleLogger`/`clientLogger` off a source-free root (pino *appends* to `base`, so the obvious spelling emits the key twice — pinned by a test, along with the trap itself). **Two real bugs found by live verification, both fixed**: (1) dropping pino's numeric `level` made the **multi-target transport worker drop every line** — file *and* dev stdout went silently empty, because that worker re-parses each line and routes on the number; the formatter now emits both keys (Alloy promotes only `logLevel`), and a new `transport.int.test.ts` writes through the real transport and fails if the sink ever goes quiet again. (2) `logger.flush(cb)` **never calls back while the transport worker is still starting**, so the new fatal paths awaited forever and the process exited **0 instead of 1**; flushing is now bounded (`FLUSH_TIMEOUT_MS`, deliberately not `unref`'d — an unref'd timer lets an idle loop exit before it fires). Gate green (lint/typecheck/**427 server + 346 client**/build). **Live-verified** on the built server (scratch `STORAGE_ROOT`, port 4747; PM2's 4646 untouched throughout): stale `log.level` row deleted on upgrade + health 200 + `integrity_check ok` on both the upgraded and a **fresh** DB (10 migrations); all three `/api/heimdall/logs*` → **404**; every line carries text `logLevel` and **exactly one `source`**; a browser batch landed as `source:"client"` with deviceId/route/UA/ip; 413 over the body cap; rotation forced with `retainFiles: 3` left exactly 3 rotated + 1 active; `pino-pretty --levelKey logLevel` renders INFO/ERROR with colour; SIGINT logged `reason: "signal SIGINT"` → restart → health 200. In **headless Chrome (CDP)** at 1280×900 and 390×844: a plain page load sends **nothing**, a `window.onerror` and an `unhandledrejection` both reached disk as `source=client`, and blocking a route's code-split chunk produced a real React render error → the **ErrorBoundary fallback rendered instead of a white screen** (screenshotted, both viewports) with **exactly one** report posted; restarting with `CLIENT_LOG_LEVEL=debug` changed the served floor **without a rebuild**. **Still manual**: anything needing Docker — the Loki/Grafana panels, the old-vs-new `logLevel` query across both file generations, and a real phone on the LAN. Note: criterion 10's "no `level` key at all" is **not** met, deliberately — see the decision row.

- 2026-07-28 — **Tree view: click a value to copy it** (owner-requested, uncommitted on `develop`). `TreeView` only made the *key* clickable (copies the JSON path); the primitive value beside it was an inert `<span>`. It is now a `<button>` carrying the same classes, with a new `onCopyValue` prop threaded down alongside `onCopyPath`. Copy takes the **raw value, not the rendered label** — strings go to the clipboard unquoted and untruncated (`primitiveLabel` clips at 160 chars for display only), numbers/booleans/null via `String()`. Container rows are unchanged: their key still copies the path, and there is no "copy this subtree" action. Both consumers (Runestone, Variant) gained a `copyValue` toast that trims to 60 chars for the message only. CSS reuses the `.rune-tree__key` button reset (transparent, no border, mono, hover underline) so the row looks identical. Owner then flagged the **copy confirmation being unreachable**: `Toast` rendered *in flow* at the page bottom, so after clicking a node mid-tree the "Path copied" line sat below the fold. Toast gained a **`floating`** mode — portalled to `<body>` (so no ancestor stacking/overflow context can trap it, incl. the `isolation: isolate` added to `.json-editor` on 2026-07-27), `position: fixed` bottom-centre at z-70 (clears the modal scrim's 50 and Loki's sheet 60), `pointer-events: none` so it never eats a click, and a `toast-rise` fade honouring `prefers-reduced-motion`. Applied to the three **transient** notices (Runestone, Variant, Loki); the in-flow banners (Runestone's restore prompt/parse errors, Upload's error) deliberately stay in flow. Live-verify caught a second bug the static reasoning missed: at 390px the floating toast **overlapped the mobile bottom nav** (toast 752–820 vs nav 783–844). Added **`--bottomnav-h`** beside `--header-h` (0 by default, 3.8125rem inside the existing 640px query) and the host offsets by it — re-measured **overlapsToast: false**, 16px gap. Gate green (lint/typecheck/**414 server + 329 client**/build); no server or storage change, so no restart smoke. **Live-verified** (CDP, built server on scratch port 4747 + scratch `STORAGE_ROOT`, PM2 4646 untouched) at 1280×900 and 390×844: toast is `position: fixed`, `parentElement === document.body`, z-70, fully in viewport with the page scrolled to the bottom, and `elementFromPoint` at its centre returns the element *underneath* (`.rune-tree__row`) — clicks pass through; auto-dismiss at 2.4s intact; zero runtime errors. With `clipboard.writeText` stubbed (headless denies the real API), the clipboard payload was captured per kind: a 400-char string copied **in full** despite the 160-char tree preview and 60-char toast trim, `"he said \"hi\" & left"` copied unquoted and unescaped, and `42` / `false` / `null` as bare text; key clicks still copy the path. Owner has **not** hand-tested yet — real-device mobile (safe-area inset under the nav) still manual.

- 2026-07-27 — **Loki code folding + an editor stacking fix** on `feat/loki-code-folding` (owner-requested). Folding was JSON-only: the JS mode omitted `foldGutter`/`foldKeymap`, so the workbench collapsed nothing. The gutter now rides **lang-javascript's own `foldNodeProp`** (Block / ClassBody / SwitchBody / ObjectExpression / ArrayExpression / BlockComment), which covers function bodies, objects, arrays, nested blocks and both IIFE forms with no custom range logic. Owner then hit the real gap: lang-javascript folds `ObjectExpression` (a value) but **not `ObjectPattern`** (a destructuring target), so `function e({ …9 lines… }) {` had no arrow and stayed expanded while its body folded. Added a **`foldService`** for `ObjectPattern`/`ArrayPattern`/`ParamList` — consulted *before* the grammar, returns `null` to fall through, so no existing fold changed; innermost match wins, so a destructured param folds as `({…})` keeping the braces as a cue. **`ArgList` deliberately excluded** (it would have hijacked the `app.get('/x', function () {` callback-body fold — regression test pins this). JS extension list extracted/exported so tests assert the real config. Fold all / Unfold added to Loki's Buffer rail (desktop-only, matching the existing Clear; mobile keeps the gutter). Separately, owner reported the **theme picker being clipped by the editor's find panel**: CM's base theme stacks `.cm-gutters` 200 / `.cm-panels` 300, which leaked into the page context and beat the sticky header (z-10) — `.theme-menu`'s own z-60 could never escape its parent context. Fixed with **`isolation: isolate` on the shared `.json-editor`**, chosen over raising the header because it covers the whole class (Heimdall modal, `.variant-rail__popover`, `.tag-suggest`, Loki mobile sheet) and Runestone/Variant/Edda inherit it. Gate green (lint/typecheck/**379 server + 321 client**/build). **Live-verified** (CDP, built server on scratch port 4747 + scratch `STORAGE_ROOT`, PM2 4646 untouched): with the owner's snippet loaded, `elementFromPoint` at the menu/panel overlap returns `.theme-menu`, and reverting **only** `isolation` at runtime returns `.cm-panels` and reproduces the report exactly (Slytherin clipped, Olympus half-covered); mobile 390×844 shows no overlap at all; fold arrows visible on both the `function e({` and `}) {` lines; clean SIGINT (`shutdown complete`, DB checkpointed). Owner has **not** hand-tested yet — real-device mobile check still manual.

- 2026-07-27 — **`npm run logs` fixed** on `fix/npm-logs-path` (owner-requested, found while tracing how logs reach Loki). The script tailed `storage/logs/app.log`, which **pino-roll never creates** — it writes `app.N.log` and symlinks `current.log` at the active file, so the designated no-Docker log fallback had been silently failing. Now `tail -F storage/logs/current.log`: **`-F` not `-f`**, because the symlink is repointed on every rotation (daily, or at 20 MB) and `-f` would keep following the stale inode. Three docs quoted the same wrong path (`observability.md`, `pm2.md`, `launchd.md`). Verified by rendering real lines through `pino-pretty`. Gate green (lint/typecheck/**379 server + 301 client**). This closes the pre-existing bug already logged as PLAN-16 Step 1 item and acceptance criterion 12 — **the plan file is left unticked** (not this PR's scope; 16a should claim it).

- 2026-07-26 — **PLAN-15 (Portkey) implemented** on `claude/plan-15-tp9uel` (uncommitted at write time, pending owner test). LAN go-links: new **local-profile-only** `portkey` module + `portkeys` table (migration `0008`, additive; **`slug` IS the primary key** — a user-chosen memorable word, immutable, rename = delete+recreate). API: `GET/POST /api/portkey`, `PATCH`/`DELETE /api/portkey/:slug`, and the redirect **`GET /go/:slug` registered outside `/api/`** so `bifrost.local/go/router` is the whole address (a real route → wins over the SPA fallback). **Always 302, never 301** (+ `cache-control: no-store`) — a router IP/NAS moves and a 301 pins it in caches. The **hit count is bumped out of band** (scheduled after the redirect is flushed via `setImmediate`, guarded against shutdown, failure is a warn) so a slow write never delays the hop, then a dedicated **`portkey.hit` → SSE** (deliberately **not** audited, unlike `portkey.saved`/`deleted`) updates open management pages live. Unknown slug → **creative-404 bounce** to `/portkey?go=<slug>` so the "enchant it now" form arrives pre-filled (the Runestone/Edda move). New **core `reserved-roots.ts`** (single source of the app's path roots + `api`/`go`, guard-tested) — a slug matching one 422s; slugs are `[a-z0-9-]{1,32}` lowercase kebab (no leading/trailing dash); targets are **http(s)-only, any host** (`javascript:`/`file:`/`data:` → 422). Client: feature-local `features/portkey/` (api · slug mirror · `usePortkeys` live SSE list · `PortkeyPage` — `/go/`-prefixed slug input with live validation, target+note, search, card grid with per-link **QrCard**, inline edit, confirm-delete, hits/last-used, 409 "already enchanted → View it") + its own `portkey.css` (nimbus precedent); capability-gated **card on Diagon Alley** + NAV match; `/go` added to the Vite dev proxy. Small additive core change: `ApiError` now carries the server's `{error,message}` so the 422 shows the specific reason. Gate green: lint/typecheck/**414 server** (+51: slug/target corpus, reserved-roots guard, usecases incl. resolve-vs-record-hit ordering + hit event, integration 302/no-store/creative-404/route-precedence/409-422 matrix, **kill test** proving no torn rows) **+ 306 client** (+8 slug helpers)/build. **Live-verified** on the built server: create 201 → `/go/router` **302 to `https://192.168.1.1/` with `no-store`** → hit count reached 1 async with `last_used_at` set; unknown slug 302→`/portkey?go=…`; duplicate 409; reserved `go`/`api` + `javascript:` 422; SIGINT → restart → portkey + hits **survived**, redirect still 302; fresh-DB bootstrap `integrity_check ok`, `portkeys` table has all 7 columns; `/portkey` serves the SPA and the `PortkeyPage` chunk code-split cleanly. (Browser-driven UI check skipped — Playwright isn't a project dep; the page mirrors the merged Accio grid and passed typecheck/build.) Docs synced (architecture registry + data flow, project-structure, decision row).
- 2026-07-25 — **npm audit swept 17 → 6 advisories** on `fix/npm-audit-advisories` (owner-reported `npm install` warning). Fixed forward inside existing carets (lockfile only): **`@fastify/static` 10.1.0→10.1.2** — the one advisory on a real runtime surface (authorization bypass via non-canonical URL paths + route-guard bypass via path traversal; this is what serves the SPA) — plus `find-my-way` 9.7.0 (HTTP/2 DoS, unreachable: HTTP/2 is never enabled), `fast-uri` 3.1.4, `postcss` 8.5.23, `concurrently` 10.0.4 / `shell-quote` 1.9.0. The **eslint cluster's five highs turned out to be one root advisory** (`brace-expansion` DoS via eslint 9's `minimatch@3`), closed with a root `overrides: {"brace-expansion": "^5.0.8"}` instead of the `eslint@10` major npm proposes — keeping the 2026-07-12 pin (typescript-eslint 8 targets ESLint 9). **Remaining 6 are accepted and logged**: `react-router` RSC-mode CSRF (unscored, and the app uses only `BrowserRouter`/`Routes`/hooks — no data router, loaders, actions or RSC; no patched 7.x exists, so npm's fix is a *downgrade* and the forward path is the v8 major) and the 4 moderate drizzle-kit→esbuild-kit rows accepted back on 2026-07-12 (0.31.10 is already latest). Gate green (lint/typecheck/**379 server + 301 client**/build). **Live-verified** the static-serving bump on the built server (port 4747): index/asset/`manifest.webmanifest` (`application/manifest+json`)/favicon all 200 with correct types, SPA fallback intact, and **10 traversal probes** — `/../.env`, `/%2e%2e/.env`, `/..%5c.env`, `/..\.env`, `/assets/../../.env`, `/%252e%252e/.env`, `/storage/data/app.db` — every one 403 or the 2458-byte index.html, **zero secret bytes** in any response; the download endpoint's own guard still 403/400s its traversal ids; all routes healthy after the router bump; clean SIGINT, `integrity_check ok`, PM2 untouched.
- 2026-07-25 — **PLAN-13 (Accio) closed out and PLAN-14 (Nimbus) implemented** on `feat/plan-14-nimbus` (uncommitted, pending owner test). PLAN-13 flipped to `done` (PR #35 merged 2026-07-25) and the plans/README gate row ticked. **Nimbus** = the LAN speed test: new local-profile `nimbus` module + `nimbus_results` table (migration `0007`, additive — one row per *complete* test, device-grouped, pruned on the audit retention policy). The measurement is split on purpose — **the server moves bytes, the client owns the clock**. `GET /ping` is a bodyless `no-store` 204 (client takes the **median of ten**; a mean would report a round trip that never happened); `GET /down?mb=` streams a window onto one 4 MiB random pool generated at boot, one chunk per `_read` so socket backpressure paces it, with `content-length` + **`content-encoding: identity`** + `no-store`; `POST /up` takes the **raw stream in a custom octet-stream parser that counts and discards** — no file, no buffer — and refuses over-cap bodies with 413 from an `onRequest` hook before reading a byte. A pure, clock-injected **`TestGuard`** holds a lease per device key across a whole test (warmup → ping ×10 → down → up), renews it per request, and frees it 5 s after the last one ends (`reply.raw.on('close')`, so an abort counts as an end) — a second device gets **409 "another broom is flying"**, `POST /release` hands it back at once, and **ping is deliberately ungated** so a busy bridge stays reachable. Client: pure `metrics.ts` (median · Mbps · 3-sig-fig formatting · sparkline points · spread%), an injected-transport `runTest.ts` orchestrator (warmup untimed both ways, download timed **from the first chunk**, upload figure taken from the **server's** ms while XHR `upload.onprogress` drives the live gauge), and a mobile-first `/nimbus` page — 88px live gauge, a 🧹 flying along a progress track, three-big-numbers result card, per-device history with sparkline + "last 3 within N%", on-page approximate-by-design note, 409 state, cancel. Capability-gated **2nd card on Diagon Alley** (`card-tone-2`, positional per the palette rule) + new `GaugeIcon`; CSS in its own `features/nimbus/nimbus.css` (screensaver precedent). Two endpoints beyond the plan's table, both justified: `GET /config` (so the size menu comes from `NIMBUS_MAX_TEST_MB`, never hardcoded) and `POST /release`. Gate green: lint/typecheck/**379 server** (+42 nimbus: pool cycling/compression-proof stream, guard state machine, usecases, integration byte-count+headers/413/409/results/audit, **kill test**) **+ 301 client** (+31 metrics + orchestrator incl. cancel/busy/never-save-a-partial)/build. **Live-verified** on the built server (port 4747, PM2's 4646 untouched): upgrade-path boot on the real DB → health 200 → SIGINT → restart → 200, `integrity_check ok`, history intact; fresh-DB bootstrap 200 + `ok`. Over HTTP: a 5 MB download arrived as **exactly 5 242 880 bytes with `content-encoding: identity` even when curl asked for gzip**, and gzipping the payload *grew* it (incompressible); with a 100 MB transfer in flight a second device got 409 on both directions while ping stayed 204 and `/config` reported the holder; 101 MB post → 413; **five consecutive 100 MB uploads left non-log storage byte-identical and RSS plateaued at ~133 MB** (no growth per test). In headless Chrome with **CDP network shaping** (the honest test — loopback finished in <250 ms): 40/12 Mbps → the page reported **↓40.0 / ↑11.9 Mbps**, 30/10 with 60 ms latency → **↓30.0 / ↑9.95 / 62 ms**, 60/20 in dark Aurora → **↓58.3 / ↑19.8**; all four phases observed with the broom advancing 0→100% per phase; **cancel mid-download** returned to Fly, saved nothing, and left the guard free; an untouched second tab gained the new reading **over SSE**; mobile 390×844 had a 308×52px thumb CTA and zero horizontal overflow. **One real bug found and fixed live**: the gauge's null placeholder rendered as a giant em-dash bar (an 88px "—" reads as a broken element) and sat there for the whole latency phase — the placeholder now has its own muted `--idle` size and the ping loop publishes a **running median** from the first round trip. Smoke rows (12 results + 12 audit rows) deleted from the owner's DB afterwards; docs synced (architecture registry + data flow, project-structure, 8 decisions).
- 2026-07-25 — **PLAN-12 (Loki) closed out and PLAN-13 (Accio) implemented** on `feat/plan-13-accio` (uncommitted, pending owner test). PLAN-12 flipped to `done` (PR #30 merged 2026-07-24), plan archived to `completed/`, README gate row ticked. **Accio**: new local-profile `accio` module + `accio_links` table (migration `0006`, additive — 6-char id, no slug; `tags` as a JSON array) with `GET/POST /api/accio`, `PATCH`/`DELETE /api/accio/:id`, and `accio.saved|updated|deleted` → SSE + audit (`updated` deliberately not audited). Pure `url.ts` normalizes and validates (http/https only, `javascript:`/`data:`/`mailto:` etc. → **422**; scheme-less input gets `https://`; `localhost:3000/x` stays a host+port; IDN → punycode; trailing-junk trim) and pure `title.ts` extracts `<title>` (entities, head-vs-svg, truncated downloads, 200-char cap). **Title enrichment never blocks a save**: the 201 returns the instant the row exists, then the module (off the bus) runs a capped, retried `HttpTitleFetcher` (`ACCIO_TITLE_TIMEOUT_MS`/`ACCIO_TITLE_MAX_BYTES`) and patches via `accio.updated`; a user edit/delete mid-flight wins; shutdown awaits in-flight lookups. Client: shared `core/accio.ts` client (in core so Hermes can use it), `/accio` shelf — paste-and-go bar, hostname letter tile (palette slot from a **hostname hash**, a logged scoped exception to the positional card rule), title/tags/relative time/author, tag chips + search that compose, sort, inline edit, confirm-delete, "Nothing summoned yet" empty state, live via SSE. **Hermes gained "Accio it"** on URL-bearing text entries (via the core client — no cross-feature import), and Accio is the **4th Midgard portal** beside Hermes (owner's pick), capability-gated. **Owner round 2 — shelf UI revamped from list to grid:** the one-per-row list wasted a desktop's width and forced a long scroll (a bookmark page fills up fast). Now a **full-width breakout at ≥1024px** (Variant/Runestone/Edda precedent, `min(105rem, 100vw − gutters)`) holding a `repeat(auto-fill, minmax(16rem, 1fr))` **card grid** — 5 columns at 1600px, 4 at 1280, 1 on a phone, no breakpoints of its own. Each card is ~126px: colour tile + 2-line-clamped title + host · time · author + tag chips, with the **whole card as the click target** (stretched-link `::after`, disabled while editing) and copy/edit/delete as **corner icon buttons revealed on hover or focus** (always visible on touch). Cards in a row share a height, tags sink to the bottom. The filter bar gained a match count and **sticks below the header ≥768px** (not on phones, where it would eat a third of the viewport); it is styled as a slim frosted panel — the shell header's own `--header-veil` + `blur(14px)` + border/radius — after a first pass filled it with flat `--bg`, which painted a black slab over the gradient sky. Sticking it also needed a real offset: a new `--header-h` (3.875rem, the header's measured 62px) so it parks under the header instead of behind it. Measured: **15 links fit one 1600×1000 screen with no scrolling**, where 3 filled it before. Live-verified in dark theme at 1600/1280/390 — no horizontal overflow at any width. 

**Owner round 1 (same branch):** (1) **any scheme is now shelf-able** — `chrome://chrome-urls/`, `chrome://net-internals/#dns`, `about:config`, `mailto:`, deeplinks — with **no per-scheme handling**: `normalizeUrl` blocks only what executes inside a page (`javascript:`/`data:`/`vbscript:`/`blob:`/`filesystem:`) and every row renders as an ordinary `target="_blank"` anchor for the browser/OS to resolve. Opaque hosts are lowercased so one page is one row, fragments preserved, and **title enrichment is skipped for non-http(s)** (nothing to fetch). _(First attempt shipped a curated browser-scheme allow-list plus badged copy-me rendering; the owner rejected it as over-engineering and it was removed — see the superseding decision-log row.)_ (2) The **save error clears on the next keystroke** in the URL field. (3) The tags field gained a **suggestion list** built from the tags already on the shelf — click/focus shows all, typing narrows on the fragment after the last comma, picking completes it and stays open (custom, not `<datalist>`, which can't see past the first comma). Gate green: lint/typecheck/**337 server** (+60 accio: url corpus incl. browser schemes, title extraction, usecases incl. enrichment races + the never-fetch-a-browser-page guard, integration CRUD/422/schema/tag-exactness/audit/browser-pages, real-HTTP enrichment, **kill test**) **+ 280 client** (+28 shelf + tag-input helpers)/build. **Live-verified** on the built server (port 4747): upgrade-path boot on the real DB → health 200 → SIGINT → restart → health 200, `integrity_check ok`; fresh-DB bootstrap also 200 + `ok`; save/422/tag+q filtering exercised over HTTP (smoke rows deleted afterwards). **UI live-verified** (headless Chrome via CDP, 1400px): every row — `chrome://chrome-urls/`, `mailto:`, https — renders as a real anchor carrying its exact href, no special-cased markup left; a rejected `javascript:` save shows the error and the **next keystroke clears it**; clicking the empty tag field lists `browser/later/reading`, typing `re` narrows to `reading`, picking it fills `reading, ` and leaves `browser/later` offered; `example.com` got its real title ("Example Domain") while every `chrome://`/`about:` row stayed null — zero runtime errors. PM2 `bifrost` was found **already stopped** at session start and left as found. Docs synced (architecture module registry + a data-flow entry, project-structure, 7 decisions).

- 2026-07-25 — **Screensaver renamed Vörðr → Nótt** (owner pick; Norse goddess of night; tagline "night over the bridge"). Display strings + `.nott*` CSS + Heimdall `NottSection`/`nott-*` ids + comments renamed; functional ids (`screensaver` module, `/api/screensaver/*`, `features/screensaver/`, `--screen-*` tokens) unchanged. (Earlier same-day: ripple-slowdown, a 4th depth layer + density bump, and a depth/porthole pass — focus vignette + real backdrop-blur rim, per-particle size jitter, twinkle pulse.) Gate green (lint/typecheck/**252 client**/build); live-verified no runtime errors.
- 2026-07-25 — **Vörðr owner tweaks (same branch, uncommitted):** (1) default idle **60s→5s** for testing (floor lowered 10→5 across config schema/overlay/module + Heimdall dropdown gains a 5s option + `.env.example`); (2) **denser field + a "look-around camera"** replacing the small parallax — the particle field is now a **world larger than the viewport** (padding = max per-depth pan), counts scaled up (bands 90/130/180, ×~3.6 world-area factor) with a weighted depth mix (mostly far stars, few near suns); moving the cursor eases a **per-depth camera pan** (`DEPTH_PAN` far 0.55 / near 1.7, signed-square `easePan`, `CAMERA_EASE` momentum) so turning toward an edge scrolls the sky and reveals new stars/suns, off-screen particles culled. Engine rewrite is pure/testable (world bounds + wrap + `easePan` unit-tested; dropped the old in-radius attraction). Gate green (lint/typecheck/**250 client**/build). **Live-verified** (CDP, idle 5s): overlay mounts (brand + live clock + random quote), cursor panned to all four edges with **zero runtime errors**, PM2 4646 intact. Pixel screenshots still blank (headless canvas limitation) — visual density/pan feel is owner's to judge.
- 2026-07-25 — **Idle screensaver "Vörðr" implemented (owner-requested, off-plan; uncommitted, pending owner test)** on `feat/house-themes-and-portal-cards`. A desktop-only full-screen particle constellation (2–3 depth layers, drift + cursor parallax, far-layer constellation lines, 30fps-capped single RAF) that appears after a configurable idle timeout, with a branded header + **live clock** and a **random lore quote** (new `assets/quotes.ts` bank — HP/MCU/Ghibli/Norse/Greek, seedable `pickRandomQuote`) floating above. **Dismissal = owner's model**: a **click** lands a ripple then fades out, a **keystroke** dismisses; `mousemove` only pushes the particles ("space" feel), never dismisses. **Never initialized on phone/tablet** (`isDesktop`: `(hover:hover)+(pointer:fine)` + `maxTouchPoints<=0` + width≥1024). New **policy-only `screensaver` server module** (both profiles) mirroring Loki: env-seeded config (`SCREENSAVER_*`), public `GET /api/screensaver/config` + admin `PATCH /api/screensaver/settings` + `screensaver.settingsUpdated`→SSE; new **Heimdall "Screensaver" card** (Realm) with enable/idle/density/motion/lines/mouse/quotes/rotation, live-rebinding. If the Heimdall modal is open when idle fires, App **closes it first**. Colours come from theme tokens: new optional **`--screen-*`** group (schema `SCREENSAVER_TOKENS`, tokens.css dark+light defaults, on-brand overrides in all 4 identity themes; `readColors` reads them at draw time like QrCard), documented in **THEME-SPEC** + the **theme SKILL** (calm-veil bar). `prefers-reduced-motion` → static field. Overlay lazy-loaded (own 9.4 kB chunk). Gate green: lint/typecheck/**277 server** (+4 screensaver int tests, capabilities list updated) **+ 249 client** (+19 particles/ripple/isDesktop/quotes)/build. **Live-verified** (built server on alt port 4747 + temp storage so PM2 `bifrost` on 4646 stayed intact; headless Chrome via CDP): after 10s idle the `.vordr` canvas mounts with brand "Vörðr", live clock, and a random quote (Thanos/Stark/Delphi across runs); a synthesized **click removed `.vordr`**; a **390-width viewport never mounted it** (`hasCanvas:false`). Note: pixel screenshots came back blank (headless canvas-capture limitation) — the visual aesthetic is the one item left for owner interactive test. (Mid-run I accidentally `pkill`ed the PM2 `bifrost` once; restored via `pm2 restart` → online/health 200. Lesson: kill smoke servers by saved PID, not `pkill -f`.)
- 2026-07-24 — **Theme work (owner, uncommitted): two new house themes + card-palette system.** Added `themes/gryffindor.json` (warm hearth gold+scarlet, iterated to fix a "too bright / bloody" first pass by darkening surfaces + deepening accents) and `themes/slytherin.json` (cool under-the-lake emerald+silver); also calmed the shipped **ghibli-dusk** built-in the same way. Then a **card-colour system overhaul**: cards no longer borrow the atmospheric `--tone-teal/violet/amber` (which collapsed to gold-on-gold in warm themes, making Variant≈Edda and Receive≈Hermes identical). New **10-slot palette** `--card-1..10` (schema `CARD_TONE_TOKENS`, tokens.css dark+light defaults, per-theme overrides in all 4 identity themes), class `.card-tone-N` sets `--ct`, tint+glow via `color-mix`; hub + portal cards share it. Cards coloured by **position, per page** — each grid is a data-driven array rendered with `cardToneClass(index + 1)` (`core/ui/cardTone.ts`, wraps after 10); reordering a card recolours it, no hand-picked colours / literal class strings / fixed numbers. **Loki folded into the palette** (removed the bespoke `.hub-card--loki` animated card + card-only `--tone-loki-2`; kept `--tone-loki`/`--glow-loki` for the Loki tool page). Also added a **`theme` project skill** (`.claude/skills/theme/`) codifying the contract + the calm-not-glaring bar + card palette + validate/restart steps. Then **unified the two card families into one `Portal` component** (`core/ui/Portal.tsx`, class `.portal`, grid `.portals`) used on all three hubs — removed the old `PortalCard`/`.hub-card` split; every card now carries a **footer tagline** (`go`) naming where it leads (e.g. Runestone → "carve the runes"); `soon` renders a dimmed non-nav card + badge. Join Bifrost stays its own band (not a portal); generic `Card` panel kept (used by many feature pages). Docs synced (THEME-SPEC card group, DESIGN cards section, rules/coding.md, decisions.md, 2 persistent memories). Gate green (lint/typecheck/**273 server + 230 client**/client build). **Needs server restart + client rebuild** to view (schema + tokens changed). Uncommitted pending owner test.
- 2026-07-24 — **PLAN-11 (Edda) merged** (PR #29 → develop, 2026-07-23) and flipped to `done`; plan file archived to `completed/PLAN-11-edda.md`; plans/README gate row checked off. **Gate cleared — starting PLAN-12 (Loki)** on a fresh `feat/plan-12a-loki-transforms` branch.
- 2026-07-24 — **PLAN-12 Loki output-panel alignment (owner)** — the output box wrapped its own header inside the border, so it sat ~40px higher than the code editor box. Restructured (CSS only, `app.css`): the output header now floats above an unstyled panel, the border/background moved to `.loki-output__body` which is pinned to the editor height (`flex:none; height: var(--loki-editor-h)` ≥1024, since `flex:1`'s 0% basis collapsed it), both heads share a fixed 2.75rem height, and the output column gets the same head→box gap as the code pane. Live-verified (CDP 1440px): editor box and output box now share top (279) and bottom (~828). Gate green (lint/typecheck/230 client).
- 2026-07-24 — **PLAN-12 Loki Part B — owner feedback round 2** (output panel layout, uncommitted). The Calcifer output moved from **below** the editor to a **right-hand panel** in a new `.loki-workarea` (code pane | draggable `.loki-divider` | output), default **20vw**, pointer-drag resize with **both panes clamped ≥ 20vw** (20% of screen); stacks below the editor <1024px (divider hidden). The panel now appears **only on Run** (not on any output state), has a **Hide** control, and a **"Show output"** button surfaces in the editor header **only after the first run** — clicking Run while hidden re-reveals it (`showOutput`/`everRan` state). The A−/A/A+ control now also sizes the output text (`.loki-output__body` uses `--panel-font`). Gate green (lint/typecheck/273+230). **Live-verified** (CDP, 1440px): pre-run no panel/no Show button; after Run the output sits right at exactly 288px (=20vw) with a divider; Hide→panel gone+Show appears; Show→back; Hide+Run→reappears; A+ grows output font 14→16px; divider drag widens the output and clamps the code pane at 20vw.
- 2026-07-24 — **PLAN-12 Loki Part B — owner feedback round 1** (uncommitted, on 12b). (1) **Run now shows the completion value of the last statement**, not only a lone expression: new `wrapLastExpression` (core/js, acorn) rewrites a trailing top-level ExpressionStatement to `return (…)` on the main thread before the worker runs it, so `…; someCall()` shows `someCall()`'s value (the reported `getOrderTypeDetailsText(...)` case now prints its JSON string). The worker dropped its own single-expression heuristic — it just runs the body and reports the return. Line numbers stay aligned (rewrite starts at the statement offset). (2) **Loki buffer no longer goes stale after Diff→Variant→back**: the draft was read from a **module-level** const (evaluated once), so a remount restored an old buffer; now it's read **per-mount** (a ref), `diffInVariant` flushes the draft synchronously before navigating, and an unmount effect flushes the latest too. Verified via a true client-side `history.back()` (react-router popstate): back at /loki the current transformed buffer is restored, not the old one. Gate green (lint/typecheck/273+230, +8 wrapLastExpression tests). Live-verified both.
- 2026-07-24 — **Find-panel match counter (owner request)** on the shared `core/ui/JsonEditor` (uncommitted, on the 12b branch). A `searchMatchCount` ViewPlugin injects a small "N of M" / "M found" / "No results" label into CM6's default find panel (it has none), computed via `SearchQuery.getCursor` (capped at 1000) with the current index resolved from the selected match; kept in sync on query/selection/doc changes. Since every panel mounts this one editor, **all find menus inherit it** — Runestone (JSON), Variant (JSON + text), Loki (JS), Edda (markdown). Themed via a `.cm-search-count` rule in `editorChrome`. **Live-verified** (CDP): Runestone "role" → "4 found" on commit, then "1 of 4" → "2 of 4" tracking CM's selected match; Loki/Edda/Variant-text all show "3 found" → "1 of 3". Gate green (lint/typecheck/273+222).
- 2026-07-24 — **PLAN-12 (Loki) Part B "Calcifer" implemented** on `feat/plan-12b-loki-execution` (stacked on 12a, uncommitted, pending owner test). Sandboxed execution via an **ephemeral module Web Worker** per run (`loki-runner.worker.ts`, Vite-bundled to its own chunk): installs a console shim with **in-worker budget** (default 500 entries, ~8 KB/entry truncation), runs via **AsyncFunction** (top-level await; single-expression detection wraps `return (…)` for a REPL value, falls back to statements), and posts back pre-**serialized** values (`serialize.ts` — cycle-safe, depth-limited, explicit undefined/BigInt/Symbol/ƒ/Error/Map/Set/Date; corpus-tested). Main-thread `LokiRunner` owns spawn → **watchdog** (config timeout) → **Stop** → `terminate()`, both kill paths just terminate, `runId` drops stale messages. `OutputDrawer` shows level-coloured entries, the `⇐ result` line, thrown errors + collapsible stack, truncation markers, and duration. Run UI ("🔥 Run"/Stop, the Calcifer flame) gated on **`capabilities.profile === 'local'` AND the Heimdall master switch**. Server: the `loki` module gained a **public `GET /api/loki/config`** + admin **`PATCH /api/loki/settings`** (executionEnabled · fetchAllowed · runTimeoutMs · consoleMaxEntries), settings-table backed (env seeds `LOKI_EXECUTION_ENABLED`/`LOKI_FETCH_ALLOWED` + overlays), broadcasting a new `loki.settingsUpdated` bus event → SSE so open pages rebind live. New **Heimdall "Loki" card** (Realm group) with those four controls (apply-on-change). Gate green: lint/typecheck/**273 server** (+4 loki int tests) **+ 222 client** (+10 serializer tests)/build (worker chunk emitted). **Live-verified** (built server, port 4747, CDP): console streaming + object serialization; `40+2` → `⇐ 42`; thrown error + stack; `fetch('/api/health')` returns with the toggle on, **blocked** ("fetch() is disabled by Heimdall") with it off; `while(true){}` **killed by the 5 s watchdog while the main thread stayed responsive** (2+2 evaluated mid-loop); **Stop** kills mid-run; a 1200-log loop truncates at 500 + "…and 700 more"; master-switch-off **hides Run live via SSE** (no reload) + shows the "turned off in Heimdall" note. Clean SIGINT; PM2 `bifrost` on 4646 untouched. **Deferred (Part-A polish, still open):** the Part-A-only Heimdall defaults (beautify/regex/font/mode) + a per-run timeout control + a stringify quote-style selector.
- 2026-07-24 — **PLAN-12 Loki Part A — owner feedback round 1** (same branch, uncommitted). (1) **Loki buffer now survives navigation** (the Diff→Variant→back data-loss bug): new `features/loki/draft.ts` caches the whole workspace (code + regex fields + mode) in localStorage, restored on mount + saved debounced. (2) **Minify no longer prepends `!`** — Terser `compress.negate_iife:false` in `core/js/format.ts`. (3) **Stringify is guarded** — new `isSingleStringLiteral` (core/js); tapping Stringify on an already-quoted buffer is refused with "use Destringify" instead of nesting. (4/5) **Shared panel controls** — new `core/panelFont.ts` hook (`--panel-font`, per-device localStorage `bifrost.editorFontPx`) + `core/ui/PanelControls` (`PanelFontControl` A−/A/A+ and `UndoRedoControl`), scoped via a `.panel-scope` wrapper (`core/ui/ui.css`); `JsonEditor` gained `undo()`/`redo()` handle methods and now **isolates each `applyEdit` as its own history group** (`isolateHistory.of('full')`) so one transform = one ⌘Z. Wired into **Loki** (rail + mobile sheetbar), **Runestone** (toolbar), and **Variant** (font control in head, undo/redo per pane). Removed Loki's old bespoke `--loki-panel-font`/`.loki-fontctl` in favour of the shared ones. Gate green (lint/typecheck/**269 server + 212 client**/build). **Live-verified** (built server, port 4747, CDP): draft restore across /loki→/variant→/loki; minify of `!function(){…}()` → `console.log(1);` (no `!`); Stringify twice → refused + notice; undo reverts only the transform (typing kept), redo re-applies; Runestone shows 3 font + 2 undo/redo btns, Variant shows head font + 4 pane undo/redo. Clean SIGINT shutdown; PM2 `bifrost` on 4646 untouched throughout.
- 2026-07-24 — **PLAN-12 (Loki) Part A first build** on `feat/plan-12a-loki-transforms` (uncommitted, pending owner test + UI checkpoint). New pure `core/js/` toolkit (all `eval`-free, 104 unit/property tests): a literal-aware `scanJs` lexer → `stripComments` + `convertQuotes`; `stringifyJs`/`destringifyJs` (round-trip property), HTML/URI escapes, `wrapIife`/`unwrapIife` (round-trip property), `curlToFetch` (tokenizer + unsupported-flag reporting), `jsonToJs` (demo D/E, JSON→JS→value identity property), and lazy `beautifyJs` (Prettier standalone) / `minifyJs` (Terser) / `checkJsSyntax` (acorn) — all three heavy deps **new + code-split** into their own chunks. Shared `JsonEditor` gained a **javascript mode** (`@codemirror/lang-javascript` + `--syn-*` tint, bracket-close, reusing `applyEdit`). **Runestone toolbar** got **"Copy as JS"** (enabled on Valid JSON ✓). New capability-only `loki` server module in **both** profiles (+ `LOKI_RUN_TIMEOUT_MS`/`LOKI_CONSOLE_MAX_ENTRIES` env for Part B); nav card = 4th Ollivanders card with a **new emerald `--tone-loki`** + a deliberately distinct **dual-hue diagonal shifting sweep** `.hub-card--loki` (reduced-motion-safe), dark+light. **LokiPage**: `Transforms | Regex` mode toggle (separate per-device-persisted workspaces), full-width breakout ≥1024, grouped rail (Format · Strings · Convert · Clean) with mobile **bottom-sheet** chips, acorn syntax banner, stats + minify before→after bytes, per-device **panel-font A−/A/A+** (`--loki-panel-font`), and a **Variant "Diff before/after"** hook via a new boundary-legal `core/variantSeed` sessionStorage bridge (Variant reads it once on mount → text mode). Gate green: lint/typecheck/**270 server + 209 client**/build. **Live-verified** (built server on an alt port 4747 so the owner's PM2 `bifrost` on 4646 stayed online + untouched; headless Chromium + CDP): `loki` in `/api/capabilities`; Ollivanders shows the emerald shifting card; type JS→"Parses cleanly"; "To ' quotes" literal-aware; Beautify (lazy Prettier) reformatted with singleQuote+semi; Regex `(\w)(\d)` on `a1 b2 c3` → 3 matches + 3 rows + 3 in-editor highlights; mobile 390 collapses the rail to group chips; SIGINT shutdown clean. **Deferred to post-checkpoint (Part A polish)**: the Heimdall "Loki" settings card (using client defaults for now) + a stringify quote-style selector (defaults to double). Part B (Calcifer sandbox) is the next PR.
- 2026-07-21 — **PLAN-12 (Loki)** authored after converter demo (JSON5 + acorn-diagnostics architecture live-proven, round-trip identity verified): Part A transforms (Prettier/Terser lazy, pure `core/js` utils, cURL→fetch, strip-comments standalone; JSON→JS only, surfaced as Runestone's "Copy as JS" — JS→JSON dropped by owner) + `Transforms|Regex` mode toggle; Part B killable-worker execution ("Calcifer", local-only, Heimdall master switch); panel font A−/A+ (per-device) + Heimdall Loki settings card; base64 excluded (toolbox owns it), snippet library skipped.
- 2026-07-23 — **PLAN-11 (Edda) implemented** on `feat/plan-11-edda`. New `edda` server module + `eddas` table (migration `0005`, additive; runestone's id/slug/301 scheme; markdown validates only the size cap — empty docs allowed) with CRUD + public `GET /edda/api/:slug` (raw `text/markdown`, CORS `*`, 301, `?download=1`), reserved-segment guard, `edda.saved`/`edda.deleted` → SSE + audit; module in **both** profiles. Client: shared `core/markdown/` (renderMarkdown marked-GFM+hljs+heading-ids+DOMPurify · outline · stats · pure toolbar commands, all unit-tested), `JsonEditor` gained a **markdown mode** (`@codemirror/lang-markdown`, `--syn-*` tint) + `applyEdit` minimal-change handle; three surfaces `/edda` (editor; mobile opens Preview) · `/edda/preview/:slug` (public render) · `/edda/api/:slug`. Perf: 200ms debounce in rAF, auto-manual over `EDDA_LIVE_PREVIEW_MAX_KB` (300) + pinned-manual toggle, **mobile preview unmounted while editing**; full-width breakout ≥1024 with draggable divider, stacked ≥768, single-pane <768; toolbar+shortcuts (⌘B/I/K), outline panel, approximate scroll-sync (toggle), stats bar, `.md`/`.html` export, scratch-draft restore, creative 404, coming-soon footer. Wired the Ollivanders card + capability nav (`edda` in `match`/`modules`). New dep `@codemirror/lang-markdown`; test-only `jsdom` (client) for DOMPurify sanitization tests (happy-dom couldn't drive attribute stripping). **Deviations**: scroll-sync percentage-only (approximate-by-design, no heading-anchor correction — logged tarpit); editor find inherited from the shared editor (already shipped) rather than re-implemented. Gate green (lint/typecheck/**269 server + 161 client**/build). **Live-verified** (built server + headless Chromium, desktop 1400 + mobile 390): live preview renders GFM/tables/hljs and `<script>` is inert on editor+public surfaces; type→save→open-from-library→public preview→raw-API round-trip; mobile `/edda/:slug` opens in Preview, Edit unmounts `.md-preview`, toggle-back remounts. Restart smoke + fresh-DB bootstrap both `integrity_check ok`. Uncommitted pending owner test.

- 2026-07-22 — **Favicon + PWA support** on `claude/favicon-pwa-support-o9p8gr` (owner-requested; site had no favicon and wasn't phone-installable). Added `client/public/`: `favicon.svg` + `favicon-16/32.png`, `apple-touch-icon.png` (180), `pwa-192/512` (`any`) + `pwa-maskable-192/512` (`maskable`), and `manifest.webmanifest` (standalone, `#0b0e14`). `index.html` head gains icon/manifest links + Apple web-app meta. **No service worker** (LAN tool, no offline need). Icons drawn by a one-off Node PNG generator (no raster tooling in env); **artwork iterated live with the owner** to the chosen **"Pure Arch"** — a glowing rainbow-bridge arch (teal→deep-purple→green) between two lit realm-nodes over a near-black starfield + vignette, deliberately dark with no white strip. Gate green (lint/typecheck/**245 server + 132 client**/build); **live-verified** on the built server — `/favicon.svg` (image/svg+xml), `/manifest.webmanifest` (**application/manifest+json**), apple-touch + pwa PNGs all 200, index head carries the links. Docs synced: `tech-stack.md` (PWA no longer deferred), `decisions.md`, this log.
- 2026-07-22 — **v1.1.0 released** (develop→main, PR #24) — plans 07–10 + editor find/tree-collapse/Runestone-revamp + category nav/Home redesign/tone cards + refreshed README screenshots (PR #23). Tag `v1.1.0`, [GitHub Release](https://github.com/0xSiddhant/bifrost/releases/tag/v1.1.0) with `bifrost-v1.1.0.tar.gz`, main==develop. **`RELEASE_TOKEN` PAT set** (Contents+Workflows RW). Hit two release-automation snags, both handled: (1) the first `gh secret set` stored an **empty** token → checkout failed; re-set with a real value; (2) `release.yml` used `git push --follow-tags` on a **lightweight** tag → the tag never pushed and Publish failed. Completed the release manually (pushed tag, published Release, back-merged) and **fixed the workflow** (push branch + tag as separate refs) on `fix/release-tag-push` → PR to develop. **PLAN-11 (Edda) is next.**
- 2026-07-21 — **Midgard redesign + card tone-gradients + docs sync** (same `feat/nav-categories` branch, owner feedback on the nav reorg). Fixed the stretched-empty Hermes/Join-Bifrost grid: **Hermes → third Home portal** (new **amber** tone `--tone-amber`/`--glow-amber`, dark+light) beside Send (teal)/Receive (violet); **Join Bifrost → wide horizontal onboarding card** (QR beside details, `core/ui/JoinBifrostCard`). Then extended the per-card **tone-gradient** to every hub card (`.hub-card--{teal/violet/amber}`, tones cycling, `--soon` dims). **Context-sync**: updated `context/project-structure.md`, `context/architecture.md`, `context/tech-stack.md` (@codemirror/search), `docs/DESIGN.md` (cards & tones, category-nav layout, breakout), `docs/THEME-SPEC.md` (amber tokens), root `README.md` (3-tab nav), + PLAN-11/PLAN-99 entry-point notes. Gate green (lint/typecheck/245+132/build); live-verified in dark theme (3 toned portals + horizontal join band on Midgard; toned hub cards on Ollivanders/Diagon Alley). Uncommitted pending owner test.
- 2026-07-21 — **Nav reorg → 3 category tabs** on `feat/nav-categories` (stacked on `feat/editor-find-tree-collapse`, owner-requested; PR based on #21). The flat 7-tab bar overflowed mobile (truncated labels), so tabs are now **Midgard** (home: Send/Receive/Hermes cards + the Join-Bifrost QR moved off Sigil into `core/ui/JoinBifrostCard`), **Ollivanders** (dev: Runestone/Variant/Edda-soon), **Diagon Alley** (toolbox: the QR maker "Make a QR" + Base64/UUID/timestamp coming-soon per PLAN-99). New app hub pages + `WandIcon`/`SparklesIcon`; category tabs light active on their tools' sub-pages (custom `useLocation` match); tabs gated by `/api/capabilities`; Sigil slimmed to the QR generator; bottom-nav labels wrap to 2 lines then ellipsis. Gate green (lint/typecheck/245+132/build). **Live-verified** (headless Chromium, mobile 390 + desktop 1280): bottom bar shows exactly `[Midgard, Ollivanders, Diagon Alley]` with full labels; `/runestone`→Ollivanders active, `/sigil`→Diagon Alley active; hub cards + Midgard Hermes/Join-Bifrost QR all render. Note: found the PM2 `bifrost` app **stopped** at start (not by this turn) — verified against a temporary `npm start` server, then restored PM2 to online (health 200).
- 2026-07-21 — **Editor find + tree collapse-all implemented** on `feat/editor-find-tree-collapse` (off develop, post-PLAN-10-merge; owner asked to build now rather than wait for PLAN-11). (1) **In-editor find** on the shared `core/ui/JsonEditor` via `@codemirror/search` (added to client deps): ⌘/Ctrl-F panel + `openSearch()` handle behind "Find" buttons (Runestone toolbar, each Variant pane header), token-styled, works in JSON + `plain` modes — so Runestone and both Variant panes get it; Edda inherits later. **Variant cross-pane reveal**: `onSearchMatch` → `crossPaneOffset` reveals the same string in the opposite pane (JSON + text), scroll-lock suspended during the reveal, no diff re-run. (2) **TreeView Expand all / Collapse all** (sticky control row; `openMode` + remount-by-key) folds the whole tree incl. the outer array — appears in Runestone + Variant. Gate green: lint/typecheck/**245 server + 132 client** (+4 cross-pane tests)/build. **Live-verified** (headless Chromium/CDP against the PM2 server serving the rebuilt `client/dist`): Runestone tree **Collapse all → 1 row / Expand all → 17 rows** (whole-tree fold incl. outer object); editor **Find** panel opens themed and highlights **3/3** `role` matches; Variant **cross-pane reveal** — searching a needle in the LEFT pane scrolled the RIGHT pane `scrollTop 0 → 1301` onto the buried match (both Find buttons present). Note: a local `npm start` couldn't bind 4646 (PM2 `bifrost` already owned it), and a stray SIGINT bounced the PM2 instance once (↺1, back online). **PLAN-11** updated to reflect Edda inheriting the now-shared find; PLAN-99 backlog row reverted. **Then (same branch) a Runestone page revamp** (owner feedback on the shipped find): fixed the find-panel theming bug (CM inputs are `.cm-textfield` with no `type=text`, so the old selector never matched → white boxes; now dark/theme-matched, verified bg `rgb(19,24,36)` + accent focus ring, fixes Variant too), and broke the editor+head out of the 62rem shell to `min(105rem, 100vw−gutters)` at ≥1024px with a viewport-filling height via `--rune-editor-h` — live-verified at 1600px: workspace 1536px wide, editor 664px tall, head aligned with the editor, tree Expand/Collapse controls intact. Gate stays green (245+132/lint/typecheck/build). Left **uncommitted** for owner testing.
- 2026-07-21 — **PLAN-10 tranche 2 (About + Logs)** on the same branch/PR #20. **About** section: `GET /about` (version/commit/buildDate/uptime/node/host/profile) with the stamp **baked at build time** (`scripts/gen-build-info.ts` prebuild → gitignored `server/build-info.json`, git/dev fallback) + `GET /changelog` serving `CHANGELOG.md` + `npm run changelog`. **Logs** section: `GET /logs` filtered tail from a new in-memory `core/logtap` the logger feeds via `pino.multistream`, `PATCH /logs/level` runtime pino level (persisted `log.level` → survives restart), and an admin-gated live tail (`GET /logs/stream`, dedicated `requireAdmin` SSE, per-send epoch/expiry recheck). Deviations logged: in-memory ring vs file tail, dedicated SSE vs public-hub gating, single-gate multistream. Gate: lint/typecheck/**245 server** + 128 client/build green + live smoke of all endpoints. **Remaining tranches**: Storage+Maintenance, Devices-admin+Modules.
- 2026-07-21 — **PLAN-10 tranche 1 (Heimdall Modal)** on `feat/plan-10-heimdall-modal` (PR #20). Converted Heimdall from a routed page to a **modal overlay**: deleted the `/heimdall` route + 404-lookalike + `heimdallGate`; viewport-gated entry (≥768px, wordmark taps + shortcut); footer-mark entry removed; existing content regrouped into searchable sections (Watchtower/Realm/Vault/Bridge) with jump-highlight (`search.ts` + test). **Owner changes during testing**: PIN required on every open (close/refresh re-lock — supersedes the 2026-07-16 relaxation); Wardens removed from main nav → Heimdall _Wardens_ section (`/wardens` route kept); **7-day stale-device prune** (`POST /api/presence/prune`, on Wardens open, best-effort GET fallback, activity preserved); Overview shows _connected_ + _known_ device counts, both from the presence roster (stats `connectedClients` now = distinct online deviceIds). Single Lock control, Heimdall brand in panel, PIN autofocus, badge-alignment fix. Gate: lint/typecheck/228 server+128 client/build green. **Remaining tranches**: About+Logs, Storage+Maintenance, Devices-admin+Modules. Full detail in [`history.md`](history.md).
- 2026-07-21 — **PLAN-09 (Ops) implemented** on `feat/plan-09-ops` (PR #19), all 8 items. **Tranche 1** (verified here): online-safe **backup/restore** (`core/backup` VACUUM INTO + zip storage/+themes/, rotation, `--include-env`; restore refuses a live server; thin CLI wrappers; 18 tests + real round-trip), scripted **restart-resilience suite** (`npm run test:resilience` — 50 SIGINT cycles + SIGKILL mid-write/mid-migration + tmp-sweep, all integrity-ok; full 50-cycle run green), **PM2** + **launchd** (`ecosystem.config.cjs`, `docs/pm2.md`, `docs/launchd.md`). **Tranche 2** (written; owner-verifies on hardware): multi-stage **Dockerfile** + compose + `docs/docker-linux.md`, optional **Grafana+Loki+Alloy** observability stack + starter dashboard + `docs/observability.md`, `docs/cloud-profile.md`, **release automation** (`release.yml` — semver-from-commits, changelogen, tag, GitHub Release + tarball, main→develop back-merge, loop guard; needs `RELEASE_TOKEN` PAT) + CI docker-build job + backup smoke (both verified locally). Added `BACKUP_KEEP`, `ops` commitlint scope, `*.cjs` eslint block. Gate: lint/typecheck/226 server+123 client/build green. Owner-manual: PM2 reboot, Linux docker run, live Grafana, PAT setup.
- 2026-07-21 — **PLAN-11 (Edda)** authored: markdown editor (shared CM6 editor gains markdown mode) + perf-budgeted live preview (debounce+rAF, manual-mode degradation over EDDA_LIVE_PREVIEW_MAX_KB, mobile unmounts preview), own `eddas` table/library, three share surfaces (`/edda/:slug` editor · `/edda/preview/:slug` public render · `/edda/api/:slug` raw+download), toolbar/outline/scroll-sync/stats/exports, coming-soon footer. Mermaid/image-paste/PDF parked in PLAN-99.
- 2026-07-21 — Variant invalid-JSON fix: a failed JSON Compare now stays in JSON mode and warns instead of switching to Text and clobbering the text workspace. Live-verified. Also on this branch: context/docs sync, this memory restructure, and two lore renames — **Muninn → Hermes** (`/hermes`, `features/hermes/HermesPage`) and **Mímir → Pensieve** (`/runestone/pensieve`, `PensievePage`); old routes 301-redirect; server module ids (`clipboard`, `runestone`) unchanged. Branch `fix/variant-invalid-json-preserves-text`.
- 2026-07-20 — **PLAN-08 (Variant)** implemented + 4 owner-feedback rounds → merged (PR #17): structural JSON diff + text fallback, editable panes in both modes, JSON/text as separate workspaces, diff-only-on-Compare, and perf caps for large docs. `--diff-*` theme tokens added.
- 2026-07-19 — **PLAN-07 (Runestone)** Part A + B merged (PR #14/#15) + addendum: public data endpoint `GET /runestone/api/:slug`, library renamed **Mímir**. Greek names added to the runestone name-bank.
- 2026-07-18 — Olympus world: `themes/olympus.json` built-in, `greek.tsx` relics 14→20, GREEK character pool.
- 2026-07-16 — **v1.0.0 released** (plans 00–06 archived). **PLAN-06** clipboard/presence/audit-log + device character aliases; Muninn link detection.
- 2026-07-15 — **PLAN-04** theming engine + **PLAN-05** Heimdall admin (auth core service, hidden gesture/PIN). Ghibli Dusk theme + theme enable/disable.
- 2026-07-12→14 — **PLAN-00** foundation, **PLAN-01** UI/UX (owner-approved), **PLAN-02** file transfer, **PLAN-03** previews + QR.
