# Progress Tracker

Update after every work session: status, branch/PR, notes. Statuses: `not-started` · `in-progress` · `in-review` · `blocked` · `done`.

**PLAN-16a (Observability — logging) in progress on `feat/plan-16a-logging`.** PLAN-15 (Portkey) merged as **PR #38** (2026-07-27) and archived to `completed/`. Side work merged since: `fix/npm-logs-path` (**PR #39**), `feat/loki-code-folding` (**PR #40**), tree-value copy + floating toast (**PR #41**). `fix/npm-audit-advisories` (17 → 6) still open.

| Plan    | Title                                              | Status         | Branch / PR                                   | Notes                                                                                                                         |
| ------- | -------------------------------------------------- | -------------- | --------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| PLAN-00 | Foundation (tech setup, zero feature code)         | done           | main (direct, per plan)                       | Pushed 2026-07-12; develop created                                                                                            |
| PLAN-01 | UI/UX foundation ⛔ approval gate                  | done           | PR #2 → develop (merged 2026-07-13)           | **UI approved by owner 2026-07-13**; gate cleared — PLAN-02 unlocked                                                          |
| PLAN-02 | File transfer (upload + download + live watch)     | done           | PR #3 → develop (merged 2026-07-14)           | Owner tested locally; CI green — PLAN-03 unlocked                                                                             |
| PLAN-03 | Previews + QR tool                                 | done           | PR #4 → develop (merged)                      | Merge confirmed in git history 2026-07-16                                                                                     |
| PLAN-04 | Theming engine                                     | done           | PR #5 → develop (merged 2026-07-15)           | Gate cleared — PLAN-05 unlocked                                                                                               |
| PLAN-05 | Heimdall admin panel                               | done           | PR #6 → develop (merged 2026-07-16)           | Gate cleared — PLAN-06 unlocked                                                                                               |
| PLAN-06 | Clipboard sync, presence, audit log                | done           | PR #7 → develop (merged 2026-07-16)           | Gate cleared — PLAN-07 unlocked                                                                                               |
| PLAN-07 | Runestone (JSON editor Part A + library Part B)    | done           | PR #14 + PR #15 → develop (merged 2026-07-19) | Both parts owner-tested; CI green. Plan archived to `completed/`                                                              |
| PLAN-08 | Variant (JSON & text diff checker)                 | done           | PR #17 → develop (merged)                     | Owner-tested; merged 2026-07-20. Plan archived to `completed/`. Gate cleared — PLAN-09 unlocked                               |
| PLAN-09 | Ops (PM2, backup, Docker, observability)           | done           | PR #19 → develop (merged)                     | All 8 items merged; plan archived to `completed/`. Owner-manual left: PM2 reboot, Linux docker run, live Grafana, release-PAT |
| PLAN-10 | Heimdall Modal (overlay conversion + new sections) | done           | feat/plan-10-heimdall-modal → PR #20 (merged) | Merged to develop; **archived to `completed/`** per owner 2026-07-21. Gate cleared — PLAN-11 unlocked                         |
| PLAN-11 | Edda (markdown editor + preview + library)         | done           | feat/plan-11-edda → PR #29 (merged 2026-07-23) | Merged to develop; **archived to `completed/`**. Gate cleared — PLAN-12 unlocked                                              |
| PLAN-12 | Loki (JS transforms + regex + execution)           | done           | feat/plan-12b → PR #30 (merged 2026-07-24)     | Part A + Part B "Calcifer" combined in one PR; owner-tested through several rounds. Plan archived to `completed/`. Gate cleared — PLAN-13 unlocked |
| PLAN-13 | Accio (read-later / bookmark shelf)                 | done           | feat/plan-13-accio → PR #35 (merged 2026-07-25)  | Owner-tested through 4 feedback rounds (schemes, error clearing, tag suggestions, grid revamp). Plan archived to `completed/`. Gate cleared — PLAN-14 unlocked |
| PLAN-14 | Nimbus (LAN speed test)                            | done           | feat/plan-14-nimbus → PR #36 (merged 2026-07-25) | Owner-tested; CI green. Plan archived to `completed/`. Gate cleared — PLAN-15 unlocked |
| PLAN-15 | Portkey (LAN go-links)                             | done           | claude/plan-15-tp9uel → PR #38 (merged 2026-07-27) | Owner-tested; merged to develop. Plan archived to `completed/` (late — in the 16a branch). Gate cleared — PLAN-16 unlocked |
| PLAN-16 | Observability (logging Part A · metrics Part B)    | in-progress    | feat/plan-16a-logging (16a of two PRs)        | 16a = Step 0 + Step 1: Heimdall Logs deleted, `logLevel` text key, client logging. 16b (metrics/Prometheus/Tempo) after 16a merges |
| PLAN-99 | Future backlog                                     | reference-only | —                                             | Never "implemented" wholesale                                                                                                 |

## Recent activity (newest first)

One line per session; **full detail — test evidence, live-verify runs, deviations — lives in [`history.md`](history.md)**.

- 2026-07-29 — **PLAN-15 archived and PLAN-16a (Observability — logging) implemented** on `feat/plan-16a-logging` (uncommitted, pending owner test). PLAN-15 flipped to `done` (PR #38 merged 2026-07-27) and moved to `completed/`; plans/README gate row ticked. **Step 0 — the Heimdall Logs section is gone entirely**: `LogsSection` + its registry entry + `.log-*` CSS, the client `fetchLogs`/`setLogLevel`/`LOGS_STREAM_URL`/`LogEntry` API surface, all three `/api/heimdall/logs*` routes, `sessionStreamLive`, and **`core/logtap.ts`** deleted; `routes/observability.ts` → **`routes/about.ts`** (deps seven fields → `config`). **`logTap` and `setLogLevel` both leave `ModuleDeps`**, taking the `moduleLoggers[]` array and the persisted-level ordering constraint with them; the settings `log.level` key is dropped from the overlay and **deleted by migration `0009`** (data-only, custom drizzle-kit migration), with a test pinning that a legacy row is *ignored* rather than obeyed. `LOG_LEVEL` default **`info` → `trace`** + new **`LOG_RETENTION_FILES`** (30) → pino-roll's file limit with **`removeOtherLogFiles: true`** (without it pino-roll counts only the current process's files, and Bifrost restarts constantly, so nothing would ever be swept). Level now written as the text key **`logLevel`**; Alloy fills it from the old numeric `level` for pre-change files, promotes `logLevel`/`module`/`source`, and its glob narrows to **`app*.log`** — a **pre-existing bug**: `current.log` is a symlink onto the active file, so `*.log` ingested every live line twice and doubled every count on the dashboard. Dashboard migrated to `logLevel=~"warn|error|fatal"`, gained **Source/Module/Level** variables and an **Errors by source** panel; the logs panel is now a filterable explorer. **Step 1 — the failure paths**: `uncaughtException`/`unhandledRejection` → `fatal` + flush + exit(1); a broken `.env` writes a `fatal` line to the archive (not just stderr) via a new `logsDirFromEnv`; shutdown records its **reason**; `AppError`s are logged **once, centrally** at `debug` in `setErrorHandler` (Fastify's request line carries the 4xx but never the why); real `warn`s added to `upload-audit-recorder`, `fs-stats-reader`, `get-download-stream`, `db-accio-repository`; the deliberate swallows (`build-info`, `core/copy`, `core/theme`, `core/deviceId`) keep silence **with a comment saying why**. **The client has a voice for the first time**: new `client-logs` module (both profiles) with a batched, rate-limited, size-capped `POST /api/client-logs` + public `GET /api/client-logs/config`; client `core/log.ts` (warn+, debounced batches, drops on failure, carries route/deviceId/UA), an `ErrorBoundary` around `<App />`, `window.onerror`, `unhandledrejection`, and `build.sourcemap: true`. `source` is a **child** binding on both sides via sibling `moduleLogger`/`clientLogger` off a source-free root (pino *appends* to `base`, so the obvious spelling emits the key twice — pinned by a test, along with the trap itself). **Two real bugs found by live verification, both fixed**: (1) dropping pino's numeric `level` made the **multi-target transport worker drop every line** — file *and* dev stdout went silently empty, because that worker re-parses each line and routes on the number; the formatter now emits both keys (Alloy promotes only `logLevel`), and a new `transport.int.test.ts` writes through the real transport and fails if the sink ever goes quiet again. (2) `logger.flush(cb)` **never calls back while the transport worker is still starting**, so the new fatal paths awaited forever and the process exited **0 instead of 1**; flushing is now bounded (`FLUSH_TIMEOUT_MS`, deliberately not `unref`'d — an unref'd timer lets an idle loop exit before it fires). Gate green (lint/typecheck/**427 server + 346 client**/build). **Live-verified** on the built server (scratch `STORAGE_ROOT`, port 4747; PM2's 4646 untouched throughout): stale `log.level` row deleted on upgrade + health 200 + `integrity_check ok` on both the upgraded and a **fresh** DB (10 migrations); all three `/api/heimdall/logs*` → **404**; every line carries text `logLevel` and **exactly one `source`**; a browser batch landed as `source:"client"` with deviceId/route/UA/ip; 413 over the body cap; rotation forced with `retainFiles: 3` left exactly 3 rotated + 1 active; `pino-pretty --levelKey logLevel` renders INFO/ERROR with colour; SIGINT logged `reason: "signal SIGINT"` → restart → health 200. In **headless Chrome (CDP)** at 1280×900 and 390×844: a plain page load sends **nothing**, a `window.onerror` and an `unhandledrejection` both reached disk as `source=client`, and blocking a route's code-split chunk produced a real React render error → the **ErrorBoundary fallback rendered instead of a white screen** (screenshotted, both viewports) with **exactly one** report posted; restarting with `CLIENT_LOG_LEVEL=debug` changed the served floor **without a rebuild**. **Still manual**: anything needing Docker — the Loki/Grafana panels, the old-vs-new `logLevel` query across both file generations, and a real phone on the LAN. Note: criterion 10's "no `level` key at all" is **not** met, deliberately — see the decision row.

- 2026-07-28 — **Tree view: click a value to copy it** (owner-requested, uncommitted on `develop`). `TreeView` only made the *key* clickable (copies the JSON path); the primitive value beside it was an inert `<span>`. It is now a `<button>` carrying the same classes, with a new `onCopyValue` prop threaded down alongside `onCopyPath`. Copy takes the **raw value, not the rendered label** — strings go to the clipboard unquoted and untruncated (`primitiveLabel` clips at 160 chars for display only), numbers/booleans/null via `String()`. Container rows are unchanged: their key still copies the path, and there is no "copy this subtree" action. Both consumers (Runestone, Variant) gained a `copyValue` toast that trims to 60 chars for the message only. CSS reuses the `.rune-tree__key` button reset (transparent, no border, mono, hover underline) so the row looks identical. Owner then flagged the **copy confirmation being unreachable**: `Toast` rendered *in flow* at the page bottom, so after clicking a node mid-tree the "Path copied" line sat below the fold. Toast gained a **`floating`** mode — portalled to `<body>` (so no ancestor stacking/overflow context can trap it, incl. the `isolation: isolate` added to `.json-editor` on 2026-07-27), `position: fixed` bottom-centre at z-70 (clears the modal scrim's 50 and Loki's sheet 60), `pointer-events: none` so it never eats a click, and a `toast-rise` fade honouring `prefers-reduced-motion`. Applied to the three **transient** notices (Runestone, Variant, Loki); the in-flow banners (Runestone's restore prompt/parse errors, Upload's error) deliberately stay in flow. Live-verify caught a second bug the static reasoning missed: at 390px the floating toast **overlapped the mobile bottom nav** (toast 752–820 vs nav 783–844). Added **`--bottomnav-h`** beside `--header-h` (0 by default, 3.8125rem inside the existing 640px query) and the host offsets by it — re-measured **overlapsToast: false**, 16px gap. Gate green (lint/typecheck/**414 server + 329 client**/build); no server or storage change, so no restart smoke. **Live-verified** (CDP, built server on scratch port 4747 + scratch `STORAGE_ROOT`, PM2 4646 untouched) at 1280×900 and 390×844: toast is `position: fixed`, `parentElement === document.body`, z-70, fully in viewport with the page scrolled to the bottom, and `elementFromPoint` at its centre returns the element *underneath* (`.rune-tree__row`) — clicks pass through; auto-dismiss at 2.4s intact; zero runtime errors. With `clipboard.writeText` stubbed (headless denies the real API), the clipboard payload was captured per kind: a 400-char string copied **in full** despite the 160-char tree preview and 60-char toast trim, `"he said \"hi\" & left"` copied unquoted and unescaped, and `42` / `false` / `null` as bare text; key clicks still copy the path. Owner has **not** hand-tested yet — real-device mobile (safe-area inset under the nav) still manual.

- 2026-07-27 — **Loki code folding + an editor stacking fix** on `feat/loki-code-folding` (owner-requested). Folding was JSON-only: the JS mode omitted `foldGutter`/`foldKeymap`, so the workbench collapsed nothing. The gutter now rides **lang-javascript's own `foldNodeProp`** (Block / ClassBody / SwitchBody / ObjectExpression / ArrayExpression / BlockComment), which covers function bodies, objects, arrays, nested blocks and both IIFE forms with no custom range logic. Owner then hit the real gap: lang-javascript folds `ObjectExpression` (a value) but **not `ObjectPattern`** (a destructuring target), so `function e({ …9 lines… }) {` had no arrow and stayed expanded while its body folded. Added a **`foldService`** for `ObjectPattern`/`ArrayPattern`/`ParamList` — consulted *before* the grammar, returns `null` to fall through, so no existing fold changed; innermost match wins, so a destructured param folds as `({…})` keeping the braces as a cue. **`ArgList` deliberately excluded** (it would have hijacked the `app.get('/x', function () {` callback-body fold — regression test pins this). JS extension list extracted/exported so tests assert the real config. Fold all / Unfold added to Loki's Buffer rail (desktop-only, matching the existing Clear; mobile keeps the gutter). Separately, owner reported the **theme picker being clipped by the editor's find panel**: CM's base theme stacks `.cm-gutters` 200 / `.cm-panels` 300, which leaked into the page context and beat the sticky header (z-10) — `.theme-menu`'s own z-60 could never escape its parent context. Fixed with **`isolation: isolate` on the shared `.json-editor`**, chosen over raising the header because it covers the whole class (Heimdall modal, `.variant-rail__popover`, `.tag-suggest`, Loki mobile sheet) and Runestone/Variant/Edda inherit it. Gate green (lint/typecheck/**379 server + 321 client**/build). **Live-verified** (CDP, built server on scratch port 4747 + scratch `STORAGE_ROOT`, PM2 4646 untouched): with the owner's snippet loaded, `elementFromPoint` at the menu/panel overlap returns `.theme-menu`, and reverting **only** `isolation` at runtime returns `.cm-panels` and reproduces the report exactly (Slytherin clipped, Olympus half-covered); mobile 390×844 shows no overlap at all; fold arrows visible on both the `function e({` and `}) {` lines; clean SIGINT (`shutdown complete`, DB checkpointed). Owner has **not** hand-tested yet — real-device mobile check still manual.

- 2026-07-27 — **`npm run logs` fixed** on `fix/npm-logs-path` (owner-requested, found while tracing how logs reach Loki). The script tailed `storage/logs/app.log`, which **pino-roll never creates** — it writes `app.N.log` and symlinks `current.log` at the active file, so the designated no-Docker log fallback had been silently failing. Now `tail -F storage/logs/current.log`: **`-F` not `-f`**, because the symlink is repointed on every rotation (daily, or at 20 MB) and `-f` would keep following the stale inode. Three docs quoted the same wrong path (`observability.md`, `pm2.md`, `launchd.md`). Verified by rendering real lines through `pino-pretty`. Gate green (lint/typecheck/**379 server + 301 client**). This closes the pre-existing bug already logged as PLAN-16 Step 1 item and acceptance criterion 12 — **the plan file is left unticked** (not this PR's scope; 16a should claim it).

- 2026-07-26 — **PLAN-15 (Portkey) implemented** on `claude/plan-15-tp9uel` (uncommitted at write time, pending owner test). LAN go-links: new **local-profile-only** `portkey` module + `portkeys` table (migration `0008`, additive; **`slug` IS the primary key** — a user-chosen memorable word, immutable, rename = delete+recreate). API: `GET/POST /api/portkey`, `PATCH`/`DELETE /api/portkey/:slug`, and the redirect **`GET /go/:slug` registered outside `/api/`** so `bifrost.local/go/router` is the whole address (a real route → wins over the SPA fallback). **Always 302, never 301** (+ `cache-control: no-store`) — a router IP/NAS moves and a 301 pins it in caches. The **hit count is bumped out of band** (scheduled after the redirect is flushed via `setImmediate`, guarded against shutdown, failure is a warn) so a slow write never delays the hop, then a dedicated **`portkey.hit` → SSE** (deliberately **not** audited, unlike `portkey.saved`/`deleted`) updates open management pages live. Unknown slug → **creative-404 bounce** to `/portkey?go=<slug>` so the "enchant it now" form arrives pre-filled (the Runestone/Edda move). New **core `reserved-roots.ts`** (single source of the app's path roots + `api`/`go`, guard-tested) — a slug matching one 422s; slugs are `[a-z0-9-]{1,32}` lowercase kebab (no leading/trailing dash); targets are **http(s)-only, any host** (`javascript:`/`file:`/`data:` → 422). Client: feature-local `features/portkey/` (api · slug mirror · `usePortkeys` live SSE list · `PortkeyPage` — `/go/`-prefixed slug input with live validation, target+note, search, card grid with per-link **QrCard**, inline edit, confirm-delete, hits/last-used, 409 "already enchanted → View it") + its own `portkey.css` (nimbus precedent); capability-gated **card on Diagon Alley** + NAV match; `/go` added to the Vite dev proxy. Small additive core change: `ApiError` now carries the server's `{error,message}` so the 422 shows the specific reason. Gate green: lint/typecheck/**414 server** (+51: slug/target corpus, reserved-roots guard, usecases incl. resolve-vs-record-hit ordering + hit event, integration 302/no-store/creative-404/route-precedence/409-422 matrix, **kill test** proving no torn rows) **+ 306 client** (+8 slug helpers)/build. **Live-verified** on the built server: create 201 → `/go/router` **302 to `https://192.168.1.1/` with `no-store`** → hit count reached 1 async with `last_used_at` set; unknown slug 302→`/portkey?go=…`; duplicate 409; reserved `go`/`api` + `javascript:` 422; SIGINT → restart → portkey + hits **survived**, redirect still 302; fresh-DB bootstrap `integrity_check ok`, `portkeys` table has all 7 columns; `/portkey` serves the SPA and the `PortkeyPage` chunk code-split cleanly. (Browser-driven UI check skipped — Playwright isn't a project dep; the page mirrors the merged Accio grid and passed typecheck/build.) Docs synced (architecture registry + data flow, project-structure, decision row).
- 2026-07-25 — **npm audit swept 17 → 6 advisories** on `fix/npm-audit-advisories` (owner-reported `npm install` warning). Fixed forward inside existing carets (lockfile only): **`@fastify/static` 10.1.0→10.1.2** — the one advisory on a real runtime surface (authorization bypass via non-canonical URL paths + route-guard bypass via path traversal; this is what serves the SPA) — plus `find-my-way` 9.7.0 (HTTP/2 DoS, unreachable: HTTP/2 is never enabled), `fast-uri` 3.1.4, `postcss` 8.5.23, `concurrently` 10.0.4 / `shell-quote` 1.9.0. The **eslint cluster's five highs turned out to be one root advisory** (`brace-expansion` DoS via eslint 9's `minimatch@3`), closed with a root `overrides: {"brace-expansion": "^5.0.8"}` instead of the `eslint@10` major npm proposes — keeping the 2026-07-12 pin (typescript-eslint 8 targets ESLint 9). **Remaining 6 are accepted and logged**: `react-router` RSC-mode CSRF (unscored, and the app uses only `BrowserRouter`/`Routes`/hooks — no data router, loaders, actions or RSC; no patched 7.x exists, so npm's fix is a *downgrade* and the forward path is the v8 major) and the 4 moderate drizzle-kit→esbuild-kit rows accepted back on 2026-07-12 (0.31.10 is already latest). Gate green (lint/typecheck/**379 server + 301 client**/build). **Live-verified** the static-serving bump on the built server (port 4747): index/asset/`manifest.webmanifest` (`application/manifest+json`)/favicon all 200 with correct types, SPA fallback intact, and **10 traversal probes** — `/../.env`, `/%2e%2e/.env`, `/..%5c.env`, `/..\.env`, `/assets/../../.env`, `/%252e%252e/.env`, `/storage/data/app.db` — every one 403 or the 2458-byte index.html, **zero secret bytes** in any response; the download endpoint's own guard still 403/400s its traversal ids; all routes healthy after the router bump; clean SIGINT, `integrity_check ok`, PM2 untouched.
- 2026-07-25 — **PLAN-13 (Accio) closed out and PLAN-14 (Nimbus) implemented** on `feat/plan-14-nimbus` (uncommitted, pending owner test). PLAN-13 flipped to `done` (PR #35 merged 2026-07-25) and the plans/README gate row ticked. **Nimbus** = the LAN speed test: new local-profile `nimbus` module + `nimbus_results` table (migration `0007`, additive — one row per *complete* test, device-grouped, pruned on the audit retention policy). The measurement is split on purpose — **the server moves bytes, the client owns the clock**. `GET /ping` is a bodyless `no-store` 204 (client takes the **median of ten**; a mean would report a round trip that never happened); `GET /down?mb=` streams a window onto one 4 MiB random pool generated at boot, one chunk per `_read` so socket backpressure paces it, with `content-length` + **`content-encoding: identity`** + `no-store`; `POST /up` takes the **raw stream in a custom octet-stream parser that counts and discards** — no file, no buffer — and refuses over-cap bodies with 413 from an `onRequest` hook before reading a byte. A pure, clock-injected **`TestGuard`** holds a lease per device key across a whole test (warmup → ping ×10 → down → up), renews it per request, and frees it 5 s after the last one ends (`reply.raw.on('close')`, so an abort counts as an end) — a second device gets **409 "another broom is flying"**, `POST /release` hands it back at once, and **ping is deliberately ungated** so a busy bridge stays reachable. Client: pure `metrics.ts` (median · Mbps · 3-sig-fig formatting · sparkline points · spread%), an injected-transport `runTest.ts` orchestrator (warmup untimed both ways, download timed **from the first chunk**, upload figure taken from the **server's** ms while XHR `upload.onprogress` drives the live gauge), and a mobile-first `/nimbus` page — 88px live gauge, a 🧹 flying along a progress track, three-big-numbers result card, per-device history with sparkline + "last 3 within N%", on-page approximate-by-design note, 409 state, cancel. Capability-gated **2nd card on Diagon Alley** (`card-tone-2`, positional per the palette rule) + new `GaugeIcon`; CSS in its own `features/nimbus/nimbus.css` (screensaver precedent). Two endpoints beyond the plan's table, both justified: `GET /config` (so the size menu comes from `NIMBUS_MAX_TEST_MB`, never hardcoded) and `POST /release`. Gate green: lint/typecheck/**379 server** (+42 nimbus: pool cycling/compression-proof stream, guard state machine, usecases, integration byte-count+headers/413/409/results/audit, **kill test**) **+ 301 client** (+31 metrics + orchestrator incl. cancel/busy/never-save-a-partial)/build. **Live-verified** on the built server (port 4747, PM2's 4646 untouched): upgrade-path boot on the real DB → health 200 → SIGINT → restart → 200, `integrity_check ok`, history intact; fresh-DB bootstrap 200 + `ok`. Over HTTP: a 5 MB download arrived as **exactly 5 242 880 bytes with `content-encoding: identity` even when curl asked for gzip**, and gzipping the payload *grew* it (incompressible); with a 100 MB transfer in flight a second device got 409 on both directions while ping stayed 204 and `/config` reported the holder; 101 MB post → 413; **five consecutive 100 MB uploads left non-log storage byte-identical and RSS plateaued at ~133 MB** (no growth per test). In headless Chrome with **CDP network shaping** (the honest test — loopback finished in <250 ms): 40/12 Mbps → the page reported **↓40.0 / ↑11.9 Mbps**, 30/10 with 60 ms latency → **↓30.0 / ↑9.95 / 62 ms**, 60/20 in dark Aurora → **↓58.3 / ↑19.8**; all four phases observed with the broom advancing 0→100% per phase; **cancel mid-download** returned to Fly, saved nothing, and left the guard free; an untouched second tab gained the new reading **over SSE**; mobile 390×844 had a 308×52px thumb CTA and zero horizontal overflow. **One real bug found and fixed live**: the gauge's null placeholder rendered as a giant em-dash bar (an 88px "—" reads as a broken element) and sat there for the whole latency phase — the placeholder now has its own muted `--idle` size and the ping loop publishes a **running median** from the first round trip. Smoke rows (12 results + 12 audit rows) deleted from the owner's DB afterwards; docs synced (architecture registry + data flow, project-structure, 8 decisions).
- 2026-07-25 — **PLAN-12 (Loki) closed out and PLAN-13 (Accio) implemented** on `feat/plan-13-accio` (uncommitted, pending owner test). PLAN-12 flipped to `done` (PR #30 merged 2026-07-24), plan archived to `completed/`, README gate row ticked. **Accio**: new local-profile `accio` module + `accio_links` table (migration `0006`, additive — 6-char id, no slug; `tags` as a JSON array) with `GET/POST /api/accio`, `PATCH`/`DELETE /api/accio/:id`, and `accio.saved|updated|deleted` → SSE + audit (`updated` deliberately not audited). Pure `url.ts` normalizes and validates (http/https only, `javascript:`/`data:`/`mailto:` etc. → **422**; scheme-less input gets `https://`; `localhost:3000/x` stays a host+port; IDN → punycode; trailing-junk trim) and pure `title.ts` extracts `<title>` (entities, head-vs-svg, truncated downloads, 200-char cap). **Title enrichment never blocks a save**: the 201 returns the instant the row exists, then the module (off the bus) runs a capped, retried `HttpTitleFetcher` (`ACCIO_TITLE_TIMEOUT_MS`/`ACCIO_TITLE_MAX_BYTES`) and patches via `accio.updated`; a user edit/delete mid-flight wins; shutdown awaits in-flight lookups. Client: shared `core/accio.ts` client (in core so Hermes can use it), `/accio` shelf — paste-and-go bar, hostname letter tile (palette slot from a **hostname hash**, a logged scoped exception to the positional card rule), title/tags/relative time/author, tag chips + search that compose, sort, inline edit, confirm-delete, "Nothing summoned yet" empty state, live via SSE. **Hermes gained "Accio it"** on URL-bearing text entries (via the core client — no cross-feature import), and Accio is the **4th Midgard portal** beside Hermes (owner's pick), capability-gated. **Owner round 2 — shelf UI revamped from list to grid:** the one-per-row list wasted a desktop's width and forced a long scroll (a bookmark page fills up fast). Now a **full-width breakout at ≥1024px** (Variant/Runestone/Edda precedent, `min(105rem, 100vw − gutters)`) holding a `repeat(auto-fill, minmax(16rem, 1fr))` **card grid** — 5 columns at 1600px, 4 at 1280, 1 on a phone, no breakpoints of its own. Each card is ~126px: colour tile + 2-line-clamped title + host · time · author + tag chips, with the **whole card as the click target** (stretched-link `::after`, disabled while editing) and copy/edit/delete as **corner icon buttons revealed on hover or focus** (always visible on touch). Cards in a row share a height, tags sink to the bottom. The filter bar gained a match count and **sticks below the header ≥768px** (not on phones, where it would eat a third of the viewport); it is styled as a slim frosted panel — the shell header's own `--header-veil` + `blur(14px)` + border/radius — after a first pass filled it with flat `--bg`, which painted a black slab over the gradient sky. Sticking it also needed a real offset: a new `--header-h` (3.875rem, the header's measured 62px) so it parks under the header instead of behind it. Measured: **15 links fit one 1600×1000 screen with no scrolling**, where 3 filled it before. Live-verified in dark theme at 1600/1280/390 — no horizontal overflow at any width. 

**Owner round 1 (same branch):** (1) **any scheme is now shelf-able** — `chrome://chrome-urls/`, `chrome://net-internals/#dns`, `about:config`, `mailto:`, deeplinks — with **no per-scheme handling**: `normalizeUrl` blocks only what executes inside a page (`javascript:`/`data:`/`vbscript:`/`blob:`/`filesystem:`) and every row renders as an ordinary `target="_blank"` anchor for the browser/OS to resolve. Opaque hosts are lowercased so one page is one row, fragments preserved, and **title enrichment is skipped for non-http(s)** (nothing to fetch). _(First attempt shipped a curated browser-scheme allow-list plus badged copy-me rendering; the owner rejected it as over-engineering and it was removed — see the superseding decision-log row.)_ (2) The **save error clears on the next keystroke** in the URL field. (3) The tags field gained a **suggestion list** built from the tags already on the shelf — click/focus shows all, typing narrows on the fragment after the last comma, picking completes it and stays open (custom, not `<datalist>`, which can't see past the first comma). Gate green: lint/typecheck/**337 server** (+60 accio: url corpus incl. browser schemes, title extraction, usecases incl. enrichment races + the never-fetch-a-browser-page guard, integration CRUD/422/schema/tag-exactness/audit/browser-pages, real-HTTP enrichment, **kill test**) **+ 280 client** (+28 shelf + tag-input helpers)/build. **Live-verified** on the built server (port 4747): upgrade-path boot on the real DB → health 200 → SIGINT → restart → health 200, `integrity_check ok`; fresh-DB bootstrap also 200 + `ok`; save/422/tag+q filtering exercised over HTTP (smoke rows deleted afterwards). **UI live-verified** (headless Chrome via CDP, 1400px): every row — `chrome://chrome-urls/`, `mailto:`, https — renders as a real anchor carrying its exact href, no special-cased markup left; a rejected `javascript:` save shows the error and the **next keystroke clears it**; clicking the empty tag field lists `browser/later/reading`, typing `re` narrows to `reading`, picking it fills `reading, ` and leaves `browser/later` offered; `example.com` got its real title ("Example Domain") while every `chrome://`/`about:` row stayed null — zero runtime errors. PM2 `bifrost` was found **already stopped** at session start and left as found. Docs synced (architecture module registry + a data-flow entry, project-structure, 7 decisions).

- 2026-07-25 — **Screensaver renamed Vörðr → Nótt** (owner pick; Norse goddess of night; tagline "night over the bridge"). Display strings + `.nott*` CSS + Heimdall `NottSection`/`nott-*` ids + comments renamed; functional ids (`screensaver` module, `/api/screensaver/*`, `features/screensaver/`, `--screen-*` tokens) unchanged. (Earlier same-day: ripple-slowdown, a 4th depth layer + density bump, and a depth/porthole pass — focus vignette + real backdrop-blur rim, per-particle size jitter, twinkle pulse.) Gate green (lint/typecheck/**252 client**/build); live-verified no runtime errors.
- 2026-07-25 — **Vörðr owner tweaks (same branch, uncommitted):** (1) default idle **60s→5s** for testing (floor lowered 10→5 across config schema/overlay/module + Heimdall dropdown gains a 5s option + `.env.example`); (2) **denser field + a "look-around camera"** replacing the small parallax — the particle field is now a **world larger than the viewport** (padding = max per-depth pan), counts scaled up (bands 90/130/180, ×~3.6 world-area factor) with a weighted depth mix (mostly far stars, few near suns); moving the cursor eases a **per-depth camera pan** (`DEPTH_PAN` far 0.55 / near 1.7, signed-square `easePan`, `CAMERA_EASE` momentum) so turning toward an edge scrolls the sky and reveals new stars/suns, off-screen particles culled. Engine rewrite is pure/testable (world bounds + wrap + `easePan` unit-tested; dropped the old in-radius attraction). Gate green (lint/typecheck/**250 client**/build). **Live-verified** (CDP, idle 5s): overlay mounts (brand + live clock + random quote), cursor panned to all four edges with **zero runtime errors**, PM2 4646 intact. Pixel screenshots still blank (headless canvas limitation) — visual density/pan feel is owner's to judge.
- 2026-07-25 — **Idle screensaver "Vörðr" implemented (owner-requested, off-plan; uncommitted, pending owner test)** on `feat/house-themes-and-portal-cards`. A desktop-only full-screen particle constellation (2–3 depth layers, drift + cursor parallax, far-layer constellation lines, 30fps-capped single RAF) that appears after a configurable idle timeout, with a branded header + **live clock** and a **random lore quote** (new `assets/quotes.ts` bank — HP/MCU/Ghibli/Norse/Greek, seedable `pickRandomQuote`) floating above. **Dismissal = owner's model**: a **click** lands a ripple then fades out, a **keystroke** dismisses; `mousemove` only pushes the particles ("space" feel), never dismisses. **Never initialized on phone/tablet** (`isDesktop`: `(hover:hover)+(pointer:fine)` + `maxTouchPoints<=0` + width≥1024). New **policy-only `screensaver` server module** (both profiles) mirroring Loki: env-seeded config (`SCREENSAVER_*`), public `GET /api/screensaver/config` + admin `PATCH /api/screensaver/settings` + `screensaver.settingsUpdated`→SSE; new **Heimdall "Screensaver" card** (Realm) with enable/idle/density/motion/lines/mouse/quotes/rotation, live-rebinding. If the Heimdall modal is open when idle fires, App **closes it first**. Colours come from theme tokens: new optional **`--screen-*`** group (schema `SCREENSAVER_TOKENS`, tokens.css dark+light defaults, on-brand overrides in all 4 identity themes; `readColors` reads them at draw time like QrCard), documented in **THEME-SPEC** + the **theme SKILL** (calm-veil bar). `prefers-reduced-motion` → static field. Overlay lazy-loaded (own 9.4 kB chunk). Gate green: lint/typecheck/**277 server** (+4 screensaver int tests, capabilities list updated) **+ 249 client** (+19 particles/ripple/isDesktop/quotes)/build. **Live-verified** (built server on alt port 4747 + temp storage so PM2 `bifrost` on 4646 stayed intact; headless Chrome via CDP): after 10s idle the `.vordr` canvas mounts with brand "Vörðr", live clock, and a random quote (Thanos/Stark/Delphi across runs); a synthesized **click removed `.vordr`**; a **390-width viewport never mounted it** (`hasCanvas:false`). Note: pixel screenshots came back blank (headless canvas-capture limitation) — the visual aesthetic is the one item left for owner interactive test. (Mid-run I accidentally `pkill`ed the PM2 `bifrost` once; restored via `pm2 restart` → online/health 200. Lesson: kill smoke servers by saved PID, not `pkill -f`.)
- 2026-07-24 — **Theme work (owner, uncommitted): two new house themes + card-palette system.** Added `themes/gryffindor.json` (warm hearth gold+scarlet, iterated to fix a "too bright / bloody" first pass by darkening surfaces + deepening accents) and `themes/slytherin.json` (cool under-the-lake emerald+silver); also calmed the shipped **ghibli-dusk** built-in the same way. Then a **card-colour system overhaul**: cards no longer borrow the atmospheric `--tone-teal/violet/amber` (which collapsed to gold-on-gold in warm themes, making Variant≈Edda and Receive≈Hermes identical). New **10-slot palette** `--card-1..10` (schema `CARD_TONE_TOKENS`, tokens.css dark+light defaults, per-theme overrides in all 4 identity themes), class `.card-tone-N` sets `--ct`, tint+glow via `color-mix`; hub + portal cards share it. Cards coloured by **position, per page** — each grid is a data-driven array rendered with `cardToneClass(index + 1)` (`core/ui/cardTone.ts`, wraps after 10); reordering a card recolours it, no hand-picked colours / literal class strings / fixed numbers. **Loki folded into the palette** (removed the bespoke `.hub-card--loki` animated card + card-only `--tone-loki-2`; kept `--tone-loki`/`--glow-loki` for the Loki tool page). Also added a **`theme` project skill** (`.claude/skills/theme/`) codifying the contract + the calm-not-glaring bar + card palette + validate/restart steps. Then **unified the two card families into one `Portal` component** (`core/ui/Portal.tsx`, class `.portal`, grid `.portals`) used on all three hubs — removed the old `PortalCard`/`.hub-card` split; every card now carries a **footer tagline** (`go`) naming where it leads (e.g. Runestone → "carve the runes"); `soon` renders a dimmed non-nav card + badge. Join Bifrost stays its own band (not a portal); generic `Card` panel kept (used by many feature pages). Docs synced (THEME-SPEC card group, DESIGN cards section, rules/coding.md, decisions.md, 2 persistent memories). Gate green (lint/typecheck/**273 server + 230 client**/client build). **Needs server restart + client rebuild** to view (schema + tokens changed). Uncommitted pending owner test.
- 2026-07-24 — **PLAN-11 (Edda) merged** (PR #29 → develop, 2026-07-23) and flipped to `done`; plan file archived to `completed/PLAN-11-edda.md`; plans/README gate row checked off. **Gate cleared — starting PLAN-12 (Loki)** on a fresh `feat/plan-12a-loki-transforms` branch.
- 2026-07-24 — **PLAN-12 Loki output-panel alignment (owner)** — the output box wrapped its own header inside the border, so it sat ~40px higher than the code editor box. Restructured (CSS only, `app.css`): the output header now floats above an unstyled panel, the border/background moved to `.loki-output__body` which is pinned to the editor height (`flex:none; height: var(--loki-editor-h)` ≥1024, since `flex:1`'s 0% basis collapsed it), both heads share a fixed 2.75rem height, and the output column gets the same head→box gap as the code pane. Live-verified (CDP 1440px): editor box and output box now share top (279) and bottom (~828). Gate green (lint/typecheck/230 client).
- 2026-07-24 — **PLAN-12 Loki Part B — owner feedback round 2** (output panel layout, uncommitted). The Calcifer output moved from **below** the editor to a **right-hand panel** in a new `.loki-workarea` (code pane | draggable `.loki-divider` | output), default **20vw**, pointer-drag resize with **both panes clamped ≥ 20vw** (20% of screen); stacks below the editor <1024px (divider hidden). The panel now appears **only on Run** (not on any output state), has a **Hide** control, and a **"Show output"** button surfaces in the editor header **only after the first run** — clicking Run while hidden re-reveals it (`showOutput`/`everRan` state). The A−/A/A+ control now also sizes the output text (`.loki-output__body` uses `--panel-font`). Gate green (lint/typecheck/273+230). **Live-verified** (CDP, 1440px): pre-run no panel/no Show button; after Run the output sits right at exactly 288px (=20vw) with a divider; Hide→panel gone+Show appears; Show→back; Hide+Run→reappears; A+ grows output font 14→16px; divider drag widens the output and clamps the code pane at 20vw.
- 2026-07-24 — **PLAN-12 Loki Part B — owner feedback round 1** (uncommitted, on 12b). (1) **Run now shows the completion value of the last statement**, not only a lone expression: new `wrapLastExpression` (core/js, acorn) rewrites a trailing top-level ExpressionStatement to `return (…)` on the main thread before the worker runs it, so `…; someCall()` shows `someCall()`'s value (the reported `getOrderTypeDetailsText(...)` case now prints its JSON string). The worker dropped its own single-expression heuristic — it just runs the body and reports the return. Line numbers stay aligned (rewrite starts at the statement offset). (2) **Loki buffer no longer goes stale after Diff→Variant→back**: the draft was read from a **module-level** const (evaluated once), so a remount restored an old buffer; now it's read **per-mount** (a ref), `diffInVariant` flushes the draft synchronously before navigating, and an unmount effect flushes the latest too. Verified via a true client-side `history.back()` (react-router popstate): back at /loki the current transformed buffer is restored, not the old one. Gate green (lint/typecheck/273+230, +8 wrapLastExpression tests). Live-verified both.
- 2026-07-24 — **Find-panel match counter (owner request)** on the shared `core/ui/JsonEditor` (uncommitted, on the 12b branch). A `searchMatchCount` ViewPlugin injects a small "N of M" / "M found" / "No results" label into CM6's default find panel (it has none), computed via `SearchQuery.getCursor` (capped at 1000) with the current index resolved from the selected match; kept in sync on query/selection/doc changes. Since every panel mounts this one editor, **all find menus inherit it** — Runestone (JSON), Variant (JSON + text), Loki (JS), Edda (markdown). Themed via a `.cm-search-count` rule in `editorChrome`. **Live-verified** (CDP): Runestone "role" → "4 found" on commit, then "1 of 4" → "2 of 4" tracking CM's selected match; Loki/Edda/Variant-text all show "3 found" → "1 of 3". Gate green (lint/typecheck/273+222).
- 2026-07-24 — **PLAN-12 (Loki) Part B "Calcifer" implemented** on `feat/plan-12b-loki-execution` (stacked on 12a, uncommitted, pending owner test). Sandboxed execution via an **ephemeral module Web Worker** per run (`loki-runner.worker.ts`, Vite-bundled to its own chunk): installs a console shim with **in-worker budget** (default 500 entries, ~8 KB/entry truncation), runs via **AsyncFunction** (top-level await; single-expression detection wraps `return (…)` for a REPL value, falls back to statements), and posts back pre-**serialized** values (`serialize.ts` — cycle-safe, depth-limited, explicit undefined/BigInt/Symbol/ƒ/Error/Map/Set/Date; corpus-tested). Main-thread `LokiRunner` owns spawn → **watchdog** (config timeout) → **Stop** → `terminate()`, both kill paths just terminate, `runId` drops stale messages. `OutputDrawer` shows level-coloured entries, the `⇐ result` line, thrown errors + collapsible stack, truncation markers, and duration. Run UI ("🔥 Run"/Stop, the Calcifer flame) gated on **`capabilities.profile === 'local'` AND the Heimdall master switch**. Server: the `loki` module gained a **public `GET /api/loki/config`** + admin **`PATCH /api/loki/settings`** (executionEnabled · fetchAllowed · runTimeoutMs · consoleMaxEntries), settings-table backed (env seeds `LOKI_EXECUTION_ENABLED`/`LOKI_FETCH_ALLOWED` + overlays), broadcasting a new `loki.settingsUpdated` bus event → SSE so open pages rebind live. New **Heimdall "Loki" card** (Realm group) with those four controls (apply-on-change). Gate green: lint/typecheck/**273 server** (+4 loki int tests) **+ 222 client** (+10 serializer tests)/build (worker chunk emitted). **Live-verified** (built server, port 4747, CDP): console streaming + object serialization; `40+2` → `⇐ 42`; thrown error + stack; `fetch('/api/health')` returns with the toggle on, **blocked** ("fetch() is disabled by Heimdall") with it off; `while(true){}` **killed by the 5 s watchdog while the main thread stayed responsive** (2+2 evaluated mid-loop); **Stop** kills mid-run; a 1200-log loop truncates at 500 + "…and 700 more"; master-switch-off **hides Run live via SSE** (no reload) + shows the "turned off in Heimdall" note. Clean SIGINT; PM2 `bifrost` on 4646 untouched. **Deferred (Part-A polish, still open):** the Part-A-only Heimdall defaults (beautify/regex/font/mode) + a per-run timeout control + a stringify quote-style selector.
- 2026-07-24 — **PLAN-12 Loki Part A — owner feedback round 1** (same branch, uncommitted). (1) **Loki buffer now survives navigation** (the Diff→Variant→back data-loss bug): new `features/loki/draft.ts` caches the whole workspace (code + regex fields + mode) in localStorage, restored on mount + saved debounced. (2) **Minify no longer prepends `!`** — Terser `compress.negate_iife:false` in `core/js/format.ts`. (3) **Stringify is guarded** — new `isSingleStringLiteral` (core/js); tapping Stringify on an already-quoted buffer is refused with "use Destringify" instead of nesting. (4/5) **Shared panel controls** — new `core/panelFont.ts` hook (`--panel-font`, per-device localStorage `bifrost.editorFontPx`) + `core/ui/PanelControls` (`PanelFontControl` A−/A/A+ and `UndoRedoControl`), scoped via a `.panel-scope` wrapper (`core/ui/ui.css`); `JsonEditor` gained `undo()`/`redo()` handle methods and now **isolates each `applyEdit` as its own history group** (`isolateHistory.of('full')`) so one transform = one ⌘Z. Wired into **Loki** (rail + mobile sheetbar), **Runestone** (toolbar), and **Variant** (font control in head, undo/redo per pane). Removed Loki's old bespoke `--loki-panel-font`/`.loki-fontctl` in favour of the shared ones. Gate green (lint/typecheck/**269 server + 212 client**/build). **Live-verified** (built server, port 4747, CDP): draft restore across /loki→/variant→/loki; minify of `!function(){…}()` → `console.log(1);` (no `!`); Stringify twice → refused + notice; undo reverts only the transform (typing kept), redo re-applies; Runestone shows 3 font + 2 undo/redo btns, Variant shows head font + 4 pane undo/redo. Clean SIGINT shutdown; PM2 `bifrost` on 4646 untouched throughout.
- 2026-07-24 — **PLAN-12 (Loki) Part A first build** on `feat/plan-12a-loki-transforms` (uncommitted, pending owner test + UI checkpoint). New pure `core/js/` toolkit (all `eval`-free, 104 unit/property tests): a literal-aware `scanJs` lexer → `stripComments` + `convertQuotes`; `stringifyJs`/`destringifyJs` (round-trip property), HTML/URI escapes, `wrapIife`/`unwrapIife` (round-trip property), `curlToFetch` (tokenizer + unsupported-flag reporting), `jsonToJs` (demo D/E, JSON→JS→value identity property), and lazy `beautifyJs` (Prettier standalone) / `minifyJs` (Terser) / `checkJsSyntax` (acorn) — all three heavy deps **new + code-split** into their own chunks. Shared `JsonEditor` gained a **javascript mode** (`@codemirror/lang-javascript` + `--syn-*` tint, bracket-close, reusing `applyEdit`). **Runestone toolbar** got **"Copy as JS"** (enabled on Valid JSON ✓). New capability-only `loki` server module in **both** profiles (+ `LOKI_RUN_TIMEOUT_MS`/`LOKI_CONSOLE_MAX_ENTRIES` env for Part B); nav card = 4th Ollivanders card with a **new emerald `--tone-loki`** + a deliberately distinct **dual-hue diagonal shifting sweep** `.hub-card--loki` (reduced-motion-safe), dark+light. **LokiPage**: `Transforms | Regex` mode toggle (separate per-device-persisted workspaces), full-width breakout ≥1024, grouped rail (Format · Strings · Convert · Clean) with mobile **bottom-sheet** chips, acorn syntax banner, stats + minify before→after bytes, per-device **panel-font A−/A/A+** (`--loki-panel-font`), and a **Variant "Diff before/after"** hook via a new boundary-legal `core/variantSeed` sessionStorage bridge (Variant reads it once on mount → text mode). Gate green: lint/typecheck/**270 server + 209 client**/build. **Live-verified** (built server on an alt port 4747 so the owner's PM2 `bifrost` on 4646 stayed online + untouched; headless Chromium + CDP): `loki` in `/api/capabilities`; Ollivanders shows the emerald shifting card; type JS→"Parses cleanly"; "To ' quotes" literal-aware; Beautify (lazy Prettier) reformatted with singleQuote+semi; Regex `(\w)(\d)` on `a1 b2 c3` → 3 matches + 3 rows + 3 in-editor highlights; mobile 390 collapses the rail to group chips; SIGINT shutdown clean. **Deferred to post-checkpoint (Part A polish)**: the Heimdall "Loki" settings card (using client defaults for now) + a stringify quote-style selector (defaults to double). Part B (Calcifer sandbox) is the next PR.
- 2026-07-21 — **PLAN-12 (Loki)** authored after converter demo (JSON5 + acorn-diagnostics architecture live-proven, round-trip identity verified): Part A transforms (Prettier/Terser lazy, pure `core/js` utils, cURL→fetch, strip-comments standalone; JSON→JS only, surfaced as Runestone's "Copy as JS" — JS→JSON dropped by owner) + `Transforms|Regex` mode toggle; Part B killable-worker execution ("Calcifer", local-only, Heimdall master switch); panel font A−/A+ (per-device) + Heimdall Loki settings card; base64 excluded (toolbox owns it), snippet library skipped.
- 2026-07-23 — **PLAN-11 (Edda) implemented** on `feat/plan-11-edda`. New `edda` server module + `eddas` table (migration `0005`, additive; runestone's id/slug/301 scheme; markdown validates only the size cap — empty docs allowed) with CRUD + public `GET /edda/api/:slug` (raw `text/markdown`, CORS `*`, 301, `?download=1`), reserved-segment guard, `edda.saved`/`edda.deleted` → SSE + audit; module in **both** profiles. Client: shared `core/markdown/` (renderMarkdown marked-GFM+hljs+heading-ids+DOMPurify · outline · stats · pure toolbar commands, all unit-tested), `JsonEditor` gained a **markdown mode** (`@codemirror/lang-markdown`, `--syn-*` tint) + `applyEdit` minimal-change handle; three surfaces `/edda` (editor; mobile opens Preview) · `/edda/preview/:slug` (public render) · `/edda/api/:slug`. Perf: 200ms debounce in rAF, auto-manual over `EDDA_LIVE_PREVIEW_MAX_KB` (300) + pinned-manual toggle, **mobile preview unmounted while editing**; full-width breakout ≥1024 with draggable divider, stacked ≥768, single-pane <768; toolbar+shortcuts (⌘B/I/K), outline panel, approximate scroll-sync (toggle), stats bar, `.md`/`.html` export, scratch-draft restore, creative 404, coming-soon footer. Wired the Ollivanders card + capability nav (`edda` in `match`/`modules`). New dep `@codemirror/lang-markdown`; test-only `jsdom` (client) for DOMPurify sanitization tests (happy-dom couldn't drive attribute stripping). **Deviations**: scroll-sync percentage-only (approximate-by-design, no heading-anchor correction — logged tarpit); editor find inherited from the shared editor (already shipped) rather than re-implemented. Gate green (lint/typecheck/**269 server + 161 client**/build). **Live-verified** (built server + headless Chromium, desktop 1400 + mobile 390): live preview renders GFM/tables/hljs and `<script>` is inert on editor+public surfaces; type→save→open-from-library→public preview→raw-API round-trip; mobile `/edda/:slug` opens in Preview, Edit unmounts `.md-preview`, toggle-back remounts. Restart smoke + fresh-DB bootstrap both `integrity_check ok`. Uncommitted pending owner test.

- 2026-07-22 — **Favicon + PWA support** on `claude/favicon-pwa-support-o9p8gr` (owner-requested; site had no favicon and wasn't phone-installable). Added `client/public/`: `favicon.svg` + `favicon-16/32.png`, `apple-touch-icon.png` (180), `pwa-192/512` (`any`) + `pwa-maskable-192/512` (`maskable`), and `manifest.webmanifest` (standalone, `#0b0e14`). `index.html` head gains icon/manifest links + Apple web-app meta. **No service worker** (LAN tool, no offline need). Icons drawn by a one-off Node PNG generator (no raster tooling in env); **artwork iterated live with the owner** to the chosen **"Pure Arch"** — a glowing rainbow-bridge arch (teal→deep-purple→green) between two lit realm-nodes over a near-black starfield + vignette, deliberately dark with no white strip. Gate green (lint/typecheck/**245 server + 132 client**/build); **live-verified** on the built server — `/favicon.svg` (image/svg+xml), `/manifest.webmanifest` (**application/manifest+json**), apple-touch + pwa PNGs all 200, index head carries the links. Docs synced: `tech-stack.md` (PWA no longer deferred), `decisions.md`, this log.
- 2026-07-22 — **v1.1.0 released** (develop→main, PR #24) — plans 07–10 + editor find/tree-collapse/Runestone-revamp + category nav/Home redesign/tone cards + refreshed README screenshots (PR #23). Tag `v1.1.0`, [GitHub Release](https://github.com/0xSiddhant/bifrost/releases/tag/v1.1.0) with `bifrost-v1.1.0.tar.gz`, main==develop. **`RELEASE_TOKEN` PAT set** (Contents+Workflows RW). Hit two release-automation snags, both handled: (1) the first `gh secret set` stored an **empty** token → checkout failed; re-set with a real value; (2) `release.yml` used `git push --follow-tags` on a **lightweight** tag → the tag never pushed and Publish failed. Completed the release manually (pushed tag, published Release, back-merged) and **fixed the workflow** (push branch + tag as separate refs) on `fix/release-tag-push` → PR to develop. **PLAN-11 (Edda) is next.**
- 2026-07-21 — **Midgard redesign + card tone-gradients + docs sync** (same `feat/nav-categories` branch, owner feedback on the nav reorg). Fixed the stretched-empty Hermes/Join-Bifrost grid: **Hermes → third Home portal** (new **amber** tone `--tone-amber`/`--glow-amber`, dark+light) beside Send (teal)/Receive (violet); **Join Bifrost → wide horizontal onboarding card** (QR beside details, `core/ui/JoinBifrostCard`). Then extended the per-card **tone-gradient** to every hub card (`.hub-card--{teal/violet/amber}`, tones cycling, `--soon` dims). **Context-sync**: updated `context/project-structure.md`, `context/architecture.md`, `context/tech-stack.md` (@codemirror/search), `docs/DESIGN.md` (cards & tones, category-nav layout, breakout), `docs/THEME-SPEC.md` (amber tokens), root `README.md` (3-tab nav), + PLAN-11/PLAN-99 entry-point notes. Gate green (lint/typecheck/245+132/build); live-verified in dark theme (3 toned portals + horizontal join band on Midgard; toned hub cards on Ollivanders/Diagon Alley). Uncommitted pending owner test.
- 2026-07-21 — **Nav reorg → 3 category tabs** on `feat/nav-categories` (stacked on `feat/editor-find-tree-collapse`, owner-requested; PR based on #21). The flat 7-tab bar overflowed mobile (truncated labels), so tabs are now **Midgard** (home: Send/Receive/Hermes cards + the Join-Bifrost QR moved off Sigil into `core/ui/JoinBifrostCard`), **Ollivanders** (dev: Runestone/Variant/Edda-soon), **Diagon Alley** (toolbox: the QR maker "Make a QR" + Base64/UUID/timestamp coming-soon per PLAN-99). New app hub pages + `WandIcon`/`SparklesIcon`; category tabs light active on their tools' sub-pages (custom `useLocation` match); tabs gated by `/api/capabilities`; Sigil slimmed to the QR generator; bottom-nav labels wrap to 2 lines then ellipsis. Gate green (lint/typecheck/245+132/build). **Live-verified** (headless Chromium, mobile 390 + desktop 1280): bottom bar shows exactly `[Midgard, Ollivanders, Diagon Alley]` with full labels; `/runestone`→Ollivanders active, `/sigil`→Diagon Alley active; hub cards + Midgard Hermes/Join-Bifrost QR all render. Note: found the PM2 `bifrost` app **stopped** at start (not by this turn) — verified against a temporary `npm start` server, then restored PM2 to online (health 200).
- 2026-07-21 — **Editor find + tree collapse-all implemented** on `feat/editor-find-tree-collapse` (off develop, post-PLAN-10-merge; owner asked to build now rather than wait for PLAN-11). (1) **In-editor find** on the shared `core/ui/JsonEditor` via `@codemirror/search` (added to client deps): ⌘/Ctrl-F panel + `openSearch()` handle behind "Find" buttons (Runestone toolbar, each Variant pane header), token-styled, works in JSON + `plain` modes — so Runestone and both Variant panes get it; Edda inherits later. **Variant cross-pane reveal**: `onSearchMatch` → `crossPaneOffset` reveals the same string in the opposite pane (JSON + text), scroll-lock suspended during the reveal, no diff re-run. (2) **TreeView Expand all / Collapse all** (sticky control row; `openMode` + remount-by-key) folds the whole tree incl. the outer array — appears in Runestone + Variant. Gate green: lint/typecheck/**245 server + 132 client** (+4 cross-pane tests)/build. **Live-verified** (headless Chromium/CDP against the PM2 server serving the rebuilt `client/dist`): Runestone tree **Collapse all → 1 row / Expand all → 17 rows** (whole-tree fold incl. outer object); editor **Find** panel opens themed and highlights **3/3** `role` matches; Variant **cross-pane reveal** — searching a needle in the LEFT pane scrolled the RIGHT pane `scrollTop 0 → 1301` onto the buried match (both Find buttons present). Note: a local `npm start` couldn't bind 4646 (PM2 `bifrost` already owned it), and a stray SIGINT bounced the PM2 instance once (↺1, back online). **PLAN-11** updated to reflect Edda inheriting the now-shared find; PLAN-99 backlog row reverted. **Then (same branch) a Runestone page revamp** (owner feedback on the shipped find): fixed the find-panel theming bug (CM inputs are `.cm-textfield` with no `type=text`, so the old selector never matched → white boxes; now dark/theme-matched, verified bg `rgb(19,24,36)` + accent focus ring, fixes Variant too), and broke the editor+head out of the 62rem shell to `min(105rem, 100vw−gutters)` at ≥1024px with a viewport-filling height via `--rune-editor-h` — live-verified at 1600px: workspace 1536px wide, editor 664px tall, head aligned with the editor, tree Expand/Collapse controls intact. Gate stays green (245+132/lint/typecheck/build). Left **uncommitted** for owner testing.
- 2026-07-21 — **PLAN-10 tranche 2 (About + Logs)** on the same branch/PR #20. **About** section: `GET /about` (version/commit/buildDate/uptime/node/host/profile) with the stamp **baked at build time** (`scripts/gen-build-info.ts` prebuild → gitignored `server/build-info.json`, git/dev fallback) + `GET /changelog` serving `CHANGELOG.md` + `npm run changelog`. **Logs** section: `GET /logs` filtered tail from a new in-memory `core/logtap` the logger feeds via `pino.multistream`, `PATCH /logs/level` runtime pino level (persisted `log.level` → survives restart), and an admin-gated live tail (`GET /logs/stream`, dedicated `requireAdmin` SSE, per-send epoch/expiry recheck). Deviations logged: in-memory ring vs file tail, dedicated SSE vs public-hub gating, single-gate multistream. Gate: lint/typecheck/**245 server** + 128 client/build green + live smoke of all endpoints. **Remaining tranches**: Storage+Maintenance, Devices-admin+Modules.
- 2026-07-21 — **PLAN-10 tranche 1 (Heimdall Modal)** on `feat/plan-10-heimdall-modal` (PR #20). Converted Heimdall from a routed page to a **modal overlay**: deleted the `/heimdall` route + 404-lookalike + `heimdallGate`; viewport-gated entry (≥768px, wordmark taps + shortcut); footer-mark entry removed; existing content regrouped into searchable sections (Watchtower/Realm/Vault/Bridge) with jump-highlight (`search.ts` + test). **Owner changes during testing**: PIN required on every open (close/refresh re-lock — supersedes the 2026-07-16 relaxation); Wardens removed from main nav → Heimdall _Wardens_ section (`/wardens` route kept); **7-day stale-device prune** (`POST /api/presence/prune`, on Wardens open, best-effort GET fallback, activity preserved); Overview shows _connected_ + _known_ device counts, both from the presence roster (stats `connectedClients` now = distinct online deviceIds). Single Lock control, Heimdall brand in panel, PIN autofocus, badge-alignment fix. Gate: lint/typecheck/228 server+128 client/build green. **Remaining tranches**: About+Logs, Storage+Maintenance, Devices-admin+Modules. Full detail in [`history.md`](history.md).
- 2026-07-21 — **PLAN-09 (Ops) implemented** on `feat/plan-09-ops` (PR #19), all 8 items. **Tranche 1** (verified here): online-safe **backup/restore** (`core/backup` VACUUM INTO + zip storage/+themes/, rotation, `--include-env`; restore refuses a live server; thin CLI wrappers; 18 tests + real round-trip), scripted **restart-resilience suite** (`npm run test:resilience` — 50 SIGINT cycles + SIGKILL mid-write/mid-migration + tmp-sweep, all integrity-ok; full 50-cycle run green), **PM2** + **launchd** (`ecosystem.config.cjs`, `docs/pm2.md`, `docs/launchd.md`). **Tranche 2** (written; owner-verifies on hardware): multi-stage **Dockerfile** + compose + `docs/docker-linux.md`, optional **Grafana+Loki+Alloy** observability stack + starter dashboard + `docs/observability.md`, `docs/cloud-profile.md`, **release automation** (`release.yml` — semver-from-commits, changelogen, tag, GitHub Release + tarball, main→develop back-merge, loop guard; needs `RELEASE_TOKEN` PAT) + CI docker-build job + backup smoke (both verified locally). Added `BACKUP_KEEP`, `ops` commitlint scope, `*.cjs` eslint block. Gate: lint/typecheck/226 server+123 client/build green. Owner-manual: PM2 reboot, Linux docker run, live Grafana, PAT setup.
- 2026-07-21 — **PLAN-11 (Edda)** authored: markdown editor (shared CM6 editor gains markdown mode) + perf-budgeted live preview (debounce+rAF, manual-mode degradation over EDDA_LIVE_PREVIEW_MAX_KB, mobile unmounts preview), own `eddas` table/library, three share surfaces (`/edda/:slug` editor · `/edda/preview/:slug` public render · `/edda/api/:slug` raw+download), toolbar/outline/scroll-sync/stats/exports, coming-soon footer. Mermaid/image-paste/PDF parked in PLAN-99.
- 2026-07-21 — Variant invalid-JSON fix: a failed JSON Compare now stays in JSON mode and warns instead of switching to Text and clobbering the text workspace. Live-verified. Also on this branch: context/docs sync, this memory restructure, and two lore renames — **Muninn → Hermes** (`/hermes`, `features/hermes/HermesPage`) and **Mímir → Pensieve** (`/runestone/pensieve`, `PensievePage`); old routes 301-redirect; server module ids (`clipboard`, `runestone`) unchanged. Branch `fix/variant-invalid-json-preserves-text`.
- 2026-07-20 — **PLAN-08 (Variant)** implemented + 4 owner-feedback rounds → merged (PR #17): structural JSON diff + text fallback, editable panes in both modes, JSON/text as separate workspaces, diff-only-on-Compare, and perf caps for large docs. `--diff-*` theme tokens added.
- 2026-07-19 — **PLAN-07 (Runestone)** Part A + B merged (PR #14/#15) + addendum: public data endpoint `GET /runestone/api/:slug`, library renamed **Mímir**. Greek names added to the runestone name-bank.
- 2026-07-18 — Olympus world: `themes/olympus.json` built-in, `greek.tsx` relics 14→20, GREEK character pool.
- 2026-07-16 — **v1.0.0 released** (plans 00–06 archived). **PLAN-06** clipboard/presence/audit-log + device character aliases; Muninn link detection.
- 2026-07-15 — **PLAN-04** theming engine + **PLAN-05** Heimdall admin (auth core service, hidden gesture/PIN). Ghibli Dusk theme + theme enable/disable.
- 2026-07-12→14 — **PLAN-00** foundation, **PLAN-01** UI/UX (owner-approved), **PLAN-02** file transfer, **PLAN-03** previews + QR.
