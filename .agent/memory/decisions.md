# Decision Log

Append-only. Never silently reverse a logged decision — add a new dated entry superseding it.

| Date | Decision | Reasoning (short) |
|---|---|---|
| 2026-07-12 | Project named **Bifrost**; admin panel named **Heimdall** | Rainbow bridge = transport between realms; Heimdall = gatekeeper |
| 2026-07-12 | Single Fastify process serves client + API + SSE | One URL on LAN, no CORS, one thing to start/stop |
| 2026-07-12 | Node 20 + TypeScript strict; Fastify over Express | Owner's Node experience; plugin encapsulation, validation, streaming |
| 2026-07-12 | React 18 + Vite; plain CSS with custom properties (no Tailwind) | Theming = JSON → CSS vars on `:root` |
| 2026-07-12 | SQLite (better-sqlite3 + Drizzle, WAL) now; repository interfaces so Postgres swap is mechanical for cloud profile ("Option A") | Embedded, restart-tolerant; don't build speculative dual-DB support |
| 2026-07-12 | SSE over WebSockets for live updates | One-directional notifications; browser auto-reconnect; far less lifecycle code |
| 2026-07-12 | chokidar with `awaitWriteFinish` for downloads/ watch | FSEvents-native; debounces half-copied files |
| 2026-07-12 | Uploads: busboy stream → tmp → atomic rename; 0644; no read route for uploads/ | Flat memory at 2 GB; crash-safe; write-only by construction |
| 2026-07-12 | Security: filename sanitization + extension blocklist ("both") | Belt-and-suspenders even though files are never executed/served |
| 2026-07-12 | Heimdall entry: hidden multi-tap AND keyboard shortcut (shortcut editable from admin, stored in DB) + PIN from .env → session cookie | Obscurity alone insufficient; DB stores runtime-mutable settings |
| 2026-07-12 | Admin sees upload **metadata only**; file management stays in Finder | Owner requirement |
| 2026-07-12 | Modularization: vertical slices, event-bus-only cross-module comms, DEPLOY_PROFILE manifest (local/cloud) | Some modules will go to the public internet later; upload/download never will |
| 2026-07-12 | pino → JSON files + rotation as baseline; Grafana+Loki as optional docker-compose add-on (PLAN-07), not load-bearing | Owner stops/starts constantly; observability stack must be detachable |
| 2026-07-12 | Docker is NOT the run mode on macOS (mDNS/FSEvents/Finder break in the VM); PM2 native instead; Dockerfile shipped for future Linux host | Portability story only |
| 2026-07-12 | UI must look unique — fonts + initial theme carefully chosen; **hard approval gate**: no feature plans implemented until user approves PLAN-01 UI/UX | Owner requirement |
| 2026-07-12 | Fonts self-hosted in repo (no font CDNs) | LAN may have no internet; offline-first |
| 2026-07-12 | PLAN-00 only: direct commits to `main` allowed after user's manual approval; `develop` created at its end; all later plans = PR into `develop` | Owner requirement |
| 2026-07-12 | Dependency versions refreshed to current stable at implementation time: React 19 + react-router 7 (supersedes "React 18" wording; our code is API-identical on both), Vite 8, Fastify 5.10, zod 4, pino 10, better-sqlite3 12, drizzle-orm 0.45. TypeScript held at 5.9 and ESLint at 9 — typescript-eslint 8 targets those; TS 7 / ESLint 10 revisit later as a logged decision | Ship on supported versions; avoid known CVEs in older pins |
| 2026-07-12 | eslint-plugin-boundaries v7 config uses the new `boundaries/dependencies` rule + `policies` + `{{from.element.captured.*}}` templates; composition-root files (`server/src/app.ts`, `client/src/main.tsx`) classified via `boundaries/files` categories (v7 elements match folders only). Legacy v5 syntax silently no-ops — enforcement verified empirically with violation probe files | The whole point is a build-failing boundary; a silently dead rule is worse than none |
| 2026-07-12 | Host runtime is Node 22 LTS via Homebrew (`node@22` linked as global node). The machine's Node 17 was broken irreparably by the icu4c upgrade during install | Project requires Node ≥ 20; 17 could not be restored (icu4c 69 gone from brew) |
| 2026-07-12 | Accepted npm audit residue: 4 moderate advisories, all the old esbuild bundled inside drizzle-kit (dev-only CLI; the advisory concerns esbuild's dev server, which we never run) | No runtime exposure; forcing the "fix" downgrades drizzle-kit to 0.18 |
| 2026-07-13 | Fonts (owner's pick): Option A "Asgard console" — Space Grotesk (display) + Inter (body) + JetBrains Mono (data), self-hosted woff2 | Cohesive dev-tool voice; per PLAN-01 two-option protocol |
| 2026-07-13 | UI direction (round 2, owner-requested): full Norse-world immersion — animated aurora sky + starfield, flowing bridge strip, realm eyebrows per page, rune dividers, per-tone portal glows | Round 1 read as stale/blank; owner asked for "world elements everywhere" |
| 2026-07-13 | Ambient-motion exception to the 150–200ms motion rule: the sky layer only (aurora drift, bridge flow, relic float, 10–42s), disabled under prefers-reduced-motion | Owner-approved; interaction feedback still obeys 150–200ms |
| 2026-07-13 | Background "relics": 6–9 random line-art artifacts per page, re-shuffled per route/reload, ~7–9% opacity, margins-biased, collision-free (rejection-sampled circle test; overflow relics dropped). Three collections in `client/src/assets/relics/` (owner-requested location): Norse (20) + Wizarding world (21, owner request) + Olympus/Greek (14, "third set close to the others") | Fun without distraction; owner explicitly asked for the easter eggs, non-overlap, and the third collection |
| 2026-07-13 | Relic collection filter lives in Heimdall ("Sky relics" card): per-collection checkboxes, default all, applies instantly. Client-side localStorage pref — same allowed class as the theme cache; migrates to server `settings` in PLAN-05 | Owner asked for an admin toggle now; real admin backend doesn't exist until PLAN-05 |
| 2026-07-14 | Oversize uploads: past the per-file cap the byte counter stops persisting but **drains** the rest of that part instead of destroying the connection; whole request stays bounded by the content-length pre-check (maxFiles×maxBytes+1MB) and the rate limit. Response is 413 when nothing was accepted and every rejection was oversize, otherwise 201 with per-file `rejected[]` | PLAN-02 requires structured per-file rejections; killing the socket mid-multipart would nuke the good files in the same batch |
| 2026-07-14 | Client uploads one file per XHR request (API still accepts N per request) | Free per-file progress, cancel, and retry; fetch has no upload progress events |
| 2026-07-14 | `GET /api/files/config` exposes maxUploadSizeMb/maxFilesPerUpload/blockedExtensions to the client; new `UPLOAD_RATE_LIMIT_PER_MIN` env (default 60) for the per-IP upload rate limit | Limits live in .env only (CLAUDE.md rule) — the UI must read, not hardcode, them |
| 2026-07-14 | Download ids = sha256(filename) base64url, 16 chars, resolved via the watcher's in-memory registry; watcher initial scan populates silently (no bus events) | Only watched files are addressable → traversal dead by design; silent scan keeps boot from spamming the future audit log |
| 2026-07-14 | core/http: `forceCloseConnections: true` + pass-through of well-formed plugin 4xx errors (statusCode 400–499 → own code/message; 5xx stays opaque INTERNAL) | Shutdown must abort in-flight 2 GB uploads or close() hangs; live probing showed the rate limiter's 429 surfacing as 500 through the old handler |
| 2026-07-14 | Range serving: single-range only; malformed or multi-range headers are ignored (200 full) per RFC 9110 — only a syntactically valid but unsatisfiable range gets 416 (`Content-Range: bytes */size`); suffix larger than the file serves the whole file as 206 | Browsers never send multi-range; ignoring beats guessing wrong |
| 2026-07-14 | Inline mime for `.html`/`.js` downloads is text/plain, and previews render html/svg as source text | An inline text/html response would execute the file same-origin against the app; source view is what a preview should be anyway |
| 2026-07-14 | `previews` resolves download ids independently: `downloadIdFor` moved to `core/download-id.ts`; the module readdir-scans downloads/ on demand instead of importing file-transfer's watcher registry | Keeps module isolation intact — ids are deterministic, so both modules derive the same id without talking |
| 2026-07-14 | Preview meta: sniffed bytes (file-type) outrank extensions; text kinds decided by extension list + null-byte heuristic; text/markdown previews capped at 1 MB (`previewable: false` above) | Extensions lie (plan requirement); phones shouldn't fetch-and-highlight 100 MB logs |
| 2026-07-14 | Boot ASCII QR (LAN-IP url) writes straight to stdout, not through pino; QrCard renders fixed black-on-white regardless of theme | A multi-line QR inside a JSON log line is unreadable; scanners need contrast, themes don't apply |
| 2026-07-14 | QR center badge (owner request): canvas-drawn white tile + three bridge-gradient arcs at ~22% width, error correction raised to H, rendered 2x for retina | No logo asset exists — drawing keeps it crisp at any size; level H's 30% tolerance absorbs the covered modules |
| 2026-07-14 | Supersedes the arc badge: QR center shows the "Bifrost" wordmark (Space Grotesk 700, bridge gradient) in a band **carved out of the module matrix** via `QRCode.create()` — modules under the band are skipped, not painted over. The `qrcode` lib has no reserved-area API; manual matrix rendering is how you get one | Owner disliked the arcs and the pasted-on look; a knockout band reads as part of the code, and EC level H reconstructs the ~7% skipped area |
| 2026-07-14 | Plan roster change (owner): **PLAN-07 = Runestone** (JSON editor + library, two PRs 07a/07b — declared exception to one-plan-one-PR), **PLAN-08 = JSON Differ** (slot reserved, file to be authored, shares 07's components), **Ops renumbered 07→09**. PLAN-04 amended with optional theme `syntax` token group (defaults derived from `--accent`/`--text`). JSON formatter/diff items pulled out of the PLAN-99 toolbox entry | Owner scheduled the JSON tooling; gates stay strictly numeric (07 after 06, 09 after 08) |
| 2026-07-15 | Theme JSON = `{id, name, mode, tokens}` where `tokens` is a flat map of the tokens.css custom properties: 14 required color roles; atmosphere/syntax/qr/fonts/shape groups optional with server-side derived defaults (resolver builds gradients/glows/syntax from accent/accent-2/ok). Unknown token keys rejected (`additionalProperties: false`) | Minimal themes stay authorable by hand; derivation beats forcing 40+ values; closed key set kills junk and typo-drift |
| 2026-07-15 | Theme value guard rails: colors strictly pattern-checked; free-form CSS tokens reject `url()`/`@`/`;`/`{}`/`<>`; fonts must lead with a self-hosted family | Themes must never trigger network fetches (offline-first) or smuggle markup/behavior into inline styles |
| 2026-07-15 | `GET /api/themes` returns `defaultId: null` unless the owner explicitly set `themes.default` in settings — client resolution: visitor choice → explicit server default → `prefers-color-scheme` mode match → first | Acceptance 4 (first visit follows OS scheme) is impossible if the server always names a default |
| 2026-07-15 | Theme write API behind `THEME_WRITE_API` env flag (default on) until PLAN-05 swaps it for Heimdall sessions; built-ins (`aurora`, `daybreak`) refuse overwrite and delete | Plan ships the API before auth exists; flag documents the temporary trust boundary |
| 2026-07-15 | Client applies themes as inline `:root` custom properties + keeps `data-theme` attr; last theme cached in localStorage and replayed by an inline `index.html` script pre-paint (FOUC guard). Engine clears previously-set vars before applying so omitted optional tokens fall back to stylesheet defaults | Attribute stays for observers (QrCard) and CSS hooks; cache replay beats a flash of Aurora for Daybreak users |
| 2026-07-15 | Client workspace gained vitest (`npm test -w client`) for pure logic (theme resolution order); DOM/browser flows stay manual per test checklist | The resolution matrix (stored/default/prefers) is exactly the kind of logic that regresses silently |
| 2026-07-15 | Landing page is **Midgard** (owner): nav label, `MidgardPage` component/file, css hooks — no page in the shell is called "Home" | Realm naming was already half-applied (404's "Back to Midgard", midgard→asgard eyebrows); consistency won |
| 2026-07-15 | Open point for PLAN-08: it claims `--diff-*` tokens ship in PLAN-04, but PLAN-04's implementation closed the schema key set without them. Owner decision pending: extend PLAN-04 pre-merge vs. extend the schema during PLAN-08 | Recorded so the mismatch isn't rediscovered mid-PLAN-08 |
| 2026-07-15 | PLAN-05 sessions use `@fastify/secure-session` (owner-chosen when asked; the plan named it but it wasn't installed). 32-byte secretbox key = sha256(`HEIMDALL_SESSION_SECRET`) when set (survives restart), else `randomBytes(32)` at boot (ephemeral, warned). Cookie `bifrost_admin`, httpOnly, SameSite=Strict, no `secure` (LAN http). Session payload `{epoch, expiresAt}`; guard slides expiry 30 min on each request | Plan-faithful; optional secret keeps existing `.env` booting; sliding expiry + explicit epoch are self-contained and testable |
| 2026-07-15 | "Revoke all sessions" = a monotonic session **epoch** the guard compares against, persisted in `settings['heimdall.sessionEpoch']`. Bumping it invalidates every outstanding cookie and survives restart | Stateless cookies can't be individually revoked; a persisted epoch gives real revoke without a server-side session store, and reuses the existing settings overlay |
| 2026-07-15 | Auth is a **core** service, not the heimdall module: `registerAuth` decorates `app.requireAdmin` on the root instance so both heimdall routes and theme POST/DELETE inherit it (modules can't import each other). `ModuleDeps` gained `auth`. **`THEME_WRITE_API` removed** — supersedes the 2026-07-15 flag decision; the flag was explicitly "until PLAN-05 swaps it for sessions" | The guard must protect two modules' routes; a shared core decorator is the only isolation-respecting way |
| 2026-07-15 | Upload metadata view backed by a new `upload_audit` table (migration `0001_upload_audit`, keyed by unique stored name). A recorder persists `file.uploaded` (upsert) and boot-reconciles existing uploads/ files (insert-or-ignore, so a recorded hint is never clobbered). `file.uploaded` gained optional `uploaderHint` set from `request.ip` in file-transfer's route | Criterion 5 needs PLAN-02-era uploads to show; the event carried no uploader identity, so a minimal additive IP hint threads through without touching the tested upload body |
| 2026-07-15 | Entry gesture is not a security boundary (the PIN is): public `GET /api/heimdall/access` serves the current shortcut+tapCount so the always-loaded client gesture works. Client `heimdallGate` is a one-shot in-memory reveal flag; `/heimdall` shows an inline 404-lookalike unless the flag is set OR a live session exists (session probe on mount). Footer `bifrost.local` mark is the tap target; `settings.updated` over SSE rebinds open clients live | Served HTML stays free of `heimdall`; a logged-in refresh still resolves (flag gone → session check), and the door re-hides on reload |
| 2026-07-15 | PLAN-05 amendment (owner): Heimdall requires the PIN on **every arrival**. The client never auto-authenticates the dashboard from a lingering cookie — the dashboard renders only after a fresh in-mount login; leaving the page (nav away / back / lock) logs out (clears the session cookie) and resets the reveal gate, so new tabs, back button, refresh, and direct URL all return to the 404-lookalike → gesture → PIN. **Supersedes acceptance criterion 2's "session survives refresh."** Hidden-entry model kept (owner chose to keep the 404-lookalike over always-show-PIN). Caveat unchanged: secure-session is stateless, so logout clears the browser cookie (the normal case, verified) but a *copied* token stays valid until expiry or an epoch bump | Owner wants no ambient admin access — a walked-up device or a fresh tab must re-enter the PIN; UI-level enforcement + cookie clear covers normal navigation |
| 2026-07-16 | PLAN-06 device identity: a stable client-generated `deviceId` in localStorage (same allowed class as the theme choice), sent two ways — as the SSE `?deviceId` query param (presence) and the `X-Bifrost-Device` header (clipboard attribution). Not an auth token. Device→name resolution is a client-side `core/devices` registry fed by `/api/presence`, so clipboard/audit never import presence | Presence must map connections to devices without polling; a header/query id is the least-coupled way, and client-side name resolution keeps module boundaries clean |
| 2026-07-16 | Presence is SSE-derived: extended the core `SseHub` to carry per-connection metadata (deviceId/ua/ip/since) and an `onConnectionChange` subscription. The presence module records each live connection (UA label via `ua-parser-js`, server-side) and broadcasts `presence.changed` on connect/disconnect/rename. `devices` table holds deviceId → name/label/lastSeen | The hub already owns every connection; exposing it (read-only) is cleaner than a parallel heartbeat/poll |
| 2026-07-16 | Clipboard: one `clipboard_entries` board, capped (`CLIPBOARD_MAX_ENTRIES`=100) oldest-out, per-entry size cap (`CLIPBOARD_MAX_TEXT_KB`=64 → 413), optional TTL (`expiresAt`, swept by a 60s timer). Broadcast is a `clipboard.updated` **delta** (add/delete), not the whole list. Client copy prefers the async Clipboard API with an execCommand-textarea fallback for plain-LAN-http | Deltas keep large boards cheap over SSE; the fallback is required where the async API is blocked on non-secure LAN origins |
| 2026-07-16 | Section renames (owner): **Clipboard → Muninn** (Odin's memory-raven), **Devices → Wardens** (the bridge-wardens), **QR → Sigil** (a mark that carries meaning). **Frontend-only** — nav label + page heading + route (`/muninn`, `/wardens`, `/sigil`) + client feature folder/component (`MuninnPage`/`WardensPage`/`SigilPage`). Server modules (`clipboard`/`presence`/`qr-tool`), API paths (`/api/clipboard` etc.), bus events, and DB tables are **unchanged** — internal plumbing, like Send/Receive over `file-transfer`. QR's "runes" eyebrow reworded to "sigils" (Runes rejected to avoid clashing with PLAN-07 Runestone) | Owner wanted lore names like Heimdall/Midgard; keeping the server untouched avoids churn to the merged/tested API surface |
| 2026-07-16 | Device character aliases (owner): each device gets a unique random alias from a curated pool (Harry Potter + Norse + MCU) on first sighting — `devices.char_name` (migration `0003_device_character`), assigned by `pickCharacterName` (unused-only, `Traveler N` fallback), filled via `coalesce` so it's set once and never re-rolled. The alias is the **default display name everywhere** (clipboard attribution, Devices page); a user-claimed rename still overrides it (`name ?? charName ?? label`). **Heimdall additionally shows the original UA label** next to the alias (Connected-devices card + audit actor column); the public Devices page hides the UA label. Client `core/devices` resolves deviceId → `{display, label}` | Owner wanted memorable device names; the admin still needs the real UA to correlate, so the label is Heimdall-only |
| 2026-07-16 | audit-log is a pure bus subscriber writing `audit_events`; it also owns the guarded `GET /api/heimdall/audit` (History) and retention prune (`AUDIT_RETENTION_DAYS`=90, boot + daily). Only `app.ts` imports it → deleting it from the manifest stops recording but breaks nothing (Rule 2). Added a `heimdall.login` bus event for it to record. **Deviation from plan**: kept PLAN-05's `upload_audit` table + uploads card rather than repointing them at `audit_events` — that would couple heimdall to audit-log's table; the History card is the richer cross-module log instead | Rule-2 self-containment beats the plan's literal "uploads view reads the richer store"; the two coexist without coupling |
| 2026-07-15 | Added a 4th relic collection `ghibli` ("Ghibli world", 19 relics): 16 original line-art motifs (forest/soot/tree spirits, flame, acorn, leaf, umbrella, feather, glider, goldfish, walking castle, river dragon, teapot, lantern, wind, mushroom) + 森/風/火 kanji glyphs. Registered in `RELIC_COLLECTIONS`; `ALL_COLLECTIONS` auto-derives so SkyRelics and Heimdall's "Sky relics" card pick it up with no other changes. Original art, not character replicas (same class as the owner-requested Wizarding set) | Owner requested Ghibli relics to match the new theme |
| 2026-07-15 | Added built-in theme `ghibli-dusk` ("Ghibli Dusk", dark) — Studio-Ghibli evening/night: deep **forest-green** night bg (owner: green, not blue/black), lantern-gold `--accent`, dusk-teal `--accent-2`, meadow-green `--ok`, warm-coral `--danger`. Full atmosphere tokens authored (not derived) so `--sky` renders a warm dusk-orange horizon glow under a green night. Added to `BUILT_IN_THEME_IDS` (undeletable) | Owner requested a Ghibli-styled evening/night theme, greenish base |
| 2026-07-15 | Theme enable/disable (owner): admin hides a theme from the switcher without deleting it. Disabled ids live in DB settings `themes.disabled`; the `themes` module filters them from **both** `GET /api/themes` and the SSE `theme.updated` broadcast — so the client switcher/engine need zero changes and fall back automatically when an active theme is disabled. Heimdall uses guarded `GET /api/themes/manage` (all themes + `enabled` flag) and `PATCH /api/themes/:id {enabled}`; toggling re-emits `theme.updated` (module wiring filters it) so open switchers update live. At least one theme must stay enabled (409 `LAST_THEME`) | Filtering at the source keeps the module boundary clean (no client filtering, no cross-module import) and reuses the existing live-update path |
| 2026-07-15 | Heimdall mobile entry (owner): the footer `bifrost.local` tap target is `display:none` ≤640px and the keyboard shortcut is meaningless on touch, so the header **Bifrost** wordmark is also a tap target (same 7-tap gesture). It's a home link, so `registerTap` calls `preventDefault()` only on the trigger tap — react-router Link honors `defaultPrevented`, so taps 1–6 still navigate home (single-tap logo behavior preserved) and tap 7 opens the gate. Verified via CDP in a 390×844 viewport (footer hidden → 7 wordmark taps → PIN screen) | Phones had no way in; the wordmark is the only identity mark always visible across breakpoints |
| 2026-07-15 | Login rate limit is a small in-memory per-IP `LoginThrottle` (5 failures / 15 min sliding + incremental 400 ms→2 s delay, attempts logged), not `@fastify/rate-limit` | The plan wants incremental delay + structured attempt logging + lockout, which the generic plugin doesn't express cleanly |
| 2026-07-16 | **Resolves the 2026-07-15 `--diff-*` open point**: PLAN-04 is merged, so the diff tokens ship in PLAN-08 — optional `diff` group added to the theme schema there, with server-side derived defaults (from `--ok`/`--danger`/`--accent`) mirroring the `syntax` group, plus explicit values for the three built-ins. PLAN-08 amended accordingly (owner approved plan validation/updates this session) | Extending the schema in PLAN-08 keeps PLAN-04's merged surface untouched; derived defaults mean no existing theme breaks |
| 2026-07-16 | Pre-implementation validation pass over PLAN-07–10 (owner-requested); plans amended: **07** — author is `author_device_id` only, resolved client-side via `core/devices` (`name ?? charName ?? label`, alias-first per PLAN-06), server author-filter by deviceId (no cross-module read of presence's `devices` table); `core/relics` name bank coexists with `presence/character-names.ts` (no refactor of merged code) and is distinct from `assets/relics` art. **09** — backup zips `themes/` too (user themes are state outside `storage/`), `.env` excluded unless `--include-env`; backup is live-safe (VACUUM INTO under WAL) and exported as an importable function, only restore refuses a live server. **10** — admin-gated `log.line` SSE re-checks session epoch/expiry per send; module toggles get a static cross-module degradation map; forget-device = row delete → fresh alias on reconnect + deviceId-fallback attribution; `GET /about` version/commit baked at server build time; acceptance 1 scoped to routes (API-path strings in the bundle accepted) | Plans referenced pre-PLAN-05/06 reality (UA-label fallback, tokens "from PLAN-04", storage-only state) and two plans conflicted (09's live-server refusal vs 10's in-app Backup now); fixing on paper is cheaper than mid-implementation |
| 2026-07-16 | Housekeeping: progress.md rows for PLAN-03 (merged as PR #4) and PLAN-06 (PR #7 merged) were stale — flipped to done from git history; PLAN-10 added to progress.md and the plans/README gate table (gate: PLAN-09 merged) | Gates are enforced from these files; stale/missing rows would have blocked PLAN-07 and left PLAN-10 ungated |
| 2026-07-16 | v1.0.0 cut manually at the 00–06 release point; release automation added to PLAN-09 scope for all subsequent releases; GHCR publish/auto-deploy deliberately excluded until a consumer exists | Owner decision |
| 2026-07-16 | `release` added to the commitlint scope enum | commitlint rejected the runbook-prescribed `chore(release): prepare v1.0.0`; PLAN-09's automation emits `chore(release): vX.Y.Z` commits (its loop guard keys on that prefix), so the scope is needed permanently, not just today |
| 2026-07-19 | **PLAN-07 addendum (owner)**: public read-only data endpoint `GET /runestone/api/:slug` returns the raw stored document text as `application/json` with `Access-Control-Allow-Origin: *` — a saved runestone doubles as a stable API URL for third-party services. Outside `/api/` (owner's URL shape); registered routes beat the SPA fallback so only this exact path escapes the client app; stale slugs 301 to the canonical data URL; raw text (not re-serialized) so formatting and >2^53 precision survive; writes stay on `/api/runestone`. Vite dev proxy gained `/runestone/api` | Owner wants slug URLs consumable by third-party tools; CORS `*` is safe because the endpoint serves nothing but the named public document |
| 2026-07-19 | Library renamed **Mímir** (owner asked for a lore name; Mímir keeps the well of remembered knowledge — fits a library of carved stones). Frontend-only per the Muninn/Wardens/Sigil precedent: route `/runestone/mimir` (old `/runestone/library` → client redirect), `MimirPage` component, user-facing copy ("Save to Mímir", "Drawing from the well…"). Server module id, API paths, events, tables unchanged. Library rows gained an "API" chip opening the public data URL in a new tab | Consistent with the established lore-naming approach; server surface stays stable |
| 2026-07-19 | Runestone route layout: `/runestone` stays the editor (nav entry + Part A draft behavior preserved), library lives at `/runestone/library`, saved docs at `/runestone/:slug` — the literal segment out-prioritizes the param in both react-router and Fastify, and a real slug always carries a `-<6char-id>` tail so "library" can't collide in practice | Making the library the root would have moved the merged Part A surface and its draft-restore semantics for no user benefit |
| 2026-07-19 | Runestone ids are 6-char lowercase base36, generated with collision-retry, and double as the primary key; the slug (`<kebab>-<id>`, diacritics stripped) is stored with a unique index and regenerated on rename. Stale-name slugs resolve by extracting an id-shaped tail → API answers **301** to the canonical slug; the client follows it and `replaceState`s the address bar | The id-anchored URL is what makes renames link-safe (plan requirement); storing the slug keeps lookups indexed instead of parsing on every request |
| 2026-07-19 | Draft cache stays **scratch-editor-only** in Part B: saved documents live on the server and don't write the localStorage draft (saving clears it — plan's handoff rule). Refreshing mid-edit of a *saved* doc reloads the server copy; per-doc draft buffers are a possible later add, not in plan scope | One draft slot with clear semantics beats a keyed cache the plan never asked for; the server is the source of truth for saved docs (coding rules) |
| 2026-07-19 | Library author display: `deviceName()` (alias-first per PLAN-06) with a "departed device" fallback for author ids presence has no record of (e.g. API-seeded or long-forgotten devices); author filter options accumulate from unfiltered listings client-side, mapped back to deviceIds for the server's exact-match `author` param | The server deliberately knows no author names (Rule 2); the fallback keeps rows honest instead of showing raw ids |
| 2026-07-17 | `<JsonEditor>` lives in `client/src/core/ui/`, not `features/runestone/` — PLAN-08's variant feature mounts two of them and features can never import features, so core is the only boundary-legal shared home (same reasoning as `core/json`). `copyText` moved `features/muninn/copy.ts` → `core/copy.ts` for the same reason (runestone needs it); mechanical move, no behavior change | Rule: shared client code goes in core; deciding it now avoids a PLAN-08 refactor of freshly-merged code |
| 2026-07-17 | `core/json` is built on jsonc-parser end-to-end: validation (strict options — comments/trailing commas are errors), format (edit-based), minify (scanner token concat) — so both preserve raw tokens exactly (key order, `9007199254740993` stays literal). sortKeysDeep and the tree view necessarily operate on `JSON.parse`d values, so numbers past 2^53 normalize in those two paths — accepted, standard JSON-tool behavior; jsonc-parser's const enums are inlined as named numeric constants (ambient const enums can't be imported under isolatedModules) | Parse/stringify round-trips mangle precision and order; edit/scanner paths don't. Full-fidelity sort would mean reimplementing a JSON writer for an edge case |
| 2026-07-17 | Nav now actually renders from `/api/capabilities` (each entry carries its module id; entries filter once capabilities arrive, all show while loading) — PLAN-07's "nav entry via capabilities" task implemented the architecture claim that had been static since PLAN-01. Bottom nav items flex-shrink with ellipsized labels so seven entries fit 390px | A cloud build must not show local-only pages; loading-state shows all to avoid a pop on the common local profile |
| 2026-07-16 | Muninn linkify (owner): text entries render detected links as anchors — any `scheme://` deeplink, allowlisted opaque schemes (mailto/tel/sms/geo/facetime/whatsapp/…), bare emails → `mailto:`, `www.`/known-TLD bare domains → `https:`, phone numbers → `tel:`. Phone shape: 7–15 digits with space/dash/paren separators only (no dots — keeps decimals/versions/IPs plain); bare digit runs need ≥10 digits, shorter ones need a leading `+` or `(` — so dates/ids stay plain. `javascript`/`data`/`vbscript`/`file`/`blob`/`about`/`filesystem` schemes always render as text. Only http(s)/ftp get `target="_blank"`; tel/mailto/deeplinks hand off to the OS without a tab. Code-snippet entries exempt (owner spec). Pure tokenizer in `features/muninn/linkify.ts`, rendering stays in `MuninnPage` | Owner asked for tappable links incl. deeplinks/tel/mailto; a shared board renders other devices' text, so a scheme blocklist and a conservative phone heuristic keep it from becoming a script-URL or false-link surface |
| 2026-07-18 | Greek-mythology world completed (owner): the Olympus relic collection (PLAN-01 round 4) was the only Greek piece, so the two missing world surfaces were added — built-in theme `olympus` ("Olympus", dark: midnight-Aegean blue bg, gilded-gold `--accent`, Aegean-cyan `--accent-2`, laurel-green `--ok`, terracotta `--danger`; full atmosphere tokens authored so `--sky` renders a gold summit glow over the sea, mirroring the ghibli-dusk precedent) added to `BUILT_IN_THEME_IDS` (undeletable), and a GREEK pool (24 names: Olympians + heroes + Titans) added to `presence/character-names.ts` device aliases. No new relic collection — Olympus already exists and auto-surfaces via `ALL_COLLECTIONS` | A "world" in Bifrost = theme + relic collection + character pool; Ghibli set the pattern and Greek had only relics |
| 2026-07-18 | Olympus relic collection expanded 14 → 20 (owner: bring Greek to parity with the other sets — Norse 20, Wizarding 21, Ghibli 19): six new line-art relics (Athena's owl, Artemis' bow, Dionysus' grapes, olive branch, trireme, theatre mask) in the same stroke language; the Ω/Δ/Ψ glyphs stay. Screenshot-verified all 20 read as recognizable line art (same harness ritual as the Ghibli set) | The smallest collection under-represented its world in the sky rotation; parity keeps the random relic mix balanced |
| 2026-07-19 | Greek entries added to the PLAN-07 relic-name bank (owner: the Greek world should reach every world surface, runestone titles included): 11 new entries across all four categories — person Athena/Circe/Atlas/Achilles, relic Aegis/Ambrosia/Golden Fleece, spell Pharmakon/Katabasis, weapon Keraunos/Harpe — applied identically to `server/src/core/relics` and its client mirror `core/relicNames` per the keep-in-sync rule; bank comment now reads Norse / Potter / MCU / Greek. Bank tests pin one name per universe | The name bank is a fourth world surface introduced by PLAN-07 after the Olympus world PR was opened; Greek titles like "Silent Ambrosia" now appear in default runestone names |
| 2026-07-20 | PLAN-08 diff tokens: 6 keys (`--diff-add/remove/change` + `-soft` line backgrounds), derived per plan from ok/danger/accent with softs at 0.16 alpha dark / 0.12 light; **all four** built-ins got explicit authored values (Olympus postdates the plan's list of three) with add=theme green, remove=theme red, change=theme amber per acceptance 3 | The strong color carries char-level emphasis (underline bar), the soft carries line tint — one pair per op keeps the schema small; authored built-ins satisfy "modification amber" while derived minimal themes lean on their accent |
| 2026-07-20 | Variant text mode diffs **live** via @codemirror/merge (split editable, unified single-pane); the explicit Compare button drives JSON mode only | The owner's explicit-compare decision was motivated by structural-diff cost on MB docs; the merge view diffs incrementally by design, and gutting its live highlighting to fake a compare step would add state for strictly worse UX |
| 2026-07-20 | Text normalization toggles (trim/strip whitespace, case, blank lines) switch the panes to **read-only normalized copies**; only EOL normalization is invisible (CM normalizes line endings natively) | @codemirror/merge exposes no ignore-whitespace/case diff options; silently rewriting the user's buffers was rejected as destructive, and diffing hidden copies under editable panes would lie about what is being compared |
| 2026-07-20 | Diff walker records carry `path` + `leftPath` + `rightPath` (by-key array matches can sit at different indices per side); by-key strategy: moves are no-ops, elements with missing or duplicate identity fall back to deep-equality set matching; `ignoreKeyOrder: false` emits order-only differences as `aspect: 'key-order'` records | Click-to-jump must resolve a record in both panes, so one display path is not enough; set-fallback prevents pairing unrelated items; the off-state of ignore-key-order needs observable semantics or the toggle is a placebo |
| 2026-07-20 | TreeView and the runestone api client moved from `features/runestone/` to `client/src/core/` (`core/ui/TreeView`, `core/runestone`); JsonEditor extended with a `highlights` prop + scroller handles instead of a second editor component | Variant needs both and features cannot import features (same precedent as the PLAN-07 copy-helper move); the plan mandates the shared editor as the deliverable, so decorations belong on it, bound to `--diff-*` tokens |
| 2026-07-20 | `variant` server module is capability-only: `register()` is an intentional no-op, manifest presence (both profiles) is the feature | All comparison is client compute; the module exists so `/api/capabilities` drives nav and the picker's runestone-capability check, keeping the cloud-profile story mechanical |
| 2026-07-20 | Variant compare cost is bounded everywhere: snapshots parse once per compare (`core/json` `docRanges`), decorations cap at 800 records, drawer rows at 100/group (200 text chunks) with "…and N more", and every @codemirror/merge diff carries `{scanLimit: 500, timeout: 300}` | Live repro: two 200 KB mostly-different docs froze the tab — per-record re-parsing was quadratic, a 12k-row DOM list is itself a freeze, and unbounded char-level Myers on one enormous minified line runs for minutes; past these budgets extra fidelity adds zero legibility |
| 2026-07-20 | Auto-close brackets/quotes (+ backspace pair-deletion) added to the shared JsonEditor via @codemirror/autocomplete | Owner asked for it on the Runestone editor; the shared component means Variant's JSON panes inherit it, and closeBrackets is the stock CM6 mechanism rather than a hand-rolled input handler |
| 2026-07-20 | Variant's compare stack breaks out of the 62rem shell column to min(105rem, viewport−gutters) on ≥1024 px via symmetric negative margins; page head stays in the shared column; Mímir picker renders in JSON mode only | Owner feedback: two side-by-side editors were cramped between wide gutters — Variant is the one page that earns the width, and a global shell widening would re-layout every other page; runestones are JSON documents, so the picker is noise in text mode |
| 2026-07-20 | **Supersedes "text mode diffs live"**: owner requires diffing to run ONLY on the Compare CTA in both modes. Text mode is now two-phase — plain editable panes (new `plain` prop on JsonEditor: no JSON parsing/lint/fold/pairing, zero per-keystroke work) → Compare snapshots + normalizes → read-only @codemirror/merge result view (split/unified) → Edit returns to the panes. Buffer-replacing actions (swap/import/pick/clear) and text-option changes drop the result; the JSON-fallback path still auto-runs one text compare so a broken side never dead-ends | Owner's explicit performance requirement outweighs the live-view convenience; a read-only result is the only honest way to guarantee no keystroke ever re-diffs |
| 2026-07-20 | Variant pane labels are display-only (imports and Mímir picks set them; users cannot edit), and JSON/text modes are **separate workspaces** — per-mode buffers and per-mode results both survive mode switches, and content never carries across a manual toggle. The invalid-JSON fallback remains the one deliberate cross-mode copy (that Compare click explicitly re-targets the same docs as text), and its return button is now always enabled so the user can reach the JSON workspace to fix the doc | Owner feedback: editable labels were unwanted surface, and shared buffers made the mode toggle feel like a formatting switch instead of a different tool; an always-enabled return is required once buffers separate or the fallback would trap the user in text mode |
| 2026-07-20 | **Supersedes the read-only text result view** (owner): panes stay editable after Compare in BOTH modes, and the first real edit removes the markers and the results drawer entirely (no stale state — results only return on the next Compare). Text mode now mirrors JSON mode's architecture: two always-editable plain panes that get chunk-derived decorations (line tint + char emphasis from `Chunk.changes`) painted at Compare time; the split⇄unified toggle, Wrap button, and Edit button die with the merge view. With destructive normalization on, inline markers stay hidden (pane buffers ≠ compared snapshots) and the drawer carries the result | Owner: "I should be able to modify them like json… the compare marker and detail should get removed [on typing] and only come again when I tap compare". One architecture for both modes is also simply less code — TextCompare.tsx deleted |
| 2026-07-21 | **Reverses the invalid-JSON cross-mode copy from the 2026-07-20 separate-workspaces decision** (owner: reported as a bug): a JSON Compare where a side won't parse now stays in the JSON workspace and just shows a warning notice naming the invalid side (`invalidMessage`; pane status already shows the error count) — no mode switch, no auto text-diff, and the separate text workspace is never touched. The whole text-fallback banner is gone (`fallback` state, "switched to Text mode" toast, `returnToJson`) since it can no longer fire | The auto-switch clobbered whatever the user had typed in the (deliberately separate) text workspace — silent data loss the "one deliberate cross-mode copy" wasn't worth; JSON mode's per-side validity already tells the user which side to fix |
| 2026-07-21 | Lore renames (owner): **Muninn → Hermes** (clipboard page) and **Mímir → Pensieve** (runestone library page). Frontend-only per the established codename precedent — server module ids stay `clipboard`/`runestone`, DB/events/API paths unchanged. Client page folders/components/routes/nav/copy updated (`features/hermes/HermesPage`, `/hermes`; `PensievePage`, `/runestone/pensieve`); old routes `/muninn`, `/runestone/mimir`, `/runestone/library` keep redirecting so existing links survive. Legit Norse mythology left untouched: the Huginn/Muninn raven relic and the "Muninn" device-alias name. Historical records (CHANGELOG, history.md, prior decision rows, completed PLAN-07 spec) not rewritten | Owner wants Harry-Potter/Greek page identities (Pensieve stores memories → a document library; Hermes the messenger → text sync); frontend-only keeps the rename cheap and reversible, and redirects avoid dead links |
| 2026-07-21 | Markdown tool named **Edda** (Norse manuscripts) = PLAN-11, gated after PLAN-10; own module + own `eddas` table — deliberately NOT stored in `runestones` (would pollute Pensieve's JSON semantics); separate library page | Owner decisions; patterns shared with Runestone, storage uncoupled |
| 2026-07-21 | Edda share = three URL surfaces: `/edda/:slug` opens the editor (mobile defaults to Preview mode), `/edda/preview/:slug` public read-only rendered page, `/edda/api/:slug` raw text/markdown with CORS/301/404 + `?download=1` attachment | Owner spec: "preview" and "api" literally in the paths |
| 2026-07-21 | Live-preview perf model: ~200ms debounce inside rAF; auto-degrade to manual "Refresh preview" above `EDDA_LIVE_PREVIEW_MAX_KB` (default 300); on mobile the preview component is **unmounted** while editing, not hidden — rendered once per toggle | Owner requirement (massive docs must not hang, mobile preview must cost nothing while editing); Variant's budget philosophy |
| 2026-07-21 | One shared markdown renderer `core/markdown/renderMarkdown` (marked GFM + highlight.js + heading ids + DOMPurify) feeds live preview, public preview page, and HTML export | Three surfaces, one renderer, zero drift; sanitization non-negotiable |
| 2026-07-21 | Scroll sync is approximate-by-design (percentage + heading correction, toggleable); exact sync explicitly out of scope | Tarpit avoidance — do not reopen |
| 2026-07-21 | Mermaid, paste-image upload, PDF export parked in PLAN-99 but advertised in an on-page coming-soon footer (array-driven) | Owner decision |
| 2026-07-21 | PLAN-09 backup/restore lives in `server/src/core/backup` (server-typed + tested), with thin `scripts/backup.ts`/`scripts/restore.ts` CLI wrappers importing it; the resilience suite is a standalone `scripts/resilience.ts` (`npm run test:resilience`) kept OUT of `npm test`. Added `BACKUP_KEEP`, an `ops` commitlint scope, and a `*.cjs` eslint block | Core is the boundary-legal home PLAN-10's in-app backup button reuses in-process; scripts aren't in the workspace typecheck so the cross-import is clean. The resilience suite spawns dozens of servers (~90s) — too heavy for the default gate, run on demand |
| 2026-07-21 | **PLAN-10 tranche 1 (conversion) implemented**: Heimdall is a modal overlay mounted at the app shell, not a route. Deleted the `/heimdall` route, the 404-lookalike (`HeimdallPage`), and `core/heimdallGate`. Entry is viewport-gated ≥768px (wordmark N-taps + keyboard shortcut, re-synced on resize); the footer-mark tap target is gone. Existing content restructured into grouped, searchable sections (Watchtower/Realm/Vault/Bridge) with per-section control manifests + scroll-jump highlight (`search.ts`). Modal UX: single **Lock** control in the content header (removed the ✕ there), a "Heimdall" brand in the left panel, PIN field auto-focus, and a `.check-row` badge-column alignment fix. Remaining PLAN-10 sections (About, Logs, Storage expansion, Devices admin actions, Modules, Maintenance) are follow-up tranches | Plan's conversion + section-restructure scope; the URL attack surface disappears with the route, and searchable manifests satisfy acceptance 4. Landed as one increment so the owner tests the shell before the new sections are built |
| 2026-07-21 | **Auth model reversed (owner, supersedes 2026-07-16 relaxation and re-adopts the spirit of 2026-07-15)**: the PIN is required on **every** modal open. The modal remounts per open so it always starts on the PIN view; any close (scrim click, Esc, Lock) calls `logout()`, and a page refresh re-locks naturally. Kept: env PIN, constant-time compare, per-IP throttle, session epoch/revoke | Owner wants no ambient admin access — closing or refreshing must re-lock. Residual caveat unchanged: a stateless cookie stays valid until expiry if you refresh *without* closing, but the UI always re-asks the PIN and the next login replaces the session |
| 2026-07-21 | **Wardens folded into Heimdall (owner)**: removed the Wardens entry from the main nav; the device roster now lives in Heimdall's **Wardens** section (renamed from "Devices"). The public `/wardens` route stays reachable so existing links survive | Owner: the roster belongs in the admin surface, not top-level nav; keeping the route avoids dead links |
| 2026-07-21 | **7-day stale-device prune (owner)**: `POST /api/presence/prune` deletes devices that are offline **and** unseen for > 7 days (currently-connected devices are never pruned); run on-demand when a Wardens surface opens (Heimdall section + public page), best-effort with a `GET /api/presence` fallback so the roster never blanks. Device **activity is preserved** — no table foreign-keys `devices`, so audit/clipboard/runestone keep the raw deviceId and resolve to the "departed device" label (the alias returns to the pool per the 2026-07-16 forget semantics) | Owner asked for automatic cleanup of long-idle devices on visit without losing their history; the no-FK schema makes row deletion safe, and the GET fallback keeps a stale server from emptying the view |
| 2026-07-21 | **Overview device counts reconciled (owner)**: Overview shows **devices connected** (online) and **devices known** (total), both derived from the presence roster so they always agree with the Wardens list; the heimdall stats `connectedClients` figure changed from raw SSE connection count to distinct online deviceIds | The old raw-connection count inflated with multiple tabs per device; deriving both figures from the one presence source removes the gap by construction |
| 2026-07-21 | **PLAN-10 tranche 2 (About + Logs) implemented**. About: `GET /api/heimdall/about` (version/commit/buildDate/uptime/node/host/profile) + `GET /api/heimdall/changelog` serving `CHANGELOG.md`; version/commit/buildDate are **baked at build time** by `scripts/gen-build-info.ts` (server `prebuild`) into `server/build-info.json` (gitignored), read at runtime with a git/dev fallback — git isn't available under PM2/Docker. Added an `npm run changelog` (changelogen) script. Logs: `GET /logs` (filtered tail), `PATCH /logs/level` (runtime pino level, persisted via `log.level` setting + config overlay so it survives restart), and an admin live tail | Plan's About+Logs scope; the single source of truth for version is the server build, the client renders `/about` |
| 2026-07-21 | **Logs plumbing — three deviations from the plan, logged**: (1) `GET /logs` reads an **in-memory ring** (`core/logtap`) the logger feeds, not a file tail — no rotation/huge-file handling, and the file stays the durable record. (2) Live follow is a **dedicated `requireAdmin` SSE endpoint** (`GET /api/heimdall/logs/stream`), not per-event gating on the public hub — inherently admin-only and simpler; it keeps the plan's **per-send epoch/expiry re-check** (captured at connect, revalidated before each `log.line` + on heartbeat) so revoke/Lock/expiry stops delivery, and the client closes the stream on modal-close to cover plain logout. (3) `createLogger` now uses `pino.multistream([transport, tap])` with **all destinations at `trace`** so the root logger level is the single gate; `setLogLevel` sets the level on the root **and every module child** (pino children don't inherit a post-creation level change) | Cleaner and more secure than gating one event on a public stream; the ring is enough for a debugging window; single-gate multistream makes the runtime level switch apply uniformly to file/stdout/tap |
| 2026-07-21 | **In-editor find implemented on the shared editor (owner)** — built now against the shipped tools (PLAN-10 merged), not deferred. `core/ui/JsonEditor` gains a `@codemirror/search` find panel (⌘/Ctrl-F, next/prev, wrap, case/regex toggles; token-styled) plus an `openSearch()` handle behind a "Find" button (Runestone toolbar code view; each Variant pane header) for discoverability + mobile. Works in `plain` (text) and JSON modes, so Runestone code view and both Variant panes (JSON + text) get it in one build; Edda inherits it later. Browser find can't reach text in a large scrolled buffer — the editor now owns find. **Variant cross-pane reveal**: a new `onSearchMatch` callback fires the matched text; Variant's `crossPaneOffset` finds it in the opposite pane (case-insensitive) and `revealOffset`s there, in both JSON and text modes, with the scroll-lock suspended for that reveal so the panes can sit on their own matches; never re-runs the diff. No server surface | Owner request ("search character in all editor panels … include in plan 11"); the shared editor is the boundary-legal single home, so one build covers every panel; scroll-lock had to yield to divergent search positions |
| 2026-07-21 | **Runestone tree collapse-all/expand-all implemented (owner)** — `core/ui/TreeView` gains a sticky "Expand all / Collapse all" control row that folds/unfolds the whole tree incl. the outermost array/object (the gap: only per-element toggles existed). Mechanism: an `openMode` (`auto`/`all`/`none`) + a generation counter keying the node subtree, so a bulk action remounts and every node re-reads its initial open state — no per-node effects. Appears wherever TreeView renders (Runestone + Variant JSON tree). Per-node toggles still work between bulk actions | Owner reported "can only collapse each element, not the whole array"; the tree already collapsed individual arrays via their node toggle, so the real missing capability was a one-click whole-tree fold. Remount-by-key is simpler and cheaper than syncing state into thousands of nodes |
| 2026-07-21 | **Runestone page revamp — full-width breakout + taller editor (owner)**: the editor/tree workspace and the page head break out of the 62rem shell to `min(105rem, 100vw − 2·space-8)` at ≥1024px (Variant's breakout precedent, head included so the whole page aligns), and the editor/tree height is driven by `--rune-editor-h` = `max(30rem, calc(100vh − 21rem))` so it fills the viewport; below 1024px both stay in-column at 60vh. Also fixed the find-panel theming bug: CM's search inputs carry class `.cm-textfield` with **no `type=text` attribute**, so the earlier `input[type=text]` rule never matched and they rendered browser-white — now targeted via `.cm-textfield` (+ `accent-color` on the checkboxes), fixing Runestone and Variant find panels alike | Owner: "increase pane width… search box colors don't match… revamp to utilize screen properly." A JSON document earns the width like Variant's two panes; the CSS var keeps code+tree heights in sync and responsive; the selector fix is the actual theming bug |
| 2026-07-21 | **Nav reorganised from 7 flat tabs to 3 category tabs (owner)** — the mobile bottom bar was truncating labels. Categories: **Midgard** (home hub — Send/Receive/Hermes cards + the Join-Bifrost QR moved off Sigil), **Ollivanders** (dev tools — Runestone/Variant/Edda-coming-soon), **Diagon Alley** (utility toolbox — the QR maker + Base64/UUID/timestamp advertised coming-soon per PLAN-99). Tools keep their own routes; a category tab lights active on its own page AND its tools' pages (custom `useLocation` prefix match, since NavLink only matches its own `to`). A tab renders only if ≥1 of its modules is in `/api/capabilities`. Sigil was slimmed to just the QR generator ("Make a QR"); its Join-Bifrost card became `core/ui/JoinBifrostCard` on Midgard. Bottom-nav labels now wrap to 2 lines then ellipsis. Category names are owner's pick from the Wizarding world (Ollivanders = "the tool chooses the maker"; Diagon Alley = the shops street); two new icons (WandIcon, SparklesIcon) | Owner: the flat 7-tab bar overflowed mobile (labels like "Midg…"/"Rune…"); grouping by purpose (transfer / dev / utilities) declutters and scales, and keeping every route avoids dead links |
| 2026-07-21 | **Card tone-gradient system + Midgard redesign (owner)** — the per-card tone (soft corner radial-gradient + tone-matched icon chip + tone hover border/glow) from the Home portals now applies to **every hub card** too (`.hub-card--{teal,violet,amber}`, tones cycling across a hub's cards; `--soon` dims a not-yet-built tool). Added a third tone **amber** (`--tone-amber`/`-soft`, `--glow-amber`, authored for dark + light) for Hermes. Midgard was restructured: Hermes promoted to a **third portal** beside Send/Receive (an equal-height 2-col grid had stretched the short Hermes card into a big void next to the tall QR card), and **Join Bifrost** became a wide **horizontal** onboarding card (QR beside its address details), extracted from Sigil into `core/ui/JoinBifrostCard`. Context/design docs synced (`context/*`, `docs/DESIGN.md`, `docs/THEME-SPEC.md`, `README.md`) plus minimal future-plan notes (PLAN-11 Edda entry point in Ollivanders, PLAN-99 toolbox shell = Diagon Alley) | Owner liked the portal gradient and asked for it on all tab cards; the three-portal + horizontal-join layout fixes the void and reads as intentional; one tone system for portals + hubs keeps the language consistent |
| 2026-07-22 | **v1.1.0 released** (first automated release since v1.0.0) — plans 07–10 + editor find/tree-collapse/Runestone-revamp + category nav/Home redesign/tone cards, cut develop→main (PR #24). **`release.yml` had a tag-push bug**: it created a *lightweight* tag then pushed with `git push origin main --follow-tags`, which only pushes *annotated* tags — so the commit/bump/CHANGELOG landed on main but the `v1.1.0` tag never reached the remote, and "Publish GitHub Release" failed ("tag exists locally but has not been pushed"). Recovered manually (pushed the tag, built + published the Release with tarball, fast-forwarded develop). Fixed the workflow to push `main` and the tag as **separate** refs so any tag type publishes | The `--follow-tags` shorthand silently drops lightweight tags; an explicit `git push origin "v$VERSION"` is unambiguous and is what the next release relies on |
| 2026-07-23 | **Edda library named "Pensieve" too (owner)** — the Markdown library shares the Pensieve identity with Runestone's JSON library (both are "a basin of saved documents", each reached from its own tool with its own page/route, so the shared name doesn't collide in navigation). Route `/edda/library` → **`/edda/pensieve`** (old path client-redirects, though nothing shipped on it); editor buttons "Save to library"/"Library" → "Save to Pensieve"/"Pensieve"; 404 + public-page back-links → "Back to the Pensieve"; `pensieve` added to the reserved slug-segment guard. Component/file stays `EddaLibraryPage` (avoids a second `PensievePage` symbol clashing in the App.tsx imports) — display name only, per the established codename-vs-internal split | Owner wants the lore name applied to Edda's library as well; two same-named pages are unambiguous because each is entered from its own tool, and a future unified library shell is already parked in PLAN-99 |
| 2026-07-23 | **PLAN-11 (Edda) implemented** on `feat/plan-11-edda`. Server: own `edda` module + `eddas` table (migration `0005_edda_documents`, additive — same id/slug/301 scheme as runestone; markdown is free text so save validates only the `EDDA_MAX_DOC_KB` cap, no content check, empty docs allowed), CRUD + public `GET /edda/api/:slug` (raw `text/markdown`, CORS `*`, 301 stale, `?download=1` attachment), reserved-segment guard (`preview`/`api`/`library`), `edda.saved`/`edda.deleted` bus events → SSE + audit pickup. Module in **both** profiles. Client: shared `core/markdown/` (renderMarkdown = marked GFM + highlight.js + heading ids + DOMPurify; outline; stats; pure toolbar `commands`), shared `JsonEditor` gained a **markdown mode** (`@codemirror/lang-markdown` + `--syn-*` tint) and an `applyEdit` handle (minimal-change toolbar transactions); three surfaces `/edda` (editor, mobile opens Preview), `/edda/preview/:slug` (public render), `/edda/api/:slug`. Toolbar-command caret rule (owner bug: "select a tag, type, and only the typed text is left"): a command applied with **no prior selection** leaves a **collapsed cursor** (heading/list/quote/task → end of the block; link → after the `https://` scheme) so the next keystroke extends the tag instead of replacing a still-selected placeholder; when the user *did* have a selection the block stays selected (re-toggle works, matches wrap semantics). Perf model per owner: 200ms debounce inside rAF, auto-degrade to manual "Refresh preview" over `EDDA_LIVE_PREVIEW_MAX_KB` (300), pinned-manual toggle, and **mobile preview truly unmounted while editing** (verified via CDP: `.md-preview` absent in Edit, present in Preview). Divider/full-width breakout ≥1024, stacked ≥768, single-pane toggle <768. Coming-soon footer (array-driven: Mermaid · image paste · PDF export). New dep `@codemirror/lang-markdown`; test-only `jsdom` (client) so DOMPurify sanitization has a faithful DOM — happy-dom was tried first but doesn't drive DOMPurify's attribute stripping. **Deviations**: scroll-sync is percentage-only (approximate-by-design decision, heading-anchor correction skipped as a tarpit); markdown editor find inherited from the shared editor (⌘F + Find button) rather than re-added per plan-checklist item (already shipped). Gate green (lint/typecheck/**269 server + 161 client**/build); live-verified (built server + headless Chromium): live preview renders GFM/tables/hljs, `<script>` inert on every surface, save→library→public page→raw API all work, restart smoke + fresh-DB bootstrap both `integrity_check ok` | Owner scheduled PLAN-11; patterns shared with Runestone, storage uncoupled; the perf budget and three URL surfaces are explicit owner requirements |
| 2026-07-22 | **Favicon + PWA (installable) added (owner)** — the site had no favicon and wasn't installable. Static-only approach in `client/public/`: `favicon.svg` (+ `favicon-16/32.png` fallbacks), `apple-touch-icon.png` (180, full-bleed since iOS masks corners itself), `pwa-192/512` (purpose `any`, rounded plate) + `pwa-maskable-192/512` (purpose `maskable`, full-bleed bg, artwork inside the ~72% safe zone), and `manifest.webmanifest` (standalone, `theme/background_color` `#0b0e14`). `index.html` head gains the icon/manifest links + Apple meta (`apple-mobile-web-app-capable`, status-bar-style, title). **No service worker** — a LAN tool needs no offline caching; the manifest + icons are all "add to home screen" on iOS/Android require. Icons were rendered by a one-off Node PNG generator (no image tooling in the env). **Artwork iterated live with the owner to a chosen "Pure Arch"**: a glowing Bifrost rainbow-bridge arch (teal→deep-purple→green — the gradient middle deepened to `rgb(109,52,214)` so it never reads as a pale/white strip) springing from two lit "realm" nodes, over a near-black (`#05070b`) starfield with a corner vignette; glow bloom kept low so the plate stays dark. `favicon.svg` mirrors the same sampled arch geometry (radial-sky bg + blur-glow filter). Vite copies `public/` to the dist root; `@fastify/static` already serves it (manifest verified as `application/manifest+json`) | Owner asked for a favicon + phone-installable icon; PWA manifest was the long-deferred tech-stack item. Static files need no build-tooling/plugin and stay boundary-neutral (no module touched); maskable + `any` variants cover Android adaptive masks and iOS's own rounding; SW deferred since there's no offline story for a same-LAN app |
| 2026-07-21 | JS tool named **Loki** (shapeshifter) = PLAN-12, gated after PLAN-11; execution panel nicknamed **Calcifer**; two PRs (12a transforms, 12b execution) + owner UI checkpoint after Part A's first build | Owner decisions; name collision with presence character pool accepted by owner |
| 2026-07-21 | Beautify = Prettier standalone, Minify = Terser (both lazy-loaded new deps, in-browser); stringify/destringify + escapes + quote conversion + strip comments (standalone action) + IIFE pair + cURL→fetch = pure `core/js` utils, no eval anywhere | Real parsers over regex hacks; offline-safe pure compute |
| 2026-07-21 | JS→JSON direction dropped (owner: no use case); **JSON→JS only**, and its surface is **Runestone's toolbar ("Copy as JS", enabled on Valid JSON ✓)** rather than Loki — the tool belongs where its validated input lives. `jsonToJs` = shared core util per demo cases D/E (round-trip identity property-tested); safe by construction, so no loss dialogs and **no JSON5 dependency**. Correction to the demo-era note: **acorn is a NEW lazy dep** added only for Loki's Part-A syntax banner — it is not currently in the tree (the JSON stack uses jsonc-parser) | Owner decision after use-case review 2026-07-21 |
| 2026-07-21 | Universal transform rules: refused transform leaves buffer byte-identical; applied transform = one undo; single buffer + CM history over in/out panes; before/after comparison delegated to Variant. **Mechanism correction:** Variant's existing `?left/?right` handoff resolves **runestone slugs into the JSON panes**, so raw before/after JS needs a **new lightweight text seed** (sessionStorage bridge) that opens `/variant` in text mode pre-filled — still route/URL-level, no cross-feature imports | Simpler model; Variant already exists for diffing; the slug handoff can't carry arbitrary text |
| 2026-07-21 | Regex tester = top-level `Transforms\|Regex` mode toggle on the Loki page (Variant's JSON\|Text precedent), separate per-mode workspaces | First-class surface, zero nav cost; owner chose toggle over nav entry or CTA |
| 2026-07-21 | Execution = Level-2 sandbox: ephemeral Web Worker per run, in-worker console budgets, corpus-tested serializer, watchdog + Stop both `terminate()`; runner `fetch` allowed by default (LAN self-API use) but Heimdall-switchable; execution master switch in Heimdall. **Profile correction:** the `loki` module ships in **both** profiles (transforms/regex everywhere), but the run UI is gated client-side on `capabilities.profile === 'local'` (a module in both profiles can't advertise a sub-capability) — never reachable in cloud | Worker = no DOM + killable, covers the actual threat model (own snippets, accidents); Level-3 hardening rejected as wrong threat model |
| 2026-07-21 | Panel font A−/A+ scoped to `--loki-panel-font` (editor + drawer only): chosen size per-device (localStorage), default from Heimdall; Heimdall "Loki" card also holds default mode, run timeout, console budget, execution + fetch switches, beautify/minify defaults, regex flags — all settings-table with env seeds | Device comfort is per-device; defaults are admin policy — the PLAN-05 pattern |
| 2026-07-21 | Base64 encode/decode deliberately excluded from Loki (PLAN-99 utility-toolbox owns generic encoders); JS snippet library skipped (Hermes covers cross-device snippets) | One tool, one home; owner decisions |
| 2026-07-23 | **PLAN-12 review pass (owner) + Loki card tone chosen.** Entry points re-verified against the live codebase and the plan corrected: acorn/`@codemirror/lang-javascript`/Prettier/Terser are all **new** deps (none currently in the tree); Variant's before/after handoff needs a new text seed (its `?left/?right` is slug-only); execution is local-only via the `profile === 'local'` client gate (module stays in both profiles); JS mode plugs into the shared `JsonEditor` the same way markdown did in PLAN-11 (reusing its `applyEdit` handle). **4th card tone = `--tone-loki` (emerald green — Loki's colour)** with a deliberately **different gradient effect** from the teal/violet/amber cards: a two-stop diagonal sweep that shifts hue (emerald → an ember/lime `--tone-loki-2`, the Calcifer flicker) so the card visibly "shapeshifts", optional slow drift gated by `prefers-reduced-motion`. Tokens (dark+light) in `tokens.css`, `.hub-card--loki` in `app.css`, wired as the 4th Ollivanders card | Owner asked to review the plan against current state and gave the tool its own distinct 4th gradient; green fits Loki, and a shifting two-hue sweep reads as the shapeshifter rather than a flat 4th colour |
| 2026-07-24 | **PLAN-12 Part A first-build scoping (at the plan's UI checkpoint).** The checkpoint is "before polish", so the first build ships the workbench with **client-side defaults** and the **Heimdall "Loki" settings card is deferred to post-checkpoint polish** — panel-font size + default mode persist **per device** in `localStorage` (`loki.panelFontPx`/`loki.mode`), beautify uses fixed `{tabWidth:2, singleQuote:true, semi:true}`, minify uses `mangle:true`, and stringify defaults to double quotes (a quote-style selector is also deferred). The `LOKI_RUN_TIMEOUT_MS`/`LOKI_CONSOLE_MAX_ENTRIES` env keys + `config.loki` group were still added now (Part B reads them). The **Variant before/after handoff** is realised as a boundary-legal `core/variantSeed` (a `sessionStorage` bridge Loki writes and Variant reads once on mount → text mode), per the plan's "new text seed" correction — no cross-feature import | The plan explicitly pauses for an owner UI review before polish; wiring the full settings round-trip first would mean re-doing it after the checkpoint. Per-device localStorage matches the plan's "chosen size persists per device" and the PLAN-11 draft-cache precedent; the seed helper lives in `core/` because features can't import features |
| 2026-07-24 | **Multi-tab SSE stall diagnosed → deferred to PLAN-99 (owner).** Tabs stuck on the "Crossing the bridge…" loader are caused by HTTP/1.1's ~6-connections-per-origin browser limit being fully consumed by one permanent `EventSource` per tab, so lazy route-chunk `import()`s queue forever (reproduced headless: closing a tab instantly unblocks a stalled one). Rather than patch now, the owner chose to log the full RCA + the leader-elected single-SSE fan-out solution (SharedWorker/BroadcastChannel) as a PLAN-99 backlog item, with the `visibilitychange` pause as the noted interim | It's a cross-cutting rework of `core/sse.ts` deserving its own spike/plan; documenting the RCA + chosen approach now prevents rediscovery, and Part B (Loki execution) is the scheduled work |
| 2026-07-24 | **PLAN-12 Part B "Calcifer" — execution model + settings boundary.** Sandboxed execution is an **ephemeral module Web Worker per run** (no DOM/localStorage/cookies, killable) — the watchdog and Stop both `terminate()`, a `runId` filters stale messages, and every value is **serialized to a display string inside the worker** before postMessage (structuredClone can't carry functions and loses `undefined`). Single-expression detection tries `return (code)` first (REPL value), falls back to statements. **Boundary decision:** rather than couple Loki's settings into Heimdall's closed settings usecase, the `loki` module **owns its own routes** — public `GET /api/loki/config` (the page reads it to gate the Run UI + pass runner limits) and admin `PATCH /api/loki/settings`, settings-table backed with env seeds + config overlays, broadcasting a new `loki.settingsUpdated` bus→SSE event for live rebind. The Heimdall "Loki" card is a client section that calls those endpoints (client features may fetch any API; only imports are boundary-gated). Execution is gated on `capabilities.profile === 'local'` **AND** the master switch; `fetch()` inside a run is Heimdall-toggleable | The worker is the right threat model (own snippets/accidents, not hostile code) and is the only killable option; owning the settings routes keeps heimdall↔loki decoupled while still riding the established settings-table + SSE-rebind pattern; the profile+switch double gate makes execution unreachable in cloud |
| 2026-07-22 | Three small modules planned from the idea round: **Accio** (PLAN-13, read-later shelf), **Nimbus** (PLAN-14, LAN speed test), **Portkey** (PLAN-15, go-links), sequenced after Loki; Pythia/Howler/Time-Turner/Echo/Iris/Skald/Argus parked in PLAN-99 with synergy notes | Owner selection |
| 2026-07-22 | Speed test renamed **Sleipnir → Nimbus** (Potter, the fast broom) for pronounceability; name collisions with external tools accepted as before | Owner decision — nobody should need to know it's "SLAYP-neer" |
| 2026-07-22 | Accio vs Hermes intent split: Hermes *passes* (ephemeral stream), Accio *keeps* (tagged shelf); URL entries in Hermes gain an "Accio it" action via API/bus handoff, never a cross-feature import. Title enrichment is best-effort post-save (3s timeout, SSE patch-in) so no-internet LAN degrades gracefully | Distinct intents justify distinct modules; boundary rules intact |
| 2026-07-22 | Nimbus methodology: median-of-10 latency, warmup transfer, incompressible pre-generated stream with identity encoding (compression would fake Mbps), upload discarded server-side (zero disk writes), single-flight guard (409 during a running test), approximate-by-design stated on-page | Honest numbers over impressive ones |
| 2026-07-22 | Portkey: user-chosen slugs (memorability IS the feature), 302-only redirects (301 caching would pin stale targets), async hit counting after the redirect, reserved-roots list centralized in core, targets restricted to http(s) but any host; **local profile permanently — a public go-links service is an open-redirect primitive** | Security stance logged so cloud-profile work never resurrects it |
| 2026-07-24 | Card colours moved off the atmospheric `--tone-teal/violet/amber` system onto a dedicated **10-slot card palette** (`--card-1..10`, class `.card-tone-N`, assigned via `cardToneClass(n)` which wraps after 10). Base colour only; tint + hover glow mixed via `color-mix`. `--tone-*` kept for eyebrows/code-highlights/join-band. Each theme defines its own 10 hues; two-colour house themes get ten *distinguishable shades*, not the default rainbow. Loki keeps its bespoke animated card as the one exception | The old 3-tone card language only exposed teal/violet to themes while amber was stuck at a CSS default — in warm themes (Gryffindor, Olympus) violet and amber both resolved to gold, so adjacent cards (Variant≈Edda, Receive≈Hermes) looked identical. A per-theme numbered palette guarantees per-card distinctness and cycles automatically for future cards |
| 2026-07-25 | Idle screensaver named **Vörðr** (ON "warden/watcher"); tagline "keeping watch over the bridge" + a live clock. Replaces the reference mock's off-lore "ALFRED" | Owner delegated the name; the guardian-who-watches-while-you're-away fits the Norse world and Heimdall lore. Kept as one constant so it's trivially renamable |
| 2026-07-25 | Screensaver is **desktop-only, never merely hidden**: gated on `(hover:hover)+(pointer:fine)` + `maxTouchPoints<=0` + viewport width ≥1024; on a phone/tablet the config is never fetched, the idle timer never armed, the canvas chunk never imported | PRD's hard constraint — a hidden-but-running canvas still burns CPU, and touch devices shouldn't pay for a mouse-oriented flourish |
| 2026-07-25 | Screensaver **dismissal = click (ripple then fade) or keystroke only**; `mousemove`/`scroll`/`wheel` keep it alive and `mousemove` drives particle parallax — deliberately *not* the PRD's "any input dismisses instantly" | Owner's explicit model: it's an interactive "in space" toy you part with the cursor, then click/press to leave — not a get-out-of-my-way overlay |
| 2026-07-25 | New **policy-only `screensaver` server module** (both profiles) rather than pure client state: env-seeded settings in the DB `settings` table, public `GET /api/screensaver/config` + admin `PATCH` + `screensaver.settingsUpdated`→SSE, exactly mirroring the Loki settings module. All rendering stays client-side | Owner wanted the timing/animation Heimdall-tunable and live across open screens; the Loki pattern already solves persisted, admin-gated, live-rebinding settings |
| 2026-07-25 | Screensaver colours via a new optional **`--screen-*` theme-token group** (schema `SCREENSAVER_TOKENS`; tokens.css dark+light defaults; per-identity-theme overrides), read from the canvas at draw time via `getComputedStyle` (QrCard pattern) with a fallback chain to `--stars`/`--accent`/`--bg`. Light themes get dark motes (a dark theme's pale stars vanish on light) | "Respect the theme palette" without hardcoding colours in JS; the fallback chain means even future user themes get an on-brand idle sky for free, while houses can tune |
| 2026-07-25 | Screensaver persona renamed **Vörðr → Nótt** (supersedes the earlier row), tagline "keeping watch over the bridge" → **"night over the bridge"**. Full rename: display strings, `.nott*` CSS classes, Heimdall `NottSection`/`nott-*` control ids, and comments. Internal ids stay generic — server module `screensaver`, `/api/screensaver/*`, `features/screensaver/`, `--screen-*` tokens all unchanged | Owner picked Nótt (Norse goddess of night) over the guardian framing — "the site enters night" fits an idle screensaver better. Kept the functional ids stable so the rename is display-only and no routes/tokens churn |
| 2026-07-25 | **Accio (PLAN-13) registers in the `local` profile only** | A household bookmark shelf has no auth story of its own; every row is writable by anyone who can reach the LAN. Plan decision — revisit for `cloud` when real accounts exist |
| 2026-07-25 | Title enrichment is triggered from **`module.ts` off the `accio.saved` bus event**, not from the save usecase or route: the POST returns 201 the moment the row exists, and the title lands later as `accio.updated` → SSE. `EnrichTitleUseCase` re-reads the row after the fetch and **never overwrites a title the user typed meanwhile** (nor resurrects a deleted row); the module tracks in-flight lookups and `onClose` awaits them | Acceptance criterion 1 is precisely "the row appears instantly, the title never blocks". Keeping the trigger in the composition root leaves the usecase free of fire-and-forget IO, and the re-read makes the race the plan implies (edit while offline-ish fetch is in flight) resolve in the user's favour |
| 2026-07-25 | The Accio shelf UI **filters and sorts client-side** over the SSE-maintained list; the server's `q`/`tag`/`sort` params stay the documented API contract (and are integration-tested) but the page does not use them | Live updates and server-side filtering do not compose: an `accio.saved` event would need a refetch just to learn whether the new row passes the active filter. One full fetch + SSE deltas keeps insert/patch/delete local and instant, and a household shelf is hundreds of rows, not millions |
| 2026-07-25 | The shelf's letter tile takes its card-palette slot from a **hostname hash**, not the render index — a scoped exception to the "colour follows position" rule in `rules/coding.md` (which governs hub/portal cards) | The tile exists to make a familiar domain recognisable before the title is read; that only works if github.com keeps one colour while the list is searched, filtered and re-sorted. Portal/hub cards are unchanged and still positional |
| 2026-07-25 | `accio.saved`/`accio.deleted` are audited; **`accio.updated` is not** | Enrichment patches a row shortly after almost every save — auditing them would double the log's volume with no signal about anything a person did |
| 2026-07-25 | Hermes' "Accio it" attributes the shelf row to the **device that pressed it**, not the device that originally posted the clipboard entry | The action is an ordinary browser call to the public Accio API, so `X-Bifrost-Device` is the summoning device; that also matches how every other write in Bifrost attributes authorship. Reading the plan's "source device" as the poster would need clipboard→accio coupling the boundaries forbid |
| 2026-07-25 | URL normalization treats `host:3000/path` as a **scheme-less host with a port** (→ `https://`), and only a colon *not* followed by a digit as a real scheme | `localhost:3000/admin` and `192.168.1.4:8080` are exactly what gets pasted onto a LAN household shelf; parsing "localhost" as a URL scheme would 422 the most common local case |
| 2026-07-25 | Accio accepts a **second class of address: browser-internal pages** (`chrome://chrome-urls/`, `chrome://net-internals/#dns`, `about:config`, `edge:`/`brave:`/`vivaldi:`/`opera:`/`arc:`/`devtools:`) alongside http(s). The blocklist is unchanged — `javascript:`/`data:`/`vbscript:`/`file:`/`blob:` and non-page schemes (`mailto:`, `tel:`, deeplinks) still 422 | Owner request. These are genuinely worth shelving, and they are **inert from page content** — a browser refuses to navigate to them from a link, so a stored row can't act. That is the whole security distinction from `javascript:`, which executes in the page |
| 2026-07-25 | A browser-internal row renders as **copy-me text with a "browser page" badge**, never as an anchor; its meta line reads "copy it into the address bar", its title is shown **verbatim** (trailing slash kept), and its tile takes the *scheme's* letter and colour so all `chrome://` rows group | An anchor to `chrome://` silently does nothing when clicked — offering one would be a lie. The exact string matters because it has to be pasted, and the scheme-derived tile keeps the hash-tone rule meaningful for URLs whose "host" is really a page name |
| 2026-07-25 | Title enrichment is **skipped for non-web rows** (guarded in `EnrichTitleUseCase` via `isWebUrl`, not in the fetcher) | `fetch('chrome://…')` throws; more to the point there is nothing to fetch. Guarding in the usecase makes it a unit test with a mock that asserts the fetcher was never called |
| 2026-07-25 | The shelf's tag field is a **custom suggestion list**, not a `<datalist>`: focus/click offers every tag on the shelf, typing narrows to the fragment after the last comma, picking completes it and leaves the list open for the next one | Owner request ("clicking should show already-added tags"). `<datalist>` matches against the whole field value, so it stops suggesting the moment the field holds more than one comma-separated tag — useless for this input. The fragment mechanics are pure functions (`tagInput.ts`) so the component only renders |
| 2026-07-25 | The save error clears on the next keystroke in the URL field | Owner request. The message describes what was typed before; once that changes it is stale, and a stuck red line reads as an unfixable failure |
| 2026-07-25 | **Supersedes the three "browser-internal page" rows above.** Accio does no per-scheme handling at all: `normalizeUrl` rejects **only** the schemes that execute or embed inside the page (`javascript:`, `data:`, `vbscript:`, `blob:`, `filesystem:`) and passes everything else through — `chrome://`, `about:`, `mailto:`, `ftp:`, app deeplinks. Every row renders as an ordinary `target="_blank"` anchor; the browser or the OS decides what a click means. The only remaining scheme check is `isWebUrl`, guarding title enrichment (nothing else has a page to fetch) | Owner: "for schemes other than http don't do any special handling… don't overengineer these things." The curated allow-list, the "browser page" badge, the copy-me rendering and the scheme-derived tile were all invented complexity for a case the platform already handles. A blocklist of page-executing schemes is the one rule that is actually load-bearing (a stored row becomes an `href`), and it matches the existing `linkify` precedent |
| 2026-07-25 | The Accio shelf is a **grid of compact cards inside the ≥1024px full-width breakout**, not a one-per-row list: `repeat(auto-fill, minmax(16rem, 1fr))`, ~126px cards, whole-card stretched link, hover/focus-revealed corner actions (always shown on touch), filter bar sticky only ≥768px | Owner: "as the list grows users will have to do long scrolling… since it's a bookmark page there will be a lot of them." A shelf is a browsing surface, so it earns the same breakout Variant/Runestone/Edda use. Measured 15 links on one 1600×1000 screen vs 3 before; auto-fill means the column count follows the viewport with no per-device breakpoints |
| 2026-07-25 | **npm audit swept 17 → 6.** Fixed forward, no declared range changes (all inside existing carets, lockfile only): **`@fastify/static` 10.1.0 → 10.1.2** (authorization bypass via non-canonical URL paths + route-guard bypass via path traversal — the only advisory that touched a real runtime surface, since this is what serves the SPA), `find-my-way` 9.6.0 → 9.7.0 (HTTP/2 DoS; we never enable HTTP/2, so unreachable, but the fix is free), `fast-uri` → 3.1.4 (host confusion via a literal-backslash authority), `postcss` → 8.5.23 (source-map path traversal, build-time), `concurrently` 10.0.4 / `shell-quote` 1.9.0 (quadratic `parse()` DoS, dev script runner) | These are patch-level and non-breaking, so there is nothing to weigh: an unexploitable advisory still costs a reader time on every `npm install`, and a genuinely exploitable one (the static-serving bypass) has no excuse to sit unpatched on a host that serves a LAN |
| 2026-07-25 | The **eslint cluster's five "high" rows are one root advisory** — `brace-expansion` unbounded-expansion DoS, reaching us only through eslint 9's `minimatch@3`; `@eslint/config-array`, `@eslint/eslintrc`, `minimatch` and `eslint` are flagged merely as dependents. Fixed with a root **`overrides: { "brace-expansion": "^5.0.8" }`**, not the `eslint@10` major npm proposes | The override keeps the 2026-07-12 decision intact (ESLint held at 9 because typescript-eslint 8 targets it) while removing the actual defect. brace-expansion's API is one exported function and is unchanged across v1→v5, and `eslint .` over the whole repo exercises minimatch's glob path heavily — a broken override would fail the gate loudly, and does not |
| 2026-07-25 | **Two advisories deliberately accepted, not fixed.** (1) `react-router` 7.18.1 — "RSC Mode CSRF Bypass" (GHSA-qwww-vcr4-c8h2, unscored). **Not reachable**: verified the app uses only `BrowserRouter`/`Routes`/`Route` + hooks — no `createBrowserRouter`, no `RouterProvider`, no loaders/actions/fetchers, no RSC mode, and no react-router server runtime at all (Fastify serves static files). There is no patched 7.x (7.18.1 is the last), so npm's "fix" is a **downgrade to 7.11.0**, and the only forward path is the react-router **8 major**, where `react-router-dom` is subsumed into `react-router` and all 11 importing files change. (2) The 4 moderates are the same drizzle-kit → `@esbuild-kit` → old-esbuild chain accepted on 2026-07-12; drizzle-kit 0.31.10 is already the latest, so the only "fix" remains a downgrade to 0.18.1 | Both fixes would be strictly worse than the defect: giving up seven minor releases of real fixes, or a framework major migration, to close a hole that requires a mode we do not run. Logged so the next `npm install` warning is recognised rather than re-investigated. Revisit react-router 8 as its own scoped change if the owner wants a clean audit |
| 2026-07-25 | **PLAN-14 (Nimbus) — the client owns the clock; the server owns the bytes and the guard.** Latency is measured client-side (median of ten bodyless 204 pings), download is timed **from the first chunk** (time-to-first-byte is the server thinking, and folding it in would under-report the link), and the **upload figure is the server's own `ms`** while the live gauge follows XHR `upload.onprogress` — the browser's `loaded` counter hits 100% when the OS accepts the last byte, which on a slow link is well before it arrives | The number that matters is the one the device in your hand sees, so the timing lives there; but for upload only the receiver knows when the bytes actually landed. XHR (not fetch) for the same reason PLAN-02 chose it: fetch has no upload-progress events, and a speed test without a live number is a spinner |
| 2026-07-25 | Nimbus's single-flight guard is a **lease held per device key across a whole test**, renewed by each request and released a 5 s **grace period** after the last one ends (`reply.raw.on('close')`, so an aborted transfer counts as ended), plus an explicit `POST /api/nimbus/release` for the cancel/finish path. `GET /ping` is deliberately **ungated** | A test is several requests, so a per-request guard would let another device slip in between phases; a lease with an expiry needs no timer or cleanup task and cannot wedge the server if a client closes its laptop mid-test. Ping is too small to disturb a transfer, and gating it would mean a busy bridge couldn't even be pinged |
| 2026-07-25 | The upload sink is a **custom `application/octet-stream` content-type parser that takes the raw stream, counts, and keeps nothing**; over-cap bodies are refused from an `onRequest` hook (before any bytes are read) and the stream is simply not drained — deliberately unlike PLAN-02's oversize-upload drain | With no file and no buffer, "writes nothing to disk" and "memory stays flat" are properties of the shape, not of a cleanup path. The drain in PLAN-02 exists to save the *other* files in a multipart batch; here there is nothing to preserve, so draining 100 MB would only waste the link the user is trying to measure |
| 2026-07-25 | Nimbus adds two endpoints the plan's contract table doesn't list: `GET /api/nimbus/config` (maxTestMb · offered sizes · pingSamples · busy state) and `POST /api/nimbus/release` | Limits live in `.env` and the UI must read them (CLAUDE.md), so the size menu is server-derived like `/api/files/config` and `/api/loki/config`; the release route is what makes the plan's own "cancel … and the server guard releases" immediate rather than grace-period-delayed |
| 2026-07-25 | Only a **complete** test is recorded — a cancelled or failed run saves nothing — and the reported figures are validated as *plausible* (finite, non-negative, ≤ 100 000 Mbps / ≤ 60 s latency, testMb inside the configured range → 422) and rounded to two decimals | The client measures its own numbers, so this is not a trust boundary; it is the line between a Wi-Fi reading and a value that would wreck every sparkline it lands in. Partial results would make the history dishonest about what was measured |
| 2026-07-25 | Nimbus history is pruned on the **audit retention policy** (`AUDIT_RETENTION_DAYS`, boot + daily, the audit-log module's pattern) rather than its own setting | History exists to answer "is the bedroom still slow"; a reading from last spring answers nothing, and one retention number for all recorded activity is one fewer knob |
| 2026-07-25 | Nimbus CSS lives in **`client/src/features/nimbus/nimbus.css`** (imported by the page), not in `app.css` — the screensaver's precedent | The broom track, the gauge and the sparkline are used on exactly one page; app.css is already 3.5k lines and a shared stylesheet should hold shared things |
| 2026-07-25 | The live gauge's "no reading yet" placeholder gets its **own muted, ordinary-sized style** (`--idle`), and the ping phase publishes a **running median** after every sample instead of one figure at the end | Found in live verification: an em dash at gauge size (88px) renders as a fat teal bar that reads as a broken element, and it was on screen for the whole latency phase. A running median gives the gauge something true to show from the first round trip |
| 2026-07-25 | New `--header-h` token (3.875rem = the shell header's measured 62px, constant at every width) in app.css; anything sticky parks below the header with `top: var(--header-h)` instead of guessing. The Accio filter bar uses the header's own `--header-veil` + `backdrop-filter: blur(14px)` + border/radius rather than a flat `--bg` fill | Owner reported the search area as a black slab: `--bg` is opaque and the page background is a gradient sky, so the bar read as a rectangle punched through it. Matching the one frosted-sticky surface the app already ships keeps the two consistent in every theme. The header is itself `position: sticky; top: 0; z-index: 10`, so `top: 0` on a second sticky bar hides it *behind* the header — the token makes that offset explicit and reusable |
| 2026-07-26 | **PLAN-15 (Portkey) implemented.** `slug` IS the table primary key and is **immutable** — a Portkey's identity is its memorable word (and every printed QR points at it), so a rename is delete + recreate, on purpose (PATCH edits url/note only). Redirect is **`GET /go/:slug` outside `/api/`** (a real route beats the SPA fallback, so `bifrost.local/go/router` is the whole address), **always 302 + `no-store`** (a target moves; 301 pins it in caches). The hit write is **out of band** — the route schedules it via `setImmediate` *after* `reply.redirect`, guarded against shutdown, its failure a `warn` (a lost count is cosmetic, never a failed hop) — and a dedicated **`portkey.hit`** event broadcasts the bumped row over SSE so open pages update live. Unknown slug **302-bounces to `/portkey?go=<slug>`** (creative-404, form pre-filled), not a 404 HTML page — keeps the enchant flow in the SPA. **Local profile only, permanently** (open-redirect/phishing primitive on a public host). | Owner-scheduled; the plan's decisions logged 2026-07-22. Slug-as-PK makes uniqueness + resolution one indexed lookup; the async-hit + 302/no-store rules are the plan's acceptance-1 latency guarantee made structural; a redirect to the pre-filled form is the Runestone/Edda creative-404 pattern's third verse |
| 2026-07-26 | The **reserved-words list lives in `server/src/core/reserved-roots.ts`** (the app's own path roots + `api`/`go`), not in the portkey module, and is asserted by an Edda/Runestone-style guard test. `portkey.hit` broadcasts over SSE but is **deliberately not audited** (only `portkey.saved`/`portkey.deleted` are) — a redirect happens constantly and says nothing about a person's action, same reasoning as `accio.updated`. **Small additive core change**: `ApiError` (client) now parses the server's `{error,message}` body so a Portkey 422 shows the specific reason (bad slug vs reserved vs bad scheme); existing consumers only read `.status`, so it's non-breaking. Scheme-less targets default to **`https://`** (accio precedent) — a plain-http LAN device works when its `http://` is typed. | The plan wants the reserved list as one core source (multiple route roots reference it conceptually); auditing hits would double the log with noise; the client couldn't surface a useful create error from a bare status code, and the reason text is exactly what makes the 422 actionable |
| 2026-07-26 | **Maintenance rule documented (owner):** adding any new **top-level route root** (a server route outside `/api/`, or a first-segment client route in `App.tsx`) requires adding that root to `core/reserved-roots.ts` **and** its guard test in the same change — written into `rules/coding.md` (new "Routing" section) and the `new-module` skill. The guard test only asserts the roots it already knows; it cannot discover new ones. | A missed entry silently lets a Portkey slug `/go/<name>` shadow a real page; the rule makes the coupling explicit where a new route is actually added |
| 2026-07-26 | **Duplicate-slug UX (owner chose "suggest a free variant" over auto-suffix or plain 409):** the create API contract stays a clean **409** (memorable slugs are the point, so the server never silently renames a user's choice to `router-a4`), but the management UI, on 409, computes the nearest still-memorable free variant from the live list (`router` → `router-2`, `router-3`, …; pure `suggestSlug`) and offers it as a **one-click "Use /go/router-2"** beside "View it". A race (another device took the same variant) just re-suggests on the next click. Entirely client-side — no server change, no new endpoint. | Owner picked this over the originally-requested random-char append: a numeric variant stays typeable/memorable, the API stays honest (409, not a surprise slug), and the live list the page already holds makes the suggestion free of a round trip |
| 2026-07-29 | **PLAN-16a: Heimdall's Logs section is deleted entirely** — the tail, the module/level filters, the SSE follow stream, `core/logtap.ts`, **and** the runtime level switch. Grafana/Loki is the log UI; `LOG_LEVEL` in `.env` + a restart is the only control. `routes/observability.ts` shrank to About and was renamed `routes/about.ts` (its deps went from seven fields to `config`) | An in-app viewer over a stream the plan was about to widen is a worse Grafana. The contract win is the point: `logTap` and `setLogLevel` both leave `ModuleDeps`, and with them go the `moduleLoggers[]` array (which existed *solely* so a level change could walk pino's children), the "apply the persisted level before child loggers are created" ordering constraint, and the settings-table `logLevel` key. Accepted trade: no in-app log view when Docker is down (`npm run logs` + `jq` cover it), and a level change needs a restart |
| 2026-07-29 | **The persisted `log.level` settings row is dropped from the overlay and deleted by migration `0009`** (data-only), and `applySettingsOverlay` now deliberately ignores the key — with a comment and a test pinning that | Leaving a stale row with no UI to change it would strand an upgraded install at whatever level it was last set to, permanently. The test matters because a DB restored from an old backup can still carry the row after the migration has run once |
| 2026-07-29 | **pino's numeric `level` is replaced by the text key `logLevel`** (`formatters.level(label) => ({ logLevel: label })`, numeric dropped). Old files are **never rewritten**: Alloy's `stage.template` fills `logLevel` in from the number when the key is absent, so one label spans the whole archive. camelCase because Loki label names forbid hyphens. `pino-pretty` is told `levelKey: 'logLevel'` in **both** places that use it — the dev transport target and the root `logs` script | Filtering the primary log UI on `{level="50"}` instead of `{level="error"}` is unusable. Verified through a real transport worker: formatters run on the main thread, so the output is `{"logLevel":"info",…}` with no numeric key. Missing the second `levelKey` would silently degrade the designated no-Docker fallback to no level name and no colour — checked by rendering both ways |
| 2026-07-29 | **`LOG_LEVEL` default moves `info` → `trace`**, and `LOG_RETENTION_FILES` (default 30) becomes pino-roll's file-count limit with **`removeOtherLogFiles: true`** | With no in-app reader the file is a pure archive feeding Loki, so the level is a query-time filter, not a write-time decision that discards data permanently. Trace volume then forces the retention question: `rollOptions` had no `limit` at all, so rotated files accumulated forever. `removeOtherLogFiles` is load-bearing rather than a flourish — pino-roll otherwise counts only files the *current* process wrote, and Bifrost restarts constantly, so every restart would start a fresh tally and sweep nothing |
| 2026-07-29 | **Alloy's glob narrows from `storage/logs/*.log` to `app*.log`** | Pre-existing bug found while migrating the panels: `current.log` is a **symlink** onto the active file, so the wide glob tailed the live file twice under two names and doubled every count on the dashboard — in the panels this plan was making authoritative. It also excludes `launchd-error.log`, which is raw stderr, not pino JSON |
| 2026-07-29 | **Client-side logging exists for the first time**, and rides the existing pipeline: a batched `POST /api/client-logs` re-emitted through pino, plus an `ErrorBoundary` around `<App />`, `window.onerror`, `unhandledrejection`, and `build.sourcemap: true`. The client ships **`warn`+**, floor delivered by `GET /api/client-logs/config` (the `/api/loki/config` pattern) with a `warn` fallback when that request fails, and **enforced again server-side** | Bifrost is opened from phones the owner is not sitting at, so a React crash there had no path back at all — the one gap where the data existed nowhere, rather than existing but being unqueryable. Riding pino means no Faro, no extra container, no second retention story. The server keeps `trace` because appending locally is free; a client line crosses the network into an unauthenticated endpoint, so the economics invert. Sourcemaps are a prerequisite, not polish: without them every reported stack is a minified hash |
| 2026-07-29 | **`source` is a *child* binding on both sides — `moduleLogger` binds `{source:'server', module}`, `clientLogger` binds `{source:'client', module}`, and both are siblings of a source-free root.** `ModuleDeps` gains one narrow field, `clientLog(moduleName)`, so the `client-logs` module can reach a root-derived logger | pino **appends** child bindings to the parent's rather than overriding them, so `base: {source:'server'}` + `child({source:'client'})` — or a child of the module logger — emits `{"source":"server","source":"client"}`. Every parser in the chain happens to take the last occurrence, so it would appear to work while resting on undefined behaviour; a unit test pins both the correct construction and the trap. Sibling children are the only way one process writes two values under one key, which is why the factory has to come from the composition root rather than from `deps.log` |
| 2026-07-29 | **Client lines carry the reporting *feature* as `module`**, not `client-logs`, and root-logger lines (boot, shutdown, Fastify request logs) carry no `source` at all — Alloy defaults those to `server` | Ten names already match exactly across the two workspaces, so `{module="accio"}` returns both halves of a feature and `source="client"` narrows it. Attributing browser lines to the relaying module would have thrown that away for nothing |
| 2026-07-29 | **Domain errors (`AppError`) are logged once, centrally, at `debug` in `setErrorHandler`** — code, status, and reason — instead of a line per usecase | Fastify's "request completed" line carries the 4xx status but never the *why*, so a rejected settings patch, a 422 slug and a 404 download all left no trace of what was wrong. One place covers every module including the ones the plan listed individually (`manage-settings`), and `debug` is right because a domain rejection is the system working — at the new trace default it still reaches disk |
| 2026-07-29 | **`POST /api/client-logs` is bounded by three separate limits** — `CLIENT_LOG_RATE_LIMIT_PER_MIN` (batches/min/IP), `CLIENT_LOG_MAX_BATCH` (entries/batch), `CLIENT_LOG_MAX_BODY_KB` (413 before the body is read) — and the client **drops a failed batch rather than retrying**, with a queue cap | It is an unauthenticated write path into the same files the server's own archive lives in, so a misbehaving tab must not be able to fill `storage/logs/`. Retrying into a refusing endpoint would grow the queue and the request rate at exactly the moment the page is already in trouble, and the rate limiter would refuse it anyway |
| 2026-07-29 | **PLAN-16b: the metrics snapshot is the system of record; Prometheus and Tempo are accelerators.** A `module:"metrics"` line per interval goes into the same pino stream, so the runtime history inherits the log files' backfill and exists with zero Docker dependency. The dashboard says so out loud — the Runtime row queries Loki, the Prometheus row is labelled "live only, gaps do not backfill" | Alloy's backfill guarantee is a property of *tailing files*, not of the stack. Prometheus **scrapes** (down = the data never existed, no backfill possible) and Tempo is **pushed to** (down = buffer, retry, drop). The owner runs Docker far less often than Bifrost, so anything that only exists while a container is up cannot answer "why did it hang at 3am last Tuesday?" |
| 2026-07-29 | **Deltas, never cumulative counters, in the log line** — `uploadsDelta: 3` since the last snapshot, not `uploadsTotal: 4711`. Prometheus gets the opposite (`bifrost_uploads_total`, a counter) | Deltas sum trivially over any window in LogQL and are immune to the counter-reset problem: a cumulative counter drops to zero on every Bifrost restart and corrupts every rate spanning that point. Prometheus handles resets itself via `rate()`, and LogQL has no equivalent — so each side gets the shape its query language can actually use. A property test pins that summing deltas equals the event count |
| 2026-07-29 | **`diskMb` is sampled on its own slow cycle** (`METRICS_DISK_INTERVAL_SEC`, default 1800), carrying the last value between ticks, and the walk itself moved to **`core/disk-usage.ts`** with Heimdall repointed at it | Sampling it per snapshot would block the event loop per snapshot — the sampler would faithfully record the lag spike it had just caused, in the metric that exists to detect blocking. The lift into core is forced by the boundary rule: `metrics` cannot import `heimdall`, and two walkers would eventually disagree. A test on the Heimdall side pins that its numbers did not move |
| 2026-07-29 | **Snapshots log through a `trace`-pinned child**, so `METRICS_ENABLED` is the only off-switch | Snapshots are log lines, so raising `LOG_LEVEL` to `warn` — the escape hatch PLAN-16a deliberately kept — would otherwise silently stop the durable record. Verified: with the root at `error`, the pinned child still writes while ordinary `info` lines stop |
| 2026-07-29 | **`core/http` emits `http.requestCompleted` on the bus, and `metrics` subscribes** — rather than the metrics module adding its own Fastify hook | A Fastify hook only ever sees the encapsulation context it was added in, and the composition root wraps every module in its own; even `fastify-plugin` would lift a hook just one level, to that wrapper. Found by testing: the histogram recorded only the metrics module's own routes. The bus is the architecture's existing answer to "one module needs to know what the whole app is doing", and it keeps prom-client out of core. `fastify-plugin` was installed for the hook approach and removed again |
| 2026-07-29 | **The histogram labels the route TEMPLATE** (`/api/downloads/:id/content`), and everything unmatched shares a single `route="unmatched"` bucket | The raw URL would mint a series per file id, which is how a label set quietly becomes a cardinality problem on a box with no memory headroom |
| 2026-07-29 | **Only metrics that `collectDefaultMetrics` does not already provide are published**, and they use Prometheus base units — `bifrost_event_loop_lag_p99_seconds`, `bifrost_storage_disk_bytes` — while the log line keeps milliseconds and MB | Caught by running `promtool check metrics` against the real exposition: the first pass duplicated `process_resident_memory_bytes` and `nodejs_heap_size_used_bytes` from the sampler and used abbreviated units. Two series for one number is how two sources of truth start disagreeing; the units differ per audience (a human reads ms, Prometheus expects seconds) |
| 2026-07-29 | **`node_exporter` is deliberately excluded from the stack** | In Docker on macOS it measures the Linux VM, not the Mac — numbers that look real and mean nothing. Every process metric comes from inside Node |
| 2026-07-29 | **`/metrics` is unauthenticated on the LAN**, and `metrics` was added to `core/reserved-roots.ts` | Prometheus carries no session, so `requireAdmin` would not secure anything — it would simply break scraping. It exposes counters and gauges, never content, which matches the existing LAN trust model; a bearer token is the escape hatch if that changes. The reserved-root entry is required by the routing rule: `/metrics` is a new top-level route, and without it a Portkey slug `go/metrics` could shadow it |
| 2026-07-29 | **OTel is off by default and loaded via `node --import ./server/dist/otel.js`** (npm start, PM2 `node_args`, Dockerfile), with a startup line naming the endpoint and instrumentation count, and **rate-limited** exporter-failure reporting on stderr | ESM hoists imports, so initialising the SDK from application code instruments nothing — silently, with no error. Four faults present identically as "no traces appear", so the startup line's *absence* is the signal for the `--import` trap, and bounded failure reporting keeps a dead collector from flooding the archive it is supposed to protect. Verified live: the line appears only with the flag, and with Tempo down a 21-second run produced exactly one failure line |
| 2026-07-29 | **Alert rules query Loki, not Prometheus**, and live in a folder named "Bifrost alerts" | A rule against a scraped series would silently evaluate over holes whenever the stack had been down — the archive is the thing that backfills. The folder name is not "Bifrost" because Grafana refuses a folder whose name matches a dashboard's and **crash-loops on boot**; found by actually running the stack |
| 2026-07-30 | **PLAN-17a: `core/notify` is a store + a host + an imperative handle, and `core/ui/Toast` stays as-is.** The handle (`notify.error(msg, opts)`) is deliberately not a hook, and the host is mounted once in `App.tsx` **outside `<Routes>`** and portalled to `<body>` | The callers that most need a notification are the ones that cannot reach a React context: promise rejections, event handlers, and 17b's SSE subscriber. Toast and this are different objects — Toast is *in-flow* and lives as long as a page's state says so; a notification is global, stacked, transient and self-dismissing — so migrating the five hand-rolled `notice` states would be churn for its own sake. Outside `<Routes>` is what makes "raised on one page, still readable on the next" true; the portal is the floating-Toast lesson (`isolation: isolate` on `.json-editor` would otherwise trap it) |
| 2026-07-30 | **The stack cap is per kind and enforced by arithmetic: `MAX_VISIBLE = 4`, `MAX_ERRORS = MAX_VISIBLE - 1`.** A fourth error evicts the oldest error; "Dismiss all errors (N)" appears once more than one is stacked | Errors deliberately never auto-dismiss (one that vanishes unread is worse than none), so a flat cap of 4 lets four unresolved errors wedge the stack permanently and every later success disappears behind an overflow count with no way to surface it. Holding errors one below the visible cap means any four newest entries contain at most three errors, so a transient notification always has a slot — no reserved-slot special case to get wrong, and it is provable in a unit test |
| 2026-07-30 | **A standing notification is cleared by its `dedupeKey`, not by a captured id** — `notify.dismissKey(key)` returns whether there was anything to clear, and that boolean is what decides whether to show the "back in sync" confirmation | Found in live verification: the downloads hook captured the error's id in its effect closure, so navigating away and back left a stale "could not refresh" error that nothing could clear — the page that recovers is rarely the one that failed. The key already identifies the condition; the id only identifies one mount's attempt to describe it |
| 2026-07-30 | **`shouldShowForOrigin` suppresses only when both device ids are non-null and equal**, and ships in 17a (with tests) although 17b is what calls it | `core/sse` documents a connection's `deviceId` as "null if the client didn't send one", so the obvious `origin === mine` makes `null === null` true and silently swallows the banner on every client lacking an id — a failure that is invisible and reads as "notifications don't work sometimes". The plan gates 17a on that behaviour (criterion 22), so the rule is a tested pure function rather than an inline comparison written later under deadline |
| 2026-07-30 | **The countdown bar is a CSS animation keyed on an `epoch` the store bumps, while the timer itself is JS**; hover/focus pauses both (`animation-play-state` + a stored remaining-ms) | Driving the bar from React state would re-render every notification on a timer for a purely decorative gradient. The epoch is what makes a *collapsed repeat* restart the bar instead of leaving it wherever the first one had got to — a counter that says ×3 over a bar about to expire is a lie about how long the entry has left |
| 2026-07-30 | **17a's adoption replaces the Send page's in-flow error `Toast` rather than adding to it**, and a failing downloads listing gets one standing error plus a transient success on recovery | The plan requires a real adoption, and a banner beside an identical row-level "failed" badge is duplication, not proof — the old Toast also only ever showed the *first* failure of a batch. The listing failure was previously swallowed with "the offline banner tells the story", which is only true when SSE is also down; a stale listing that silently stops refreshing is how "the file isn't there" becomes a mystery |
| 2026-07-30 | **PLAN-17b supersedes two logged decisions, out loud.** `decisions.md:14` (2026-07-12) — *"no read route for uploads/ … write-only by construction"* — and `decisions.md:17` — *"Admin sees upload metadata only; file management stays in Finder"*. `uploads/` is now a staging area with preview, rename, delete and publish routes, and the Send page's "write-only vault — nothing here can be read back" copy is gone (it had become untrue). **Security model: LAN trust** — anyone on the network can publish any staged file; the plan's per-upload capability token was **not** built | Owner reversal, not drift, so it is written down rather than absorbed. The write-only rule made sense when uploads had no UI: it made a whole class of bug impossible for free. It stops being free the moment a sender needs to fix a filename or check they sent the right photo — the alternative is walking to the host. LAN trust matches the rest of the app (everything in `downloads/` is already world-readable on the LAN, Hermes takes any device's text, `/metrics` is unauthenticated); a token store with expiry would be the only stateful auth in the app, and the blast radius it protects against on a household network is "someone else published your file". The `UPLOAD_EXT_BLOCKLIST` comment was corrected in the same change: uploads *are* now served, so the list is load-bearing rather than belt-and-suspenders |
| 2026-07-30 | **A file is placed with `link()` + `unlink()`, never `open(wx)` + `rename()`** — one shared `placeFile` helper for both "upload lands in uploads/" and "move lands in downloads/", capped at 50 collision probes before a short random suffix, with an `EXDEV` fallback that copies through a dot-hidden staging file **inside the destination folder** and fsyncs before linking | `link()` is an atomic no-overwrite reservation *and* the content in one syscall, so the old sequence's window — crash after the placeholder was created, before the rename — can no longer strand a **zero-byte file under the final name** (criterion 24). The old dedupe loop was also unbounded, and its own comment excused that with the timestamp prefix this plan removes: with clean names, a folder holding `report.pdf … report-40.pdf` would cost 41 syscalls on the next upload and grow forever. The staging file must live in the destination (not tmp/) or the final `link` would hit EXDEV again for the same reason the first one did |
| 2026-07-30 | **The move's one remaining window — file hard-linked into `downloads/` but not yet unlinked from `uploads/` — is closed by a boot sweep** that deletes an upload sharing a `dev:ino` with a download, before the watcher's initial scan | Preferring this to the alternative anomaly is the whole point: `link`-then-`unlink` can transiently show a file in *both* folders, while `open(wx)`-then-`rename` can show a *truncated* one. A duplicate hard link costs no disk and loses nothing; an empty file in downloads/ is announced to every device as a real file. Identity is the inode, never the name — a different file that merely shares a name is a coincidence, not a crash artefact |
| 2026-07-30 | **`file.published` owns the banner; `download.added` stays the sole source of the Downloads listing.** Nothing may notify on the second event | They describe the same file and they race: publish emits immediately, chokidar's arrives after its `awaitWriteFinish` debounce. Bannering on both is a guaranteed double notification; bannering on the watcher instead would delay the banner by seconds. The cost is a visible gap — a banner can arrive before the listing has the row — which is stated in the docs rather than papered over |
| 2026-07-30 | **`.svg` is served as `text/plain` from `core/http/mime.ts`**, for uploads *and* downloads | `mime.ts` already refused `text/html` with the comment "an inline-served page would run same-origin scripts" — and SVG carries `<script>`. That was survivable while only `downloads/` (populated by the host via Finder) could be served inline; PLAN-17b makes `uploads/`, writable by anyone on the LAN, previewable. Fixing it in the mime layer closes the download side for free, and costs nothing visible: `previews/kind.ts` already renders SVG as source, deliberately, so only the direct content URL changes behaviour |
| 2026-07-30 | **Preview responsibilities split by ownership, not by URL prefix**: `previews` serves `GET /api/files/:name/preview` (what a file *is*) while `file-transfer` serves `GET /api/files/:name/content` (the bytes) — mirroring the existing downloads split, with one `FsFileInspector` pointed at either folder. The client's viewers now take a **URL** instead of a download id, and the Send page opens the preview as a **route** (`/upload/:name/preview`) rather than importing anything | Modules may not import each other and features may not either, so the alternatives were duplicating the kind-sniffing in file-transfer or duplicating the viewers in the Send page. A route + `<Outlet/>` is how the downloads preview already works, and it keeps back-closes-the-preview behaviour for free |
| 2026-07-30 | **The card's exit is one CSS animation (hold, then swipe), removed on `animationend`** — there is no `dismissing` React state and no `setTimeout` anywhere in the path. Reduced motion swaps in a *still* animation of the same length rather than removing the animation | A timer keeps counting in a backgrounded tab while the animation does not, so the two desync exactly when the user is not looking — the card vanishes before it was read, or lingers after the swipe finished. Folding the plan's `dismissing` state into the animation's own second half removes a step that could disagree with what is on screen. The reduced-motion variant must stay *an animation*, or `animationend` never fires and the card would never be removed at all |
| 2026-07-30 | **Heimdall's Uploads section reads the directory; the dashboard's upload counts read `audit_events`; `upload_audit` is dropped (migration 0010)** | One section was answering two questions and getting both wrong: it claimed to show "everything that has been sent" while listing files deleted months earlier (the owner's screenshot), and counted `.DS_Store` as an upload. A directory listing cannot drift, and `audit_events` already recorded every `file.uploaded` with time, uploader hint, name and size — the dropped table's only unique column, `stored_name`, converged with `original_name` when this plan removed the timestamp prefix. Heimdall reading a table the `audit-log` module conceptually owns is a deliberate shared-database coupling, written into the repository's doc comment rather than hidden |
| 2026-07-30 | **Dot-file filtering lives in `core/disk-usage`, shared by the walk and the new `listFiles`** | The listing and the storage totals disagreeing about what is in a folder is worse than either being wrong: `.DS_Store` was 6 KB of "uploads" in the owner's Heimdall while never appearing as an upload. One predicate, both readers, one answer |
| 2026-07-30 | **`file.published` is deliberately not audited** | `download.added` already records "download available: x" for the same file a moment later, so auditing the publish would double every row. Same reasoning as `portkey.hit` and `accio.updated` |
| 2026-08-02 | **PLAN-99's "Utility toolbox page" (and its "Iris" row) promoted to `PLAN-18-toolbox.md`** — Diagon Alley becomes the toolbox itself: pure-client tools open **in place** as a full-width panel below the source card's row, while tools with a server module and real state (Nimbus, Portkey) keep their page and their card keeps navigating. Two PRs (18a shell + 4 tools, 18b the remaining 9), a capability-only `toolbox` module (the `variant` precedent), and deep-linkable open state at `/diagon-alley/:toolId` | Owner scheduled it with a design sketch. The category shell has existed since the 2026-07-21 nav reorg with three cards permanently reading "coming soon"; the toolbox is a dozen small tools, and a page per tool would mean a dozen routes and a dozen navigations away from the page you were browsing. `diagon-alley` is already a reserved root so nested segments need no `RESERVED_ROOTS` change |
| 2026-08-02 | **Sideways card expansion rejected in favour of one direction — a full-width panel below the row — with a per-tool *inner* layout (`split` \| `full`) doing the work "expand right" was meant to do** | At ≤2 columns sideways degenerates into the same below-panel, so both paths get built and only one gets the benefit; a card in the **last column** has no room to its right, so grid wraps it and the card physically jumps on click; and sideways shoves the rest of the row around. A panel below leaves every card where the user last saw it, which is what makes the caret pointing back at its card legible — and one direction means one animation, one scroll rule and one focus model to get right |
| 2026-08-02 | **The toolbox gets its own grid + `.tool-card`, not `.portals`/`Portal`; the tone-lit surface recipe is extracted from `.portal` into a shared `.tone-surface` base class** rather than copied. `cardToneClass`/`--ct`/`--card-1..10` are unchanged | `.portals` is tuned for cards carrying a description and a `go` footer; tool cards are compact, so a smaller track fits more per screen — and keeping the grids separate stops the expanding-row behaviour leaking into Midgard and Ollivanders, which don't want it. Extraction over duplication because two copies of the same gradient recipe drift, and the card palette is the one thing that must stay identical across every hub |
| 2026-08-02 | **The tool model carries `supported?: () => boolean` and an unsupported tool is not rendered on that device.** Its only user is SHA-256 (`crypto.subtle`); UUID deliberately does **not** need it | `crypto.subtle` and `crypto.randomUUID` are secure-context-only and Bifrost is served over plain http on the LAN, so both are `undefined` on every device except the host Mac at `localhost` — the "works for the developer, broken on the phone" class of bug. `crypto.getRandomValues` is *not* gated, so UUID/password are built from raw bytes and work everywhere (`core/deviceId.ts:11` already carries this fallback). Hiding the one tool that genuinely cannot run beats bundling a JS hash implementation to paper over it |
| 2026-08-02 | **New toolbox tools get functional titles, not lore names** (Base64, JWT, URL, Epoch). Existing names stand — Make a QR, Nimbus, Portkey, Iris | Owner's call. With ~13 stalls on one page, lore names stop being charm and start being a lookup problem: you cannot find the JWT tool if it is called something else. The pages that earned lore names earned them as destinations, not as rows in a toolbox |
| 2026-08-04 | **PLAN-21 — PLAN-99's "Unified library shell" promoted, and it keeps the name it already has: `Pensieve`, moved from a nested segment to top-level `/pensieve`** | Asked to suggest a name, the finding was that inventing one would be the mistake: the codebase already calls this the Pensieve **twice** — `PensievePage` at `/runestone/pensieve` and `EddaLibraryPage` at `/edda/pensieve`, both titled `<h2>Pensieve</h2>` — and the word is taught to users on every save button ("Save to Pensieve", "Back to the Pensieve", "Kept in the Pensieve"). Two pages sharing one title at two URLs is the confusion the plan exists to end; a third name would mean three names for one concept. `pensieve` is currently only ever a *nested* segment, so promoting it to a first segment means adding it to `RESERVED_ROOTS` + the guard test in the same change |
| 2026-08-04 | **The unified library is a client-side `Promise.allSettled` fan-out across each module's existing list API, never a server-side aggregate** — with a `core/library/` registry keyed by kind (label · capability · list · remove · editor route · icon · palette slot) | A server endpoint returning "all documents" would have to read three modules' tables from one place, which **rule 2 forbids**; the only legal shapes are a core-owned aggregate table or a bus-fed projection, both of which couple the storage PLAN-99's note explicitly says stays uncoupled. All three API clients already sit in `core/`, so the fan-out costs nothing. **`allSettled` not `all`**, because with three independent endpoints one being down must degrade to "the other kinds plus a Retry strip", not a blank page. The registry is where the value is: adding a fourth document type becomes one entry, not a fourth page — pinned by a test that registers a fake kind and asserts it lists/filters/sorts/deletes with no page change |
| 2026-08-04 | **PLAN-21 should run BEFORE PLAN-19 (recommended order 18 → 21 → 19 → 20)** even though its number is higher | PLAN-19's task list contains a `GrootLibraryPage` — a *third* copy of the page PLAN-21 deletes. Run 21 first and that task collapses to one registry entry with `/groot/pensieve` as a redirect; run it after and Groot builds a page that is deleted in the next PR. Numbers are identity, not a schedule — the 16b-stacked-on-16a and 17b-gate-lifted precedents already established that ordering is the owner's call |
| 2026-08-04 | **The library row's type badge takes its colour from the document *kind*, not the row position** — a scoped exception to the positional card-colour rule | Exactly the reasoning logged for Accio's hostname-hash tile (2026-07-25): the badge exists so a type is recognisable at a glance *while the list is being searched, filtered and re-sorted*, which only works if JSON keeps one colour as rows move. Hub and portal cards are untouched and stay positional |
| 2026-08-04 | **The unified library gets no server module of its own**, unlike PLAN-18's `toolbox`; the page lives in `app/pages/` with its testable logic in `core/library/` | The shell is pure client compute over three capabilities that already exist — it renders when at least one library kind is present, and the registry filters kinds by `capabilities.modules`. A module existing only to gate a page derived entirely from other modules' capabilities is ceremony. Toolbox needed one because its tools had no other capability to hang from; this does not, which also keeps the `features/` ↔ modules mapping 1:1 and puts a cross-cutting shell where the other cross-cutting shells (Midgard, Ollivanders, Diagon Alley) already live |
| 2026-08-04 | **PLAN-20 — PLAN-99's "Edda: Mermaid diagrams" and "Edda: PDF export" promoted into one plan, single PR, no parts** (owner's instruction) | They share a dependency chain, not just a feature area: a PDF that omitted the diagrams would be a bug, so the print path has to be built on top of the diagram path, and splitting them would mean building the export template twice. Both are entirely client-side — no module, route, table, migration or capability change |
| 2026-08-04 | **`renderMarkdown` stays pure and synchronous — a ```mermaid fence emits a `<pre class="mermaid-src">` placeholder and a separate async pass swaps the SVG in** | `architecture.md` names "one pure function shared by the live preview, the public page, and the HTML export" as the zero-drift property. Mermaid is async *and* needs a live DOM to measure text before it can lay a graph out, so running it inside the renderer would make that function async and DOM-bound and break all three consumers at once. `exportHtmlDocument` does become async, because an exported `.html` whose diagram is the literal text `graph TD; A-->B` is a broken artifact — and shipping mermaid's megabyte into a standalone export file to avoid that is worse |
| 2026-08-04 | **PDF export prints a generated standalone document in a hidden `srcdoc` iframe, not the app page with a print stylesheet** | The export template already *is* the clean-document representation, so printing it makes `.html` and `.pdf` the same artifact — PLAN-11's zero-drift property extended to a third output rather than forked. Printing the SPA instead would mean hiding header, bottom nav, sky relics, the Nótt overlay, the editor pane, toolbar, outline rail and divider: a dozen `@media print` overrides that rot silently every time the shell changes. It also **removes the async race structurally** — the iframe's document has its SVGs already inlined, so there is no "wait for every diagram before calling print()" timing hack to get wrong. Cleanup is on `afterprint` **with a timeout fallback**, because that event is not reliably delivered everywhere and a leaked iframe per export is a real leak |
| 2026-08-04 | **The print path forces an ink-on-paper palette regardless of the active theme** | The export inlines the *active* theme's tokens, so printing an Aurora-dark document either burns a cartridge or — because browsers drop backgrounds by default — prints near-white text on white paper. The `@media print` block in the export template fixes the plain `.html` export for anyone who prints that, too |
| 2026-08-04 | **Mermaid is treated as untrusted input**: `securityLevel: 'strict'`, `htmlLabels: false`, `suppressErrorRendering: true`, and the `secure` key list left at its default so an in-document `%%{init: …}%%` directive cannot raise its own privileges | Any device on the LAN can save an edda and `/edda/preview/:slug` is a public URL, so a diagram is untrusted markup rendered in *someone else's* browser. `htmlLabels: false` earns its place three times over: it closes the HTML-in-`foreignObject` surface, `foreignObject` is the part DOMPurify is most likely to strip, and it prints unreliably |
| 2026-08-04 | **The `<style>`-inside-SVG question is a spike to settle before anything is built on it**, not an assumption | Mermaid emits a `<style>` element inside its SVG and DOMPurify's defaults strip `<style>` — which would leave a diagram with no fill or stroke, usually invisible. Whether a scoped style block can be kept safely decides the shape of the rest (the fallback is driving colour entirely through `themeVariables` and presentation attributes), so it is fifteen minutes spent first rather than a rewrite later |
| 2026-08-04 | **Diagrams are cached by source text and invalidated on a theme change** | The live preview re-renders on a 200 ms debounce inside rAF and mermaid's layout is dagre — re-laying-out every diagram on every keystroke batch would make typing stutter in a diagram-heavy document. The theme half is the mirror problem: mermaid bakes colours into the SVG at render time, so a diagram drawn in Aurora stays dark after a switch to Daybreak and reads as broken |
| 2026-08-04 | ~~**The `previews` module's `MarkdownViewer` does not get mermaid**~~ — **SUPERSEDED the same day by the row below** | It is a separate renderer for arbitrary `.md` files on disk — it calls `marked.parse` directly and never touches `renderMarkdown`. A megabyte of lazy dependency is a different trade for a file-preview modal than for the markdown workspace |
| 2026-08-04 | **Supersedes the row above: `MarkdownViewer` DOES get mermaid, and the mermaid pass therefore lives in `core/markdown/mermaid.ts` rather than `features/edda/`.** `MarkdownViewer` also switches from bare `marked.parse` to `renderMarkdown` | Owner asked for it if it was small, and it is — **but only because the location is decided now**. In `features/edda/` the `previews` feature could never reach it (features may not import features), so adding it later would be a move plus a full re-test; in `core/markdown/`, beside `render`/`outline`/`stats`/`commands`, the second consumer is about ten lines. Both original objections dissolve on inspection: the `hasMermaid` gate means a fence-less `.md` loads nothing, and a file in `downloads/` is writable by anyone on the LAN — the same threat model as a saved edda, already covered by the pinned strict config. The switch to `renderMarkdown` also closes a real drift, giving the modal hljs highlighting, heading ids and GFM |
| 2026-08-04 | **A PLAN-99 row is deleted when its idea is PROMOTED into a numbered plan, not when that plan completes** (owner's instruction; applied retroactively to PLAN-18's and PLAN-20's rows, and supersedes the "strip it in the plan's own PR" instruction written into PLAN-18) | A backlog row describing work that already has a numbered plan is a trap for whoever reads `.agent/` next and takes it as open work — and that window is longest precisely while the plan is being implemented. A `PROMOTED … to PLAN-XX` note in its place keeps the history without keeping the false invitation. The plan's own PR still archives the plan file |
| 2026-08-02 | **PLAN-19 — a YAML workspace as the third structured-text tool on Ollivanders, named `Groot`** (owner's pick, from a shortlist spanning Norse/Greek/Ghibli/Potter/MCU/Tolkien; `Yggdrasil` was proposed first and **rejected as hard to pronounce**). Module `groot`, table `groot_docs`, library in the Pensieve, public raw endpoint at `/groot/api/:slug` | Owner asked for a YAML editor/viewer as a Runestone/Edda sibling. Groot is a literal walking tree — one trunk, many branches, folded and unfolded — in one unmissable syllable, and "I am Groot" is three fixed words meaning something different every time depending on context, which is **exactly YAML's defining hazard** and the reason this plan has an advisory rail at all. The MCU is already one of the app's worlds (the screensaver quote bank) and character names already name features (Loki, Accio, Portkey, Nimbus, Pensieve), though this is the first MCU-named *feature* and the most playful name on the shelf — a deliberate owner choice. Table takes the **`accio_links`** naming precedent (module + generic noun) because `runestones`/`eddas` does not generalise: Groot is the tree, not the document. The pure utilities stay format-named at `core/yaml/` — they describe the format, not the tool, and Variant will share them |
| 2026-08-02 | **The `yaml` (eemeli) v2 parser, client-side only — the server stores YAML text and enforces the byte cap, and never parses it** (the Edda precedent, not the Runestone one) | `yaml` v2 is the only mainstream choice giving all four of: a comment-preserving document model, multi-document support, errors with line positions, and YAML 1.2 by default (`js-yaml` destroys every comment on round-trip, which for a config file is silent data loss). The server refuses to parse because YAML alias expansion is a **billion-laughs amplifier** — a 3 KB document can expand to gigabytes, so the size cap does *not* bound the parse — and there is nothing to gain: the raw endpoint promises the bytes that were saved, exactly as Edda's markdown endpoint does. The client sets `maxAliasCount` so a pasted bomb can't hang the tab either |
| 2026-08-02 | **`@codemirror/lang-yaml` already folds block YAML — no custom `foldService`, unlike Loki's.** The work is enabling `foldGutter`/`foldKeymap` for the new mode | Verified by unpacking the package rather than assuming: its `foldNodeProp` covers `Item`, `Pair` and `BlockLiteral` (block style) plus `FlowMapping`/`FlowSequence` (flow style), folding from the end of a node's first line to its end — and a single-line `name: foo` correctly yields no arrow. Written down so nobody builds a speculative indentation-based fold service by analogy with Loki, where lang-javascript had a genuine `ObjectPattern` gap |
| 2026-08-02 | **YAML mode pins `indentUnit.of('  ')`**, making a literal tab untypeable | `JsonEditor` binds `indentWithTab` globally and CM's `insertTab` inserts the `indentUnit` facet. A tab in YAML indentation is a hard syntax error, so without the pin the editor's own Tab key is the easiest way to produce a file that will not parse |
| 2026-08-02 | **`JsonEditor`'s `plain`/`markdown`/`javascript` booleans collapse into one `mode` prop, in its own commit before any YAML code** | A fifth mode would mean a fourth boolean, a fifth ternary branch, and mutual exclusivity enforced only by a doc comment. It touches Runestone, Variant (both panes), Edda and Loki with no behaviour change, and `rules/coding.md` forbids mixing a refactor with a feature in one commit — so the refactor lands green on its own |
| 2026-08-02 | **YAML's ambiguities are surfaced as non-blocking advisories that change no bytes** — unquoted `no`/`yes`/`on`/`off`, duplicate keys, indentation tabs, version-like numbers, unsafe-range integers, anchors | These are *valid* documents that mean something other than they look like, so they are not lint errors. The Norway problem is the sharp case: `country: no` is the string "no" under the YAML 1.2 schema this parser uses, and `false` in any YAML 1.1 consumer (Ruby, older Python, some CI runners) — correct here and wrong on the reader's machine. Telling the author beats silently rewriting their file |
| 2026-08-02 | **Iris implements WCAG contrast client-side instead of reusing `themes/contrast.ts`, correcting PLAN-99's note** | That util is in the wrong workspace *and* inside a server module — it is unreachable from the browser twice over. Forty lines of WCAG maths, unit-tested against the same vectors as `theme-validation.test.ts`, beats introducing a shared client/server package to share them |
| 2026-08-06 | **The panel's scroll is planned twice — once on open, and once more when the panel's height first changes — via a `ResizeObserver` that then disconnects.** The plan specified one pass (a layout effect plus one `requestAnimationFrame`) | Found by running it: the *first* tool opened arrives as a lazy chunk, so the frame that pass ran in held the **skeleton**, not the tool. Planning against a panel a third of its final height picks the wrong branch outright — a tall tool took the "it fits" path and landed 60px too high at 1280×420, with its own heading above the fold, which is precisely the bug the tall-panel branch exists to prevent. Re-planning on the first real height change costs at most two scrolls per open, and because the observer disconnects after it, a window resize on a settled panel still cannot move the page (criterion 3 measured: scrollY unchanged across 1280 → 760 → 1600 with a panel open) |
| 2026-08-06 | **The caret is two stacked border-triangles, not a rotated square** | A square sitting half outside the panel shows its own lower edges wherever its fill fails to match the panel's background — and the panel's background is a tone gradient, so on Aurora it read as a floating diamond in the screenshot. A triangle has no lower half to give itself away. The measured-or-hidden rule from the plan is unchanged |
| 2026-08-06 | **The `.tone-surface` extraction renamed `.portal__icon` to a shared `.tone-chip`, and hover moved from `a.portal:hover` to an opt-in `.tone-surface--interactive`** | The icon chip is part of the recipe the plan says must not drift, and a BEM class named after one component cannot be shared by two. The hover selector had to stop keying on the element being a link, because a tool card is a `<button>` — `Portal` applies the modifier in exactly the case it renders an `<a>`, so a coming-soon card still gets no hover. Verified by comparing **computed styles and box geometry of all 8 portal cards on both hubs, before and after**: zero differences (criterion 10). A screenshot diff would have been the wrong instrument — `SkyRelics` places its line-art randomly, so two runs of the *same* build never match pixel for pixel |
| 2026-08-06 | **The QR tool's output column pairs the code with what it encodes**, rather than centring the canvas in the column | A QR is inherently a fixed-size square, so the obvious rendering is exactly the "centred fixed-width block with empty gutters" the plan forbids as an acceptance criterion. Putting the encoded text, its length and the error-correction note beside it uses the width and answers the question the user has next. Measured at 1600px: every tool's panel body reaches both edges (rightmost gutter 0) |
| 2026-08-06 | **Card order on Diagon Alley follows PLAN-18's registry table — Nimbus, Portkey, then the tools** — which recolours the existing cards | Colour follows position, so any order is a colour decision. The plan's table is the specification and the two page-owning tools reading first keeps the "these two leave the page" cards together instead of scattered among the ones that expand in place |
| 2026-08-06 | **The JWT tool's "this decodes, it does not verify" is a standing note above the input, and the verdict reads "Unexpired", never "Valid"** | The plan required the panel to state it; where it states it decides whether it works. A footnote under the output is read after the reader has already formed a judgement from a green badge. The word matters as much as the placement: "Valid" beside a decoded token is the exact false conclusion — an unexpired token is still forged if the signature is wrong, and this tool has no key with which to know |
| 2026-08-06 | **The password generator draws by rejection sampling, not `byte % alphabet.length`** | 24 symbols do not divide 256, so the modulo makes the first 16 characters ~1.5× more likely than the rest — and the panel prints an entropy figure underneath that is only true if the draw is uniform. A few discarded bytes are the price of not lying in the UI. Pinned by a distribution test comparing the head of the alphabet against its tail |
| 2026-08-06 | **Base conversion runs on `BigInt`** | The values people paste into a base converter are ids — Snowflakes, database keys, 64-bit hashes — which are past `Number.MAX_SAFE_INTEGER`. Silently rounding the last digits is the worst failure a converter can have, because the output still looks like a number. The test pins a 25-digit decimal and asserts `Number()` would have mangled it |
| 2026-08-06 | **Iris's theme export drives only the decorative roles from the palette; `--ok`/`--danger`/`--warn` keep semantic defaults** | Found by exporting a real palette and reading it: a five-blue photo produced a navy `--warn` and a sandy `--danger`. A photo has no opinion about which of its colours means "this failed", and a theme whose danger colour is not red-ish is actively worse than the default. The accents and the ten positional card hues do come from the palette, which is the part a palette can legitimately decide. **Verified end to end**: a generated theme POSTed to the running server returned **201 with `warnings: []`** — so it satisfies the real schema *and* the WCAG contrast lint — then applied with `data-theme` and `--card-1` reading back the extracted colour |
| 2026-08-06 | **`splitWords` is Unicode-aware (`\p{L}`/`\p{N}`), and `slugify` runs through it after folding diacritics** | Two bugs in one place, both found by running the tool rather than by the unit tests. With an ASCII character class `Crème` splits on its own accent into `Cr` + `me`, so every case form mangled a word the user typed correctly. And slugging the *raw* string — the obvious implementation — has no separators to work with in a camelCase input, so `HTTPResponseCode` slugged to `httpresponsecode`; going through the shared split gives `http-response-code`, which is what every other form already produced |
| 2026-08-08 | **PLAN-21: the two per-tool library pages are deleted and replaced by one shell over a `core/library/` registry** — `LibraryKind`/`LibraryItem` plus one entry per kind carrying label, capability, `list`, `remove`, editor route, icon, palette slot and SSE events. The per-tool API clients (`core/runestone.ts`, `core/edda.ts`, later `core/groot.ts`) are untouched; the registry adapts them | `RunestoneSummary` and `EddaSummary` were **field-for-field identical**, both pages held the same six state fields and ran the same SSE effect, and `EddaLibraryPage` already reused Runestone's `rune-lib-*` CSS — the codebase admitting they were one page twice. Groot would have been a third copy. The return on the plan is that a fourth type is one array element and **no page change**, which is why that is the one thing pinned by a dedicated test (a fake kind that lists, filters, sorts and deletes through the same code paths) rather than left as an aspiration |
| 2026-08-08 | **The fan-out is client-side `Promise.allSettled`, never a server-side aggregate**, and a failed kind yields a non-blocking "<Label> documents couldn't be loaded · Retry" strip plus a disabled chip beside the kinds that answered | A server endpoint returning "all documents" would read three modules' tables from one place, which rule 2 forbids; the only legal shapes are a core-owned aggregate table or a bus-fed projection, and both couple storage the backlog row explicitly said stays uncoupled. All three clients already sit in `core/`, so the fan-out is free. `all` was rejected outright: with three independent endpoints it turns one flaky module into an empty library, which reads as "my documents are gone" rather than "one thing is down" |
| 2026-08-08 | **The type badge's colour follows the kind (a fixed `tone` per registry entry), not the row's position** | A scoped exception to `rules/coding.md`'s positional-palette rule, on exactly the reasoning logged for Accio's hostname-hash tile (2026-07-25): the badge exists so a type stays recognisable *while the list is being searched, filtered and re-sorted*, and position-based colour would repaint every badge on every sort. Hub and portal cards are untouched and stay positional — including the new Pensieve card, which takes whatever slot its position on Ollivanders gives it |
| 2026-08-08 | **`q`/`author`/`sort`/`order` are sent to every kind's endpoint *and* re-applied locally by `filterItems`; the type chip is applied only locally** | The redundancy is deliberate: one rule keeps three independent sources honest, and it is what lets an SSE-driven refresh narrow a list already in hand. The type filter has no server to ask — no endpoint knows the other kinds exist — so the chip is instant and costs no round trip. The merge is then re-sorted client-side because three separately-sorted lists stacked on each other is not one sorted list |
| 2026-08-08 | **`sortItems` breaks ties on `id`, then on `kind`** | The repositories already tie-break on `asc(id)`, so matching them keeps one shared ordering. The second key is the one that is easy to miss: ids are only unique **within a table**, so a runestone and an edda can hold the same 6-char handle, and without it two rows with equal sort keys could swap places between renders. It is also why every row is keyed `${kind}:${id}` |
| 2026-08-08 | **`kinds` is memoised on the capability list's *contents*, not on the `capabilities` object** | Found by testing: `kinds` is what both the load effect and the SSE subscriptions depend on, so a fresh-but-equal capabilities object re-fetches every kind and re-subscribes to every event on every render. Keying on the joined module string makes the dependency what it actually is |
| 2026-08-08 | **The page lives in `app/pages/`, owns no capability, and renders whenever at least one kind is present** | It is a shell across several features, and features may not import features — the same reason Midgard, Ollivanders and Diagon Alley live there. Adding a module purely to gate a page derived entirely from *other* modules' capabilities would be ceremony; this is the one place PLAN-18's `toolbox` precedent does not transfer, because toolbox's tools had no other capability to hang from. All the logic worth testing sits in `core/library/`, where it runs without a DOM |
| 2026-08-08 | **Deviation (additive): a Pensieve card was added to the Ollivanders hub, and `/pensieve` to that tab's `match` list.** The `Tool` interface there gained `modules: string[]` (any-of) so one card can hang off several capabilities | Not in the plan's task list. Without it the unified library's only entrances are the Pensieve buttons *inside* the two editors — which is how the per-tool pages were reached, so nothing regressed, but a library whose whole point is spanning tools is not discoverable from the hub that holds them. The nav `match` entry is the smaller half of the same problem: `/pensieve` is a new first segment, so no category tab would have lit on it |
| 2026-08-08 | **Groot is not in the registry yet, and that is the plan's own recommended order (18 → 21 → 19 → 20)** | PLAN-19's task list contains "`GrootLibraryPage` (Pensieve)" — a third copy of the page this plan deletes. Running 21 first collapses that task to one registry entry and makes `/groot/pensieve` a redirect with no page behind it. `LibraryKind` already names `groot`, and the registry is an **array** rather than a `Record<LibraryKind, …>` precisely so a named-but-absent kind is legal |
| 2026-08-10 | **PLAN-19 implemented. `intAsBigInt` on the formatting parse only — the analysis parse keeps numbers** | Found by formatting a real manifest and *reading the result*, not by a test: re-emitting a document goes through the parsed value, so `buildId: 9007199254740993` came back as `…992`. Format is a re-indent and may never edit a value, and the fact that the `unsafe-integer` advisory warns about that very number made the silent rewrite worse rather than better. The analysis path deliberately keeps `number`: `toJS()` would otherwise hand BigInts to `JSON.stringify`, which throws on them, and to the tree view — and losing the digits in a *JSON conversion* is honest, because that is exactly what JSON does |
| 2026-08-10 | **YAML mode binds `Tab` explicitly at `Prec.high`; `indentUnit.of('  ')` alone is not enough — correcting the plan's stated mechanism** | The plan (and decisions, 2026-08-02) said "CM's `insertTab` inserts the `indentUnit` facet", so pinning the unit would make a literal tab untypeable. It does not: `defaultKeymap` binds Tab to `insertTab`, which on an **empty selection** inserts a literal `"\t"` whatever `indentUnit` says, and the trailing `indentWithTab` never gets a look in. A tab in YAML indentation is a hard syntax error, so this was the single easiest way for the editor to write a file it could not parse. A test drives the real keymap through `runScopeHandlers` (not the command directly, which is where the earlier reasoning went wrong) and fails without the binding. JSON and JavaScript keep the stock behaviour, where a tab is legal whitespace |
| 2026-08-10 | **Duplicate keys are parsed with `uniqueKeys: false` and reported as an advisory, and `<<` merge keys are resolved (`merge: true`)** | Two schema decisions the plan named only by their outcome. A duplicate key is valid YAML that every parser accepts — leaving it as the library's default *error* would refuse the save, contradicting the plan's own "advisories change no bytes and block nothing". Merge keys are a YAML 1.1 extension the 1.2 core schema drops, but Docker Compose, GitLab CI and most of the real world rely on them, so resolving them is what makes the tree view show what the reader's tool will see; the `merge-key` advisory says out loud that a strict 1.2 reader treats `<<` as an ordinary key |
| 2026-08-10 | **Two advisories beyond the plan's table: `merge-key` and `parser-warning`** | Both are cases where a value quietly becomes something other than it looks like, which is the rail's whole remit. `parser-warning` surfaces `doc.warnings` — an unresolvable tag (`!!weird 1` silently becomes the string `"1"`), an unimplemented version — which the library exposes *nowhere else*, so without this they were collected and dropped |
| 2026-08-10 | **`TreeView` gained one optional `annotationAt` prop rather than Groot rendering its own tree** | The tree shows **resolved** values, as every YAML consumer sees them, which leaves an aliased branch looking exactly like a hand-written duplicate of another one — criterion 8 asks for the alias to be marked. A second tree implementation to add one badge would be the wrong trade; the prop is undefined for Runestone and Variant, so nothing else changes. Note the limit, since it is not a defect: an alias used through a **merge key** has no node of its own in the resolved value, so it is explained by the rail rather than badged in the tree |
| 2026-08-10 | **Cross-tool handoffs travel as one-shot `sessionStorage` seeds — `core/runestoneSeed` joins `core/variantSeed`** | Runestone addresses saved documents by slug, and a document converted from YAML has never been saved, so there is no URL that can carry it. The Loki→Variant bridge already established the shape: the source writes and navigates, the destination reads once on mount, and no feature imports another. In Runestone the seed is merged into the existing draft-prompt effect rather than added beside it — two effects on the same dependency would have let the draft prompt reappear over a document the person had just asked for |
| 2026-08-10 | **The Groot card is appended to Ollivanders (6th), and the Pensieve's kind chips now wrap instead of scrolling** | Card colour follows position, so inserting Groot beside the other editors would silently recolour Loki and the Pensieve on a page people already know by its colours. The chip change came out of measuring the phone layout: `.lib-chips` is a `nowrap` scroller with a hidden scrollbar, which always fitted with two kinds — the third pushed YAML past the right edge at 375 with nothing pointing at it. Wrapping costs one line of height on a phone and keeps every kind reachable without a swipe |
| 2026-08-10 | **The scratch buffer is restored silently when you *return* to an editor inside one page lifetime, and still only *offered* on a fresh page load** (`core/draftReturn`, applied to Runestone and Groot). Leaving also **flushes** the buffer instead of trusting the 500 ms debounce | Owner report: converting between Runestone and Groot lost the buffer, because the draft was written to localStorage but never applied — it sat behind a "Restore the draft from your last visit?" prompt. Jumping to the other tool to see the same document in the other format and coming back is **one task**, not two visits, so the content should simply be there; opening a tool fresh is the opposite case, where silently refilling the editor with something written days ago would be wrong, which is why the prompt stays. The marker that separates them is deliberately **module scope, not storage**: a reload wipes it and a client-side navigation does not, which is exactly the line between the two. `sessionStorage` would survive a reload and turn every refresh into a silent restore. Flushing on unmount fixes a real second bug found while testing it — the draft is debounced 500 ms, so clicking the convert button within half a second of typing lost the last keystrokes outright; the leave handler is guarded on `isScratch` read through a ref, so a document that was *just saved* (which clears its draft on purpose) cannot resurrect it |
| 2026-08-15 | **PLAN-20's spike answered the opposite way to the plan's premise: DOMPurify's SVG profile *keeps* a `<style>` element inside the `<svg>`, so no fallback is needed** — sanitize with `USE_PROFILES: { svg: true, svgFilters: true }` and nothing else | The plan called this "the one thing to prove first" because mermaid puts its colours in a `<style>` and DOMPurify's defaults strip that tag. Rendering a flowchart, a sequence diagram and a Gantt chart in a dark and a light palette showed the sanitized output **byte-identical in length** to the raw SVG, `<style>` intact, computed `fill`/`stroke` identical with and without `ADD_TAGS: ['style']`: `style` is on DOMPurify's SVG allow-list even though the HTML profile drops it. The plan's fallback — driving colour entirely through `themeVariables` and presentation attributes — is therefore not built, and the fifteen minutes bought certainty instead of a rewrite |
| 2026-08-15 | **Mermaid's `secure` key list is extended past its default to cover `themeCSS`, `theme`, `themeVariables`, `fontFamily`, `altFontFamily` and `htmlLabels` — a deliberate departure from the plan's "leave the `secure` list at its default"** | The plan's *reason* for that instruction was that an in-document `%%{init: …}%%` directive "cannot raise its own privileges". Reading mermaid 11.16's source, the default list is exactly `secure, securityLevel, startOnLoad, maxTextSize, suppressErrorRendering, maxEdges` — **`themeCSS` is not on it**, and `themeCSS` is raw CSS that lands inside the `<style>` element the sanitizer now deliberately keeps. This was reproduced, not reasoned about: a diagram carrying `%%{init: {"themeCSS": "& .node rect { filter: url(http://host/BEACON.png#f) }"}}%%` **made the browser fetch that URL during render** — a beacon from a document any LAN device can save, rendered on a public page, breaking "Bifrost is offline by design" (criterion 7) and criterion 6's "the directive does not take effect". With the keys locked, the CSS never reaches the output and no request is made. Keeping `<style>` and leaving `themeCSS` writable are only safe apart, never together |
| 2026-08-15 | **The mermaid pass renders into the cache first and swaps the DOM synchronously last, and never holds a node across an `await`** (`renderMermaidIn` takes a container *or a getter*) | Found by running it: the first implementation collected the placeholders, awaited mermaid, then replaced them — and the diagrams never appeared, because the container belongs to React and had been re-set from `dangerouslySetInnerHTML` while the import was in flight, so every `replaceWith` landed on a detached node (`isConnected: false`, silently a no-op per spec). Splitting the pass so the awaits only fill the cache, and re-resolving the container for a synchronous swap, makes a late pass a no-op instead of a wrong write and removes the whole class of race — including React swapping the node itself, which the getter covers |
| 2026-08-15 | **Every `dangerouslySetInnerHTML` consumer memoizes its `{ __html }` object** (`MarkdownPreview`, `MarkdownViewer`) | React compares that prop **by object identity**, so an inline `{ __html: html }` literal re-sets `innerHTML` on every re-render of the parent — for Edda that is once per keystroke, via the word-count/outline/byte-count state. The diagrams the pass had just swapped in were being erased a few hundred milliseconds later by a re-render that changed nothing, which is what made the failure look intermittent. Memoizing is both the fix and a real saving: the preview stops re-parsing and re-laying-out the whole document on every keystroke |
| 2026-08-15 | **The HTML/PDF export inlines each diagram twice — once in the active theme for screen, once ink-on-paper for print — toggled by `@media print`** | The plan's paper-palette decision covers the document tokens, but mermaid bakes colour into the SVG at render time, so a single copy cannot satisfy both criterion 13 ("the `.html` export … prints to the same paper palette") and the export's existing promise of opening in the theme it was written in. One copy would mean either an Aurora-dark diagram printing as ink-heavy blocks on white, or a near-invisible pale diagram on the dark page. The cost is one extra render and ~15 kB per diagram in the exported file only, which is the right side of that trade for an artifact |
| 2026-08-15 | **PLAN-20's "PLAN-99 correction" was itself wrong: the Edda coming-soon footer *does* exist** (`.edda-soon`, `COMING_SOON` in `EddaPage.tsx`), and this PR removes "Mermaid diagrams" and "PDF export" from it, leaving "Image paste" | The plan states flatly that "there is no coming-soon footer in the Edda UI — the note is stale, and there is nothing to remove there". There is: PLAN-11 shipped one, listing exactly the three parked features, two of which this plan delivers. Shipping the features while the page still advertises them as coming soon would be the visible half of the bug. Recorded because the plan file is the spec of record and this contradicts it |
| 2026-08-15 | **`hasMermaid` walks marked's own lexer output, following every array of token-shaped children rather than a fixed key list** | It has to agree *exactly* with the renderer, since it is the gate for the ~1 MB mermaid chunk — disagreement means a document showing a raw placeholder with nothing loaded to fill it. A regex over the source cannot tell a fence from the word "mermaid" in a sentence or from a fence quoted inside a wider code block. The first implementation followed `tokens`, `header` and `rows`, and the corpus test caught it missing a fence inside a **list item**, whose children hang off `items` — hence the generic walk rather than another enumerated key |
